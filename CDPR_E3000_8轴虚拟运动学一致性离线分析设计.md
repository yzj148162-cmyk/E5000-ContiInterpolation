# CDPR E3000 8轴虚拟运动学一致性离线分析设计

## 1. 目的与适用范围

当前八个电机尚未接入绞盘、绳索和 CDPR 框架，因此 Trace 只能证明电机轴按
指令运动，不能直接证明真实动平台已经按规划位姿运动。本设计定义一套**离线
虚拟运动学一致性分析**方法：把同一 Trace 帧中的八轴实际位置按假设绞盘模型
换算为八根虚拟绳长，经正运动学得到虚拟末端位姿，再与同一规划时刻的期望
末端位姿比较。

本分析统一适用于：

- 离线 PVT 位控：整条八轴位置表在启动前已经生成并装入板卡；
- 八轴实时速度模式位置闭环：每个控制周期在线下发速度命令；当前测试可使用
  已生成的五次多项式参考轨迹，未来可替换为 Newmark-β 实时生成的期望状态；
- 后续力交互控制：期望状态由当前力样本计算得到，必须逐周期写入运行记录。

分析只读运行记录，绝不参与运动控制、PID、Trace 读取或安全停机。

## 2. 结论边界

该分析可验证：

- EtherCAT 轴号、CDPR 绳编号、出绳点和平台连接点的映射是否一致；
- 电机正负方向、零位、角度/card unit 换算和假设绞盘半径是否一致；
- 八轴实际反馈组合后，是否能恢复出与规划相近的**虚拟**末端轨迹；
- 各轴不同的响应延迟是否表现为多轴同步误差。

该分析不能验证真实绞盘卷绕半径、绳索弹性与松弛、滑轮包络、张力、干涉或真实
动平台位姿。接入样机后，应将 Nokov 测得的真实位姿作为最终外部真值。

## 3. 必需配置与映射

每一根绳在 `cdpr_configuration.json` 中必须明确包含：

```text
cable                 CDPR 绳编号 / 该行出绳点编号
axis                  对应 EtherCAT 轴号
direction             电机正角度对应绳长增加(+1)或减少(-1)
frame_anchor_m        该绳的固定端/出绳点世界坐标
platform_anchor_m     该绳的动平台连接点局部坐标
```

分析器不得假设“绳号等于轴号”。当前配置恰好为 `cable i -> axis i`，但未来实体
布线或安装方向改变时，只修改配置中的 `axis` 和 `direction`。

每次运行还必须保存配置快照，至少包括：八根绳映射、绞盘半径、card unit 定义、
Trace 周期、初始末端位姿和初始绳长。否则将来配置改动后无法复现旧日志。

## 4. 同帧实际状态与虚拟正运动学

设第 `k` 个 Trace 帧序号为 `s_k`，其中八个轴的实际位置均来自同一帧。首先以
运行起点实际位置为零增量基准：

\[
\Delta\theta_i(k)=\theta_{act,i}(k)-\theta_{act,i}(k_0)
\]

其中角度必须已按该次运行的 card unit 换算为输出轴角度。使用假设绞盘半径
`r` 后，虚拟绳长为：

\[
l_{act,i}(k)=l_{0,i}+direction_i\,r\,
\Delta\theta_i(k)\frac{\pi}{180}
\]

随后对**同一个 Trace 帧**的全部八根绳长做正运动学：

\[
q_{FK}(k)=FK(l_{act,0}(k),\ldots,l_{act,7}(k))
\]

严禁按各轴单独延迟把不同帧的实际位置拼成一组再做正运动学。动平台某一时刻的
构型只取决于这一时刻八根绳的共同状态；逐轴移帧会构造出不存在的混合构型。

正运动学须同时输出八根绳的重建残差和求解状态。残差显著增大时，优先检查轴—
绳映射、方向、零位、单位和绞盘半径，而不应立即归因于控制器。

## 5. 统一时间轴：启动锚点与全局延迟

### 5.1 为什么需要启动锚点

规划轨迹从 `t=0` 开始，而 Trace 序号从配置 Trace 起一直递增。离线比较前必须
确定规划的零时刻对应哪一帧 Trace：

\[
t_{run}(k)=(s_k-s_{start})T_{trace}
\]

其中 `s_start` 是本次运行的有效启动 Trace 序号。没有它，无法知道某一帧实际
位置应与规划的第几个毫秒比较。

### 5.2 全局延迟的定义

每轴延迟标定给出的 `\tau_i` 用于单轴诊断。八轴末端位姿比较只使用一个公共
延迟 `\tau_g`，例如通过全部有效 `\tau_i` 的中位数得到初值：

\[
\tau_g^{(0)}=median(\tau_0,\ldots,\tau_7)
\]

再在本次运行的有效运动区间内联合拟合：

\[
\tau_g^*=arg\min_{\tau}\sum_{k,i}w_i
 [\theta_{act,i}(k)-\theta_{plan,i}(t_{run}(k)-\tau)]^2
\]

实际位姿与规划位姿的对比为：

\[
e_q(k)=q_{plan}(t_{run}(k)-\tau_g)-q_{FK}(k)
\]

各轴相对公共延迟的偏差
`\Delta\tau_i=\tau_i-\tau_g` 必须保留并报告，它代表真实的轴间响应不一致，
不能在正运动学前逐轴补偿掉。

### 5.3 离线 PVT 的锚点

`dmc_pvt_move()` 返回只表示主机调用完成，不能直接作为板卡进入 PVT 的硬件时刻。
应使用 Trace type05 指令位置与 PVT 规划位置表的整体匹配确定有效锚点：

\[
s_{start}=arg\min_s\sum_{k,i}
 [\Delta\theta_{cmd,i}(k)-\Delta\theta_{plan,i}((s_k-s)T_{trace})]^2
\]

其中比较两者相对启动位置的变化量，避免绝对零位差影响。运行时还应写入
启动调用前后的 Trace 序号和主机调用时刻，用于缩小离线搜索窗口、记录诊断；
最终锚点以 type05 全轨迹匹配结果为准。该锚点的时间分辨率受 Trace 周期限制，
当前为 1 ms。

### 5.4 八轴实时速度闭环的锚点

八轴实时速度闭环由主机控制周期生成参考状态并下发速度命令。启动时，在第一帧
八轴有效 Trace 反馈到达时，运行模块已记录
`cdpr_velocity_control_time_anchor trace_sequence=...` 事件；该序号即为
`s_start`。同一瞬间启动主机单调计时器，主机参考时间为控制命令唯一时间基准。

离线分析仍以 Trace 帧时间重建反馈时间：

\[
t_{run}(k)=(s_k-s_{start})T_{trace}
\]

并从运行记录中的期望状态序列插值得到 `q_plan(t_run-\tau_g)`。当前已知测试
轨迹可从导出的规划表重建；未来 Newmark-β 在线产生的状态必须逐周期记录，
不能在事后根据新传感器数据重新计算。

## 6. 运行记录要求

每次离线 PVT 或八轴实时速度闭环运行自动建立独立目录，至少包含：

- `metadata.json`：运行模式、Trace 周期、card unit、绞盘半径、版本和配置摘要；
- `configuration_snapshot.json`：完整 CDPR 结构与轴—绳映射快照；
- `events.log`：启动请求、启动 Trace 序号/锚点、停止、故障和丢帧事件；
- `expected_trajectory.csv`：每个时间点的末端期望位姿、速度、加速度，以及八轴
  期望位置和速度；
- `trace_position.bin`：每个 Trace 帧的序号、同帧八轴 type05/type06 位置、
  type03/type04 速度、状态字和错误码；
- `analysis/`：离线分析生成物，不覆盖原始记录。

原始 Trace 永远按采样顺序保存。延迟补偿、插值或滤波只能出现在分析输出中。

## 7. 离线分析流程

1. 校验记录完整性：格式版本、八轴有效掩码、Trace 序号连续性、配置快照、期望
   轨迹和运行结束原因；若发生丢帧，报告无效时间区间，不把它静默拼接。
2. 根据模式建立 `s_start`：PVT 使用 type05—规划表匹配；实时速度闭环读取启动
   事件，并交叉核验首帧命令变化。
3. 从有效单轴标定读取 `\tau_i`，形成 `\tau_g` 初值；在有足够运动激励时完成
   全局联合拟合，并输出置信度、搜索范围和残差。
4. 对每一同帧八轴 Trace 位置换算虚拟绳长，执行正运动学并记录残差。
5. 以 `t_run-\tau_g` 插值查询期望末端位姿和期望绳长；不得逐轴错移实际位置。
6. 计算平移、姿态、绳长、轴位置和时间同步误差，形成统计值、峰值和异常区间。
7. 输出报告和曲线；若正运动学失败、残差过大、Trace 不连续或延迟质量不合格，
   明确标记本次动态比较为“不可信”。

## 8. 输出文件与判定量

生成 `analysis/virtual_consistency.csv`，每行对应一个 Trace 帧，包含：

- Trace 序号、Trace 时间、运行相对时间、延迟对齐后的规划时间；
- 八轴规划/实际角度，八根规划/实际虚拟绳长；
- 规划末端位姿、同帧正运动学末端位姿；
- 三维平移误差、三维姿态误差、总姿态角误差；
- 正运动学绳长残差、各轴 `\Delta\tau_i`、有效性标志。

生成 `analysis/summary.json`，包含启动锚点、全局延迟、每轴延迟、Trace 丢帧统计、
最大/RMS 位置姿态误差和所有不可信原因。

建议绘制：规划/虚拟正解末端六自由度曲线、八根绳长误差、八轴同步误差、正运动学
残差和误差范数随时间曲线。

## 9. 实施顺序

1. 补齐运行记录中的配置快照、末端期望状态和 PVT 启动诊断序号；
2. 实现二进制 Trace 解码器及记录完整性校验；
3. 实现同帧八轴角度—虚拟绳长—正运动学计算；
4. 实现 PVT type05 锚点匹配与实时速度闭环事件锚点读取；
5. 实现公共延迟拟合、误差统计、CSV/JSON 输出；
6. 最后接入 UI 的“运行记录分析”入口和曲线显示。

分析器应首先以当前八电机空载试验验证配置、方向和单位自洽；接入绳索与动平台后，
再增加 Nokov 真值位姿对比，作为虚拟正解之外的实物验证。
