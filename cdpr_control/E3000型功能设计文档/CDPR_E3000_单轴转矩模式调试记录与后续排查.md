# CDPR E3000 单轴转矩模式调试记录与后续排查

> 状态日期：2026-07-28  
> 当前结论：转矩模式暂时停止继续调试。本文件用于保存已确认事实、尚未解决的问题和下次恢复调试时的最短排查路径。

## 1. 文档用途

本文件不是功能设计文档，而是当前实机调试状态的交接记录。恢复转矩模式工作时，应先阅读
本文，再查阅《CDPR_E3000_单轴转矩模式测试设计》，避免重复验证已经排除的问题。

当前实际控制卡已经换回雷赛 E3000。程序窗口和底层类已采用CDPR及卡型无关命名：
`LeadshineMotionCard` 封装SDK调用，`MotionCardHardwareInterface` 管理独占硬件线程。

## 2. 当前硬件与软件基线

- 控制卡：雷赛 E3000 EtherCAT 总线控制卡；
- 伺服驱动器：ISMC Diamond 系列；
- 减速器减速比：`44:1`；
- 电机编码器：增量式编码器，1024 线、4 倍频，即 `4096 pulse/电机圈`；
- 减速器输出轴分辨率：`4096 × 44 = 180224 pulse/输出轴圈`；
- 输出轴脉冲当量：`180224 / 360 = 500.622222 pulse/deg`；
- 电机额定转速：`5370 rpm`；
- 最近一次已记录的 LTDMC 运行库版本：`2.5.3.3`；
- 当前 EtherCAT 控制卡日志设置周期：`1000 us`；
- Diamond V21 手册说明 EtherCAT 通信周期默认值为 `4 ms`，主站周期应与伺服端一致；当前尚未完成伺服端实际同步周期的最终核对。

## 3. 手册与实现依据

- [雷赛 EtherCAT 控制卡转矩功能应用说明](./雷赛EtherCAT控制卡转矩功能应用说明-20190618(1).pdf)；
- [雷赛控制 EtherCAT 总线卡用户使用手册](./雷赛控制EtherCAT总线卡用户使用手册20250728.pdf)；
- [ISMC Diamond 系列伺服使用手册 V21](./ISMC%20Diamond系列伺服使用手册%20V21.pdf)；
- [单轴转矩模式测试设计](./CDPR_E3000_单轴转矩模式测试设计.md)；
- 原工程 `0523_final` 的转矩模式实现，仅作为另一种驱动器配置下的参考。

## 4. 对象、单位与换算

### 4.1 目标与实际转矩

Diamond 手册将目标转矩定义为额定转矩千分比：

```text
raw = 1000  → 100% 额定转矩
raw = 200   → 20% 额定转矩
raw = 50    → 5% 额定转矩
```

当前程序 UI 使用百分比，因此边界换算为：

```text
TorqueRaw = round(TorquePercent × 10)
TorquePercent = TorqueRaw / 10
```

相关对象为：

- `6071h:00h`：目标转矩，INT16，额定转矩千分比；
- `6077h:00h`：实际转矩，INT16，额定转矩千分比。

该换算已经通过 `6071h` 读回验证，不是当前“不运动”问题的原因。

### 4.2 最大电机速度

`6080h:00h` 是 CiA402 最大电机速度对象，原生值使用电机侧 `rpm`。直接写入 `5370`
表示限制电机最高速度为 `5370 rpm`，不应乘编码器脉冲数，也不应写减速器输出轴转速。

按 `44:1` 折算：

```text
5370 rpm / 44 = 122.045 rpm（输出轴）
122.045 rpm × 6 = 732.273 deg/s（输出轴）
```

`5370 rpm` 在单位上正确，但作为第一次转矩试验的硬件速度上限不够保守。恢复调试时建议先使用
电机侧 `100～300 rpm`，待确认控制链正常后再提高。

### 4.3 官方软件中的归一化速度限制

Diamond 官方调试软件“模块控制/转矩模式”页面显示：

- 目标转矩：`rated‰`；
- 转矩斜坡：`rated‰/s`；
- 正、负速度限值：`rated velocity‰`。

例如额定速度按 `5370 rpm` 计算：

```text
1000‰ = 5370 rpm
50‰   = 268.5 rpm
20‰   = 107.4 rpm
```

这里的正、负速度限值是额定速度千分比，不能把界面中的 `50` 直接当作 `6080h=50 rpm`。

### 4.4 原工程的 220Bh

原工程配套的是另一品牌驱动器，使用厂商对象 `220Bh:00h` 作为转矩模式速度限制，其原始单位是
`pulse/s`。原工程先在 UI 输入 rpm，再按以下方式换算：

```text
rawPulsePerSecond = rpm / 60 × 360000
```

原工程默认 `600.01 rpm`，实际写入约 `3,600,060 pulse/s`。该对象及换算不能移植到
Diamond 的 `6080h`。

## 5. 当前程序调用链

当前程序的主要调用路径为：

```text
UI目标转矩百分比
  → percent × 10 得到 raw
  → nmc_torque_move
  → 控制卡切换伺服运行模式
  → 6060h/6061h = 10（CST）
  → 6071h 接收目标转矩
  → nmc_get_torque 读取 6077h
```

启动不会自动使能轴，也不会在每次转矩测试开始时自动写入 `6080h`。同方向在线调整使用
`nmc_change_torque`；停止使用 `dmc_stop`。

## 6. 已确认的实机事实

### 6.1 Diamond 官方软件

使用 Diamond 官方调试软件的本地“转矩模式”可以使电机正常运动。因此基本可以确认：

- 电机和编码器能够工作；
- 驱动器功率级与电流环具备输出能力；
- 机械系统并非完全卡死；
- 抱闸至少在官方软件测试流程下能够正确释放；
- Diamond 本地转矩控制参数能够形成有效输出。

官方软件的本地转矩模式并不等于 E3000 经 EtherCAT 执行 CST。它可能同时设置转矩斜坡、
正负速度限值、内部指令源和完整控制字序列，因此仍需与 EtherCAT 路径对比。

### 6.2 E3000/EtherCAT 路径

当前程序的实机日志已经确认：

- `nmc_torque_move` 返回成功；
- 调用前伺服处于模式 8，调用后 `6060h=10`、`6061h=10`；
- 驱动器已经显示进入 CST；
- `6071h` 能读回程序发送的目标转矩 raw；
- 控制卡认为轴处于转矩运动状态；
- `6077h` 实际转矩仅在零附近小幅波动；
- Trace 实际速度为零；
- Trace 实际位置没有发生可见变化；
- 目标提高到较大值后仍没有形成有效实际转矩。

该现象可概括为：

```text
上位机命令
  → E3000
  → EtherCAT
  → CST模式切换成功
  → 6071h目标转矩写入成功
  → 驱动器未形成对应的实际转矩/电流
```

## 7. 原工程与当前工程的主要差异

两套程序底层都使用：

- `nmc_torque_move`：启动转矩运动；
- `nmc_change_torque`：在线修改目标转矩；
- `nmc_get_torque`：读取实际转矩；
- `dmc_stop`：停止。

差异主要来自驱动器及上层控制：

| 项目 | 原工程 `0523_final` | 当前工程 |
|---|---|---|
| 控制卡 | E3000 系列 | E3000 |
| 驱动器 | Servotronix 等原工程配置 | ISMC Diamond |
| 转矩界面单位 | Nm，经硬编码额定转矩换算 | 额定转矩百分比 |
| 速度限制对象 | 厂商对象 `220Bh` | CiA402 `6080h` |
| 速度限制原始单位 | pulse/s | motor rpm |
| 上层目标来源 | 力传感器 PID、阻尼与斜率限幅 | 当前为单轴人工固定目标 |
| 方向切换 | 停止后自动重新启动 | 当前要求人工先停再反向 |

原工程的 PID、力传感器和自动方向切换不会解决当前静态目标转矩无法出力的问题。当前问题发生在
驱动器接收 `6071h` 之后。

## 8. 已基本排除的问题

根据现有证据，以下问题不是首要怀疑对象：

- E3000 完全不支持雷赛转矩 API；
- `nmc_torque_move` 根本没有执行；
- 驱动器没有进入 CST；
- 百分比与千分比换算错误；
- `6071h` 没有配置或目标转矩没有到达驱动器；
- `6080h=5370` 因数值过小而阻止零速起动；
- 单纯的电机、编码器或驱动器功率硬件损坏。

## 9. 尚未解决的问题

目前仍需确认：

1. E3000 的 EtherCAT 周期与 Diamond 伺服实际同步周期是否一致；
2. EtherCAT CST 下 `6072h` 最大转矩是否为有效非零值；
3. `6071h` 是否继续传递为 `6074h` 驱动器内部转矩需求；
4. `6074h` 非零时，`6078h` 实际电流是否建立；
5. `6087h` 转矩斜坡是否为合理非零配置；
6. Diamond 官方软件本地转矩模式是否设置了 EtherCAT CST 未设置的内部参数；
7. E3000 `nmc_torque_move` 生成的 `6040h` 控制字序列是否满足当前 Diamond 固件要求。

## 10. 下次恢复调试的最短路径

### 10.1 统一对照参数

先在 Diamond 官方软件中确认一组可以运动的低风险基准：

```text
目标转矩：50 rated‰（5%）
转矩斜坡：1000 rated‰/s
正向速度限制：20～50 rated velocity‰
负向速度限制：20～50 rated velocity‰
```

停止并失能后，再用 E3000 程序以相同的 `5%` 目标测试。不要使用 20% 或 100% 作为恢复调试的
第一条命令。

### 10.2 核对总线周期

确认 Diamond 驱动器侧的实际 EtherCAT 同步周期。若尚未明确改成 `1 ms`，先进行一次
主站和从站均为 `4 ms` 的对照试验，或者明确将伺服端改成 `1 ms` 后读回验证。

### 10.3 扩展只读诊断

下一版程序应在以下三个时刻记录一次驱动器对象快照：

- 调用 `nmc_torque_move` 前；
- 调用后约 `100 ms`；
- 调用后约 `1 s`。

建议读取：

| 对象 | 含义 |
|---|---|
| `6040h` | 控制字 |
| `6041h` | 状态字 |
| `6060h` | 请求运行模式 |
| `6061h` | 实际运行模式 |
| `6071h` | 目标转矩 |
| `6072h` | 最大转矩 |
| `6074h` | 转矩需求值 |
| `6077h` | 实际转矩 |
| `6078h` | 实际电流 |
| `6080h` | 最大电机速度 |
| `6087h` | 转矩斜坡 |

若 Diamond 对象字典支持，再读取正、负方向转矩限制对象，但不应在未确认对象定义前盲目写入。

### 10.4 结果判定

```text
6071非零，6074为零
→ 目标转矩被驱动器内部指令源、限幅或模式参数阻断

6074非零，6078为零
→ 控制字、功率使能或电流输出链没有建立

6078非零，6077非零，但电机不动
→ 检查抱闸、机械卡滞及反馈换算

6074、6078、6077均有效且电机运动
→ 再验证nmc_get_torque比例、方向、停止和在线更新
```

### 10.5 官方软件与 E3000 快照对比

如果条件允许，应保存 Diamond 官方软件能够运动时的对象值，再与 E3000 启动后的快照逐项比较。
两者之间第一个出现差异的对象，比继续猜测库函数内部行为更有诊断价值。

## 11. 安全要求

- 初次恢复测试使用 `5%` 或更低目标转矩；
- 硬件最大电机速度先限制在 `100～300 rpm`；
- 使用短运行超时、较小软件行程和实际速度保护；
- 保证全局立即停止和轴失能随时可用；
- 电机、减速器和绳索机构附近不得有人；
- 在确认停止逻辑前，不进行在线反向；
- 任何写 SDO 的操作都应在停止、失能且无其他运动任务时执行，并立即读回。

## 12. 恢复工作检查表

- [ ] 阅读本文和单轴转矩测试设计；
- [ ] 确认实际板卡为 E3000；
- [ ] 记录 LTDMC DLL 版本；
- [ ] 核对 E3000 与 Diamond EtherCAT 周期；
- [ ] 在官方软件中复现低风险转矩运动；
- [ ] 保存官方软件参数；
- [ ] 增加三时刻对象快照；
- [ ] 以 `5%` 目标执行 E3000 对照试验；
- [ ] 根据 `6071→6074→6078→6077` 链路定位断点；
- [ ] 确认断点后再决定是否修改写入参数或控制字序列。
