# 0523_final 安全逻辑总结与评估

## 1. 文档目的

本文档总结原工程 `0523_final` 中已经实现的安全逻辑，说明安全检测条件、停机等级、实际硬件动作、故障锁存与复位流程，并指出当前实现中仍需补充或验证的部分。

本文档基于当前源码静态检查得出，重点涉及：

- `safetymonitor.h/.cpp`
- `controlworker.h/.cpp`
- `hardwareinterface.h/.cpp`
- `mainwindow.h/.cpp`
- `singlemotortrajectoryworker.h/.cpp`
- `motortorquetestworker.h/.cpp`
- `pvtexecutionworker.h/.cpp`

需要明确：原工程的安全功能主要属于上位机软件安全层，不能替代实体急停、驱动器 STO、硬限位及符合安全等级要求的独立安全回路。

---

## 2. 总体安全架构

原工程采用分层安全结构：

```text
轨迹和指令下发前校验
        ↓
运行模式互锁与启动条件检查
        ↓
ControlWorker 控制周期内快速保护
        ↓
SafetyMonitor 独立线程复核
        ↓
Warning / ControlledStop / SafetyStop / EmergencyStop
        ↓
停止软件运动源并执行硬件停机
        ↓
故障锁存，人工复位后才允许再次运动
```

各层职责如下：

1. **MainWindow**
   - 负责启动条件检查、模式互锁、故障锁存、日志记录和停机流程编排。
2. **ControlWorker**
   - 在控制线程中处理位置、张力、速度和实际转矩等反馈。
   - 对必须快速响应的异常直接调用硬件急停。
3. **SafetyMonitor**
   - 运行在独立线程中。
   - 按控制周期检查通信、控制快照、主线程心跳、张力、工作空间、电机位置和速度。
4. **HardwareInterface**
   - 统一串行化访问控制卡。
   - 执行轴停止、全轴停止、轴失能和控制卡断连。
5. **各运动模式 Worker**
   - 在各自模式内执行参数校验、运行超时和反馈有效性检查。

---

## 3. 安全监控线程

### 3.1 线程结构

`SafetyMonitor` 是独立 `QObject`，被移动到单独的 `QThread` 中运行。

监控定时器采用 `Qt::PreciseTimer`，周期与控制周期一致，最小不低于 1 ms。

关键位置：

- `mainwindow.cpp:9157`：创建并启动安全监控线程
- `safetymonitor.cpp:59`：创建精确定时器
- `safetymonitor.cpp:463`：安全检测主循环

### 3.2 监控启用条件

安全监控总开关满足以下条件时工作：

- 已启用雷赛控制卡；
- 系统处于运行状态；
- `SafetyMonitor` 已经启动。

通信、主线程心跳和控制快照检查在系统运行期间持续执行。

张力、工作空间、电机位置和速度等动态保护，仅在检测到实际运动任务运行时执行。运动任务包括：

- PVT 位置轨迹；
- 力控线程；
- 零力拖动；
- 转矩模式调试；
- 单电机轨迹测试。

无运动任务时，安全线程会更新历史张力并清除持续故障计数，但不会触发动态运动保护。

---

## 4. 停机等级

原工程定义了四个安全等级。

| 等级 | 含义 | 软件动作 | 硬件动作 |
|---|---|---|---|
| `Warning` | 安全预警 | 记录日志、更新界面 | 不停机 |
| `ControlledStop` | 受控停机 | 停止运动源并锁存故障 | 一般逐轴停止；可控断绳走速度置零缓停 |
| `SafetyStop` | 安全停机 | 停止运动源并锁存故障 | 逐轴调用 `motorStop()` |
| `EmergencyStop` | 安全急停 | 清理全部运动状态并锁存故障 | 调用 `emergencyStopAll()`，全部轴停止并失能 |

关键位置：

- `safetymonitor.h:18`：停机等级定义
- `mainwindow.cpp:9782`：统一停机动作
- `mainwindow.cpp:9857`：统一故障处理

### 4.1 普通停止

普通受控停机和安全停机会：

1. 停止轨迹检查定时器；
2. 停止单轴轨迹、转矩调试、力控和零力拖动；
3. 清理仿真、PVT 和运行模式状态；
4. 对所有电机轴调用 `HardwareInterface::motorStop()`；
5. 保持故障锁存，禁止继续运动。

`motorStop()` 当前调用：

```cpp
dmc_stop(0, hardwareAxis, 0);
```

### 4.2 安全急停

安全急停调用：

```cpp
HardwareInterface::emergencyStopAll();
```

该函数对每个雷赛电机轴执行：

```cpp
dmc_stop(0, hardwareAxis, 0);
nmc_set_axis_disable(0, hardwareAxis);
```

同时清空活动 PVT 轨迹状态。

需要注意：当前实现没有调用库中独立存在的 `dmc_emg_stop()`，因此该“急停”仍属于软件命令停止并失能，不等价于独立硬件急停回路。

---

## 5. 故障检测逻辑

## 5.1 控制卡与通信

以下情况触发安全急停：

- `HardwareInterface` 不存在；
- 控制卡未连接；
- 配置状态显示硬件链路断开；
- `ControlWorker` 不存在；
- 控制快照停止更新。

当前控制快照超时阈值为：

```text
max(300 ms, 8 × 控制周期)
```

安全线程连续超过该时间没有收到新快照时，认为控制链路失效并触发急停。

关键位置：`safetymonitor.cpp:558`。

## 5.2 主线程软件看门狗

主界面周期性更新心跳时间戳，`SafetyMonitor` 独立检查该时间戳。

当前参数为：

```text
基础超时：max(1500 ms, UI快照周期 × 20)
宽限时间：max(250 ms, UI快照周期 × 5)
连续确认：3 个安全监控周期
```

主线程心跳持续超时时，安全线程会：

1. 绕过主线程直接调用 `emergencyStopAll()`；
2. 写入独立看门狗日志；
3. 触发 `SoftwareHang` 故障；
4. 将故障锁存为安全急停。

关键位置：`safetymonitor.cpp:481`。

## 5.3 绳索张力

### 5.3.1 张力下限

当前界面没有独立的逐绳安全张力下限，程序采用：

```text
张力下限 = 预紧力 × 0.5
```

张力连续低于下限约 250 ms 后，触发 `ControlledStop`。

### 5.3.2 张力上限

张力上限来自各轴配置。

- 张力持续超过上限约 250 ms：`SafetyStop`
- 张力一次超过上限的 1.15 倍：`EmergencyStop`
- 张力为 NaN 或无穷值：`EmergencyStop`

### 5.3.3 断崖式失张

疑似断绳判据为：

```text
前一帧张力 > 张力下限
当前张力 ≤ max(张力下限 × 0.2, 0.1)
前后帧下降量 ≥ 张力下限 × 0.65
```

满足条件后进入断绳可控性评估。

关键位置：`safetymonitor.cpp:738`。

## 5.4 断绳可控性评估

检测到疑似断绳后，程序会：

1. 找到断绳对应电机轴；
2. 从力雅可比矩阵中排除该轴；
3. 计算剩余绳索的力雅可比矩阵秩；
4. 与设定秩阈值比较。

当前秩阈值为 6。

### 可控

若剩余绳索系统秩不低于 6：

- 触发 `ControlledStop`；
- 通过控制盒速度置零链路缓停；
- 缓停期间持续重新计算力雅可比矩阵秩。

### 不可控

以下情况直接触发急停：

- 秩低于 6；
- 位姿反馈缺失；
- 力雅可比矩阵无法计算；
- 缓停过程中剩余系统失去可控性；
- 受控缓停链路执行失败。

关键位置：

- `safetymonitor.cpp:323`
- `mainwindow.cpp:9914`
- `mainwindow.cpp:10224`

## 5.5 电机位置

动态监控期间：

- 位置反馈为 NaN：急停；
- 位置达到或越过软件上下限：急停。

此外，`ControlWorker` 内还有一套独立位置检查。一旦发现位置无效或越界，会直接调用 `emergencyStopAll()`。

因此位置保护形成两层结构：

```text
ControlWorker 快速保护
        +
SafetyMonitor 独立复核
```

关键位置：

- `controlworker.cpp:785`
- `safetymonitor.cpp:837`

## 5.6 电机速度

- 速度反馈为 NaN：急停；
- 速度持续超过软件速度上限的 1.2 倍约 250 ms：安全停机。

速度超限采用持续周期计数，避免单次采样毛刺直接停机。

关键位置：`safetymonitor.cpp:861`。

## 5.7 实际转矩

`ControlWorker` 可以持续读取电机实际转矩。

实际转矩达到限值后会：

1. 将对应控制命令置零；
2. 直接调用 `emergencyStopAll()`；
3. 清除转矩命令状态；
4. 发出 `actualTorqueLimitExceeded` 信号；
5. 由主界面锁存 `MotorTorqueExceeded` 故障。

实际转矩保护默认并非在所有模式下无条件启用。在转矩 PID 力控模式运行时会自动参与，其余情况由界面配置决定。

关键位置：

- `controlworker.cpp:543`
- `mainwindow.cpp:7881`

## 5.8 工作空间

工作空间监控检查末端实际位置的 X、Y、Z 坐标。

| 情况 | 动作 |
|---|---|
| 接近工作空间边界 | Warning |
| 持续越界约 250 ms | SafetyStop |
| 越界量达到严重阈值 | EmergencyStop |
| 位姿反馈连续缺失约 300 ms | EmergencyStop |
| 位姿包含 NaN 或无穷值 | EmergencyStop |

工作空间范围来自当前模型和界面配置。

关键位置：`safetymonitor.cpp:622`。

---

## 6. 单绳力控调试模式的特殊处理

单绳力控调试模式会跳过：

- 末端工作空间检查；
- 低张力检查；
- 断崖式失张检查。

仍然保留：

- 高张力保护；
- 电机位置保护；
- 电机速度保护；
- 通信、快照和软件看门狗保护。

这样做便于单绳实验，但调试模式的安全保护低于完整系统模式。启动该模式时应当明确提示操作者，并限制实验力、速度和行程。

关键位置：`safetymonitor.cpp:718`。

---

## 7. 指令下发前的安全校验

## 7.1 运行模式互锁

程序使用统一运行状态判断以下任务是否正在执行：

- 位置轨迹；
- PVT 运动；
- 力控；
- 零力拖动；
- 转矩模式调试；
- 单电机轨迹测试。

存在运动任务时，禁止：

- 切换运行模式；
- 启动另一种运动任务；
- 修改部分运动源配置；
- 直接进入相互冲突的控制模式。

## 7.2 安全锁存检查

统一启动入口通过 `ensureSafetyReadyForMotion()` 检查：

- 是否处于软件抱闸保持状态；
- 是否存在未复位的安全故障。

任一条件不满足时拒绝启动。

关键位置：`mainwindow.cpp:9760`。

## 7.3 单轴命令限位

硬件接口层会在指令下发前检查：

- 绝对位置指令是否超出软件限位；
- 相对位置指令是否超出软件限位；
- 当前已到达上限时是否仍要求正向运动；
- 当前已到达下限时是否仍要求负向运动；
- 输入位置和速度是否为有限数值。

不满足条件的指令不会下发。

关键位置：`hardwareinterface.cpp:143`。

## 7.4 单电机轨迹校验

单电机轨迹生成完成后，会在下发前检查：

- 轨迹时长是否有效；
- 规划步长是否有效；
- 位置上下限是否有效；
- 轨迹点数量是否超过 PVT 上限；
- 每个位置和速度是否为有限数值；
- 每个轨迹点是否在软件位置限位内；
- 每个轨迹点速度是否超过速度上限；
- 相邻时间戳是否严格递增；
- 每段平均速度是否超过速度上限。

运行时还检查：

- 控制卡是否断开；
- 轨迹数据是否失效；
- Trace 是否长时间没有返回；
- 轨迹结束后控制卡是否确认运动完成。

关键位置：`singlemotortrajectoryworker.cpp:328`。

## 7.5 末端轨迹工作空间校验

末端轨迹规划完成后，程序逐点检查轨迹的 X、Y、Z 坐标。

任意轨迹点越界都会给出：

- 轨迹点序号；
- 对应时间；
- 实际位姿；
- 安全工作空间范围。

存在越界点时拒绝进入执行链路。

关键位置：`mainwindow.cpp:8410`。

## 7.6 绳力可行性校验

程序利用动力学和绳力分配算法检查整条轨迹是否存在满足上下限的绳力解。

主要检查：

- 绳索数量与模型配置是否一致；
- 锚点和接点数量、维度是否有效；
- 动力学参数是否已初始化；
- 最小和最大绳力范围是否有效；
- 每个轨迹点是否有可行绳力解。

若某个时刻无可行解，会输出失败轨迹点、时间和对应位姿。

完整位控模式下一般拒绝继续执行；特定“6绳位控 + 2绳恒张力保持”模式允许以警告方式继续仿真。

关键位置：`mainwindow.cpp:10932`。

---

## 8. 使能和失能逻辑

## 8.1 轴使能

使能电机轴前会：

1. 读取总线错误码；
2. 尝试清除总线错误；
3. 读取轴错误码；
4. 尝试清除轴错误；
5. 调用 `nmc_set_axis_enable()`；
6. 读取驱动状态机；
7. 只有状态机进入状态 4 才确认使能成功。

程序最多尝试 20 次，每次间隔 50 ms。

关键位置：`hardwareinterface.cpp:733`。

## 8.2 软件抱闸

“软件抱闸”不是真实机械抱闸，实际流程为：

1. 若电机正在运动，先执行普通停止；
2. 最长等待 5 s，确认全部电机停稳；
3. 如果未停稳，拒绝失能；
4. 停稳后失能全部电机轴；
5. 再次点击时重新使能全部轴。

该设计避免在电机仍运动时直接失能导致平台失控。

关键位置：`mainwindow.cpp:11334`。

---

## 9. 故障锁存与复位

除 `Warning` 外，所有故障都会锁存。

锁存后：

- 保存故障等级、代码、摘要和详细信息；
- 禁止新的运动任务启动；
- 界面显示安全停机或安全急停状态；
- 重复的同等级同类故障不会反复执行停机；
- 断绳受控停机期间允许更高级故障继续升级。

复位条件：

1. 当前没有运动任务；
2. 电机已经停稳；
3. 控制卡连接正常时，必须成功清除所有轴错误码；
4. 急停锁存后，系统运行状态恢复为未启动；
5. 清除安全线程和主界面的故障锁存。

复位不会自动重新使能或重新启动运动。

关键位置：

- `safetymonitor.cpp:158`
- `mainwindow.cpp:9980`

---

## 10. 软件异常与退出保护

## 10.1 软件崩溃保护

原工程安装了：

- Windows 未处理异常过滤器；
- `std::terminate` 处理器；
- `SIGABRT`；
- `SIGFPE`；
- `SIGILL`；
- `SIGSEGV`。

捕获到致命异常后，会尽力：

1. 调用 `emergencyStopAll()`；
2. 写入独立故障日志；
3. 终止进程。

该逻辑只能作为崩溃后的尽力补救。异常处理阶段程序状态可能已经损坏，因此不能保证停机调用一定执行成功。

关键位置：`mainwindow.cpp:2758`。

## 10.2 正常退出

主窗口析构时会：

1. 停止轨迹检查、快照和心跳定时器；
2. 停止单轴轨迹与转矩测试线程；
3. 停止安全监控、UDP、控制和位置线程；
4. 调用 `disconnectLS()`；
5. `disconnectLS()` 失能全部电机轴；
6. 关闭控制卡。

关键位置：

- `mainwindow.cpp:801`
- `hardwareinterface.cpp:712`

---

## 11. 已发现的问题和风险

## 11.1 急停仍是软件急停

`emergencyStopAll()` 没有调用库中独立的 `dmc_emg_stop()`，而是逐轴调用：

```cpp
dmc_stop(..., 0);
nmc_set_axis_disable(...);
```

需要结合所用雷赛库版本和手册确认：

- `dmc_stop(..., 0)` 的准确停止方式；
- 停止后立即失能是否会破坏期望的减速过程；
- 是否应使用 `dmc_emg_stop()`；
- 是否应由独立安全回路切断驱动使能。

## 11.2 转矩测试局部限位没有真正生效

`MotorTorqueTestWorker::Config` 中包含：

- `relativeMinPos`
- `relativeMaxPos`
- `velocityLimit`

界面也会把这些参数传给工作线程，但当前 `loop()` 中没有使用它们进行位置或速度越限判断。

因此，转矩调试页显示的局部位置/速度限制目前不是有效的停机保护。

在公共 `SafetyMonitor` 正常运行时，仍可能由公共轴位置和速度保护停机；但这不能替代转矩测试自身的局部保护。

## 11.3 部分测试入口未直接调用统一安全检查

统一的 `ensureSafetyReadyForMotion()` 目前主要用于：

- 主运行模式启动；
- 位控轨迹下发；
- 零力拖动/阻抗调试。

单电机轨迹和转矩测试启动入口主要依赖：

- 控制卡连接检查；
- 轴使能检查；
- 已有运动任务互锁；
- 界面按钮状态。

建议所有产生硬件运动的入口都显式调用统一安全检查，避免仅依赖 UI 禁用状态。

## 11.4 断绳判断存在重复代码

`SafetyMonitor` 中存在两段近似相同的断崖式失张判断。

第一段调用 `handleCableBreakFault()` 后立即返回，因此后一段通常不可达，不会造成同一周期重复停机，但属于冗余逻辑，后续维护时容易引入不一致。

## 11.5 张力下限配置不够细

当前统一采用“预紧力的一半”作为全部绳索的张力下限。

不同绳索的几何位置、额定载荷、传感器噪声和最低可控张力可能不同，后续应支持逐绳配置：

- 预警下限；
- 停机下限；
- 严重失张阈值；
- 持续确认时间；
- 断崖下降比例。

## 11.6 软件保护不具备安全认证能力

现有保护依赖：

- Windows 调度；
- Qt 定时器；
- 上位机进程；
- 控制卡通信；
- Trace 和控制快照；
- 软件配置正确性。

因此必须保留独立于软件的：

- 实体急停；
- STO；
- 驱动器过流、过速和跟随误差保护；
- 正负硬限位；
- 必要的机械限位或缓冲机构；
- 断电后平台防坠落设计。

---

## 12. 向当前控制程序迁移时的建议

建议迁移时保留以下核心结构：

1. **独立 SafetyMonitor 线程**
   - 不与 UI、绘图、日志或控制算法共用线程。
2. **统一运动状态**
   - 所有单轴、多轴、速度模式和转矩模式都必须注册为活动运动源。
3. **统一启动安全检查**
   - 每个可能导致硬件运动的入口都调用同一检查函数。
4. **统一停机编排**
   - 先停软件运动源，再执行控制卡停止，最后按等级决定是否失能。
5. **控制线程快速保护**
   - 位置、速度、转矩和反馈失效等关键故障应允许控制线程直接停机。
6. **故障锁存**
   - 停机后不得因按钮重复点击或任务重新启动而自动恢复。
7. **独立看门狗**
   - UI线程卡死时，安全线程仍能直接请求硬件停机。
8. **原始安全数据记录**
   - 保存触发前后的时间戳、位置、速度、张力、转矩、驱动状态和API返回码。
9. **逐轴参数**
   - 位置、速度、张力和实际转矩阈值均采用逐轴配置。
10. **硬件安全链路**
    - 软件急停只能作为补充，不能替代实体急停和 STO。

### 12.1 8轴力交互速度闭环的参数归属

后续 8 绳 6 自由度力交互控制不是一条具有固定终点的离线轨迹，而是按控制周期执行以下闭环：

```text
六维力传感器/模拟外力
        ↓
纯惯性 Newmark-β 动力学
        ↓
下一周期末端期望位姿、速度和加速度
        ↓
逆运动学与绳速映射
        ↓
8轴速度前馈 + 位置误差 PID
        ↓
Trace 实际位置反馈与正运动学
```

因此，8轴运行页面不应简单保留当前单轴测试页的全部参数，也不应只保留 PID 参数而丢失执行限制和安全阈值。建议按职责划分如下。

#### 页面保留的闭环调试参数

- 速度前馈开关和前馈系数；
- PID 开关；
- `Kp`、`Ki`、`Kd`；
- 积分状态上限；
- PID 速度修正上限；
- 必要时提供“统一参数”和“逐轴覆盖”两级配置。

这些参数决定控制器行为，适合保留在 8轴速度闭环页面中。

#### 转为全局执行参数

- 控制周期；
- 各轴速度上限；
- 各轴加速度/速度变化率上限；
- 在线变速时间；
- 启动速度阈值；
- 板卡脉冲当量；
- Trace 采样周期和反馈延迟标定结果。

这些参数不是某一条测试轨迹专属参数，应由全局硬件与执行配置统一管理。8轴控制器对速度、加速度进行逐轴限幅后，还应采用公共缩放系数同步缩放全部轴命令，避免某一轴单独饱和破坏多轴协同关系。

#### 仅属于单轴有限轨迹测试的参数

- 轨迹类型；
- 测试轴；
- 相对位移；
- 轨迹时间；
- 位置容差；
- 速度容差；
- 稳态确认时间；
- 终点超时。

上述参数可继续保留在单轴验证页面，但不应作为连续力交互模式的运行终止条件。连续力交互模式没有天然轨迹终点，其停止来源应是操作者停机、模式退出或安全保护。

#### 必须保留并加强的安全参数

- 逐轴最大轨迹跟踪误差；
- 逐轴位置、速度和实际转矩限制；
- Trace 超时、丢帧和帧序号连续性；
- 六维力传感器超时、量程和跳变阈值；
- 末端工作空间与姿态范围；
- 各绳张力上限、下限和突降阈值；第一阶段暂以10 N为运行防松绳下限、
  500 N为待实物确认的算例上限；
- 安全监控、控制线程和硬件线程看门狗阈值。

最大轨迹跟踪误差应采用延迟对齐后的：

```text
期望绳长（或等效轴位置） - Trace 实际位置
```

而不是仅使用同一帧中的板卡指令位置与实际位置之差。前者衡量整套“动力学产点—逆运动学—速度闭环”控制策略的跟踪效果；后者更适合作为驱动侧位置跟随性能诊断。

### 12.2 8轴力交互模式的最低安全闭环

与原工程相比，当前程序已有的单轴位置容差、速度容差、最大跟随误差和 Trace 超时，仅构成局部测试保护，尚不足以支撑 8轴 CDPR 力交互运行。正式接入前至少应补齐以下链路：

1. **独立安全线程**
   - `SafetyMonitor` 独占线程，不执行绘图、文件写入、UI 更新或动力学计算。
2. **8轴同步反馈检查**
   - 检查每轴 Trace 新鲜度、帧号连续性和轴间时间对齐；任一关键轴反馈失效时不得继续使用旧值控制。
3. **控制算法有效性检查**
   - 检查 Newmark-β 求解结果、逆运动学结果、正运动学结果以及全部速度命令是否为有限数；
   - 检查求解是否收敛、位姿是否在工作空间内、绳长是否可实现。
4. **逐轴快速保护**
   - 在控制线程内检查位置、速度、实际转矩和延迟对齐轨迹跟踪误差；
   - 触发严重异常时允许绕过 UI 直接请求硬件停机。
5. **绳索与力传感器保护**
   - 检查张力过高、失张、突降和疑似断绳；
   - 检查六维力传感器通信超时、饱和、零漂异常和不合理跳变。
6. **故障分级与锁存**
   - 明确预警、受控停机、安全停机和急停；
   - 故障触发后锁存，只有在运动停止、反馈恢复且人工确认后才允许复位。
7. **统一停止顺序**
   - 先冻结新的动力学产点和控制命令；
   - 将8轴速度指令同步降为零；
   - 超时未停稳时升级为立即停止；
   - 最后根据故障等级决定是否失能。
8. **独立硬件安全**
   - 保留实体急停、STO、驱动器保护、硬限位及平台防坠落措施，不依赖 Windows 或 Qt 调度。

纯惯性微重力模拟尤其需要注意：模型不包含回中刚度、虚拟阻尼和重力项，外力撤除后末端会保持已有线速度和角速度继续漂移。因此“外力回零”不能作为安全停止条件，必须由工作空间、速度、绳长、张力、操作者停机和看门狗共同约束。

### 12.3 G302数学模块迁移后的安全约定

本节描述当前目标软件，不改变第5章对原工程既有行为的事实记录。

1. **启动预紧与运行张力分开**
   - 预紧只作为启动前建立可运行构型的工序；
   - 运行期间不叠加预紧控制目标；
   - 运行张力分配暂采用 `10～500 N` 可行域，其中10 N是防松绳下限，
     不是运行预紧指令，500 N仍需按实物额定值确认。
2. **张力分配只承担真实重力**
   - 运行张力可行性采用真实动平台、传感器、工装和负载的总质量；
   - 平衡外力为 `[0,0,-m_pg]`，质心力矩为零；
   - 交互力只用于Newmark生成虚拟期望运动，不在张力分配中重复叠加。
3. **干涉保护**
   - 每周期检查全部28对绳索的有限线段最短距离；
   - 使用上一周期和当前周期几何关系辅助识别动态穿越；
   - 第一版使用有限圆柱体近似动平台，检查全部8根绳索；
   - 圆柱尺寸、绳径和安全间隙必须配置化。
4. **数学链路必须整帧有效**
   - Newmark、逆运动学、绳长/绳速、干涉、张力可行性和8轴命令必须全部有效；
   - 任一环节失败时不得发送部分轴或半有效命令；
   - 冻结新动力学状态并按故障等级请求保护停止。
5. **方向未标定不得运行**
   - 几何绳长统一定义为伸长为正；
   - 电机正转对应收绳还是放绳必须逐轴通过实物确认；
   - 轴映射、方向、零位和卷筒半径未确认时禁止进入8轴运行状态。

---

## 13. 结论

原工程已经形成较完整的软件安全框架，其优点主要是：

- 安全监控与 UI、控制逻辑分线程；
- 具有通信、心跳和控制快照超时保护；
- 位置保护同时存在于控制线程和独立安全线程；
- 对断绳进行了剩余系统可控性评估；
- 建立了故障分级、升级、锁存和人工复位流程；
- 轨迹下发前包含位置、速度、工作空间和绳力可行性校验；
- 程序崩溃时会尽力执行软件急停。

当前最需要补充和验证的部分是：

1. 明确 `dmc_stop(..., 0)`、轴失能和 `dmc_emg_stop()` 的实际停止差异；
2. 修复转矩测试局部位置/速度限位未生效的问题；
3. 让全部硬件运动入口统一检查安全锁存；
4. 清理重复的断绳判断；
5. 将统一张力下限改为逐绳安全阈值；
6. 为 8轴力交互模式补齐同步 Trace、Newmark/运动学有效性、力传感器和多绳状态保护；
7. 建立独立实体急停、STO 和硬限位安全链路。

因此，原工程的安全逻辑适合作为当前程序的软件安全架构参考，但不能未经验证直接视为完整的设备安全系统。
