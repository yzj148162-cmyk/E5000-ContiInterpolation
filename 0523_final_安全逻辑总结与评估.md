# MotionControl（原 0523_final）安全逻辑源码审计与迁移评估

> 文件名暂时保留为 `0523_final_安全逻辑总结与评估.md`，以免已有引用失效；
> 本文审计对象已更新为根目录中的完整原工程 `MotionControl/`。
>
> 审计日期：2026-08-13。
>
> 按当前项目范围，原工程的控制盒、PLC 串口协议和按钮联锁不作为迁移目标，
> 本文仅在范围说明中保留这一事实，不展开其实现。

---

## 1. 文档结论

原工程已经形成了较完整的软件安全链，最值得迁移的不是某一段判断代码，
而是以下结构：

1. 运动前统一自检和模式互锁；
2. 控制周期内的快速保护；
3. 独立安全监控线程的交叉复核和软件看门狗；
4. 故障分级、首故障锁存、后续故障只记录；
5. 所有运动源统一进入同一停机编排；
6. 停机动作失败也必须记录，不能只修改 UI 状态；
7. 结构化保存故障发生时的状态快照。

但原工程不能原样搬入当前 `cdpr_control`：

- 原工程安全状态和模式清理由 `MainWindow` 大量集中编排，耦合很重；
- 原工程工作空间监控使用的是活动规划点或规划末点，不是真实末端位姿；
- 张力下限统一取预紧力的一半，不符合当前逐绳 10 N 下限设计；
- 原工程的“软件急停”本质是控制卡命令停止和轴失能，不等于 STO；
- 原工程的安全监控线程可直接发起硬件急停请求，当前工程必须继续遵守
  `MotionControlWorker` 独占控制卡的线程边界；
- 当前完整版本已经取消旧文档所述的“断绳后判断剩余系统秩并受控停机”，
  实际实现是断绳直接急停。

因此，迁移原则是：**迁移安全状态机和判据，不迁移原工程的 UI 耦合和共享状态组织方式。**

---

## 2. 审计范围与关键文件

| 职责 | 原工程位置 |
|---|---|
| 独立安全判据、看门狗、故障锁存 | `MotionControl/embedded_module/safetymonitor.*` |
| 控制卡停止、失能、连接诊断 | `MotionControl/embedded_module/hardwareinterface.*` |
| 控制周期快速保护 | `MotionControl/pose_control_module/controlworker.*` |
| 启动自检、模式清理、故障升级与复位 | `MotionControl/control_interface_module/mainwindow.*` |
| PVT、单电机、转矩等局部保护 | 各运动 Worker |

本文以函数调用关系为依据，不再使用旧文档中的历史行号。代码更新后应优先
按函数名检索。

明确排除：控制盒按钮状态、PLC 串口帧、按钮未复位锁存及其回点逻辑。

---

## 3. 原工程真实安全架构

### 3.1 三层保护

```text
运动入口
  └─ 启动自检、故障锁存、模式互锁、轨迹预检
       ↓
ControlWorker / 各运动 Worker
  └─ 每周期位置、速度、转矩、传感器和命令快速检查
       ↓
SafetyMonitor 独立线程
  └─ 心跳、总线、快照、轴使能、张力、位置、速度、工作空间复核
       ↓
MainWindow 故障编排
  └─ 冻结全部运动源、执行停机、锁存首故障、记录后续故障
       ↓
HardwareInterface
  └─ 在硬件访问线程中串行执行控制卡 API
```

这三层不是重复浪费：控制线程负责低延迟保护，SafetyMonitor 负责控制线程或
UI 卡住时的独立复核，统一故障编排负责清除所有潜在命令源。

### 3.2 SafetyMonitor 的线程性质

`SafetyMonitor` 运行在独立 `QThread`，使用精确定时器，并以较高线程优先级
启动。它读取 `ControlWorker::Snapshot`，维护自己的连续故障计数和锁存状态。

主线程心跳超时时，SafetyMonitor 不等待 UI 响应，而是直接向
`HardwareInterface::emergencyStopAll()` 发出请求；硬件接口再把实际控制卡调用
串行化到硬件线程。这一点比“发信号给 UI，再由 UI 停机”更可靠。

### 3.3 监控启用条件

安全评估在雷赛控制启用且系统处于运行态时生效。控制快照和总线链路在系统
运行期间持续检查；张力、位置、速度和参与轴使能等运动判据在 `motionActive`
后执行。

`motionActive` 不是简单的“主程序运行”标志，当前包含 PVT、受保护 PVT、
力控、转矩调试、单轴点位和直线模组运动等来源。这个“所有运动源必须注册”
的思想应迁移到当前工程。

---

## 4. 停机等级与实际硬件动作

原工程定义四级语义：

| 等级 | 用途 | 当前实际硬件动作 |
|---|---|---|
| Warning | 预警，不锁存停机 | 不停止 |
| ControlledStop | 可缓停故障 | 全部电机 `motorStop()` |
| SafetyStop | 持续越界等安全故障 | 全部电机 `motorStop()` |
| EmergencyStop | 严重故障/断绳/通信故障 | `emergencyStopAll()` |

需要特别澄清：当前 `ControlledStop` 与 `SafetyStop` 的硬件动作实际相同，
主要区别是故障语义，而不是两套不同减速曲线。

`motorStop()` 调用：

```cpp
dmc_stop(card, axis, 0);
```

`emergencyStopAll()` 对每个雷赛轴执行：

```cpp
dmc_stop(card, axis, 0);
nmc_set_axis_disable(card, axis);
```

因此原工程所谓 EmergencyStop 是“软件命令停止后失能”。它没有构成独立于
Windows、进程、通信和控制卡的安全功能，也没有替代实体急停、STO、驱动器
保护或机械防坠落装置。

此外，停止后立刻失能是否允许减速过程真正完成，必须在当前板卡、驱动器和
负载条件下实测，不能只依据函数名判断。

---

## 5. 原工程故障判据审计

### 5.1 软件心跳、控制快照和 EtherCAT

| 判据 | 当前行为 |
|---|---|
| 主线程心跳超时 | 独立安全线程直接请求急停并写独立日志 |
| HardwareInterface 缺失或软件连接标志断开 | 急停 |
| EtherCAT 主站诊断非 Connected | 急停 |
| ControlWorker 未绑定 | 急停 |
| 控制快照序号长期不更新 | 急停 |

重要更新：`isLSConnected()` 只表示软件曾经打开控制卡，原工程现在还会约每
100 ms 读取控制器/主站诊断，避免拔掉 EtherCAT 后软件连接标志仍为真的假在线。

软件看门狗当前最低参数约为：

```text
心跳超时阈值：max(4000 ms, UI刷新周期 × 20)
附加宽限：max(1000 ms, UI刷新周期 × 5)
连续确认：3个安全监控周期
```

因此通常需超过约 5 s 才触发，并非旧文档所写的约 1.75 s。

基础控制快照超时为：

```text
max(300 ms, 8 × 控制周期)
```

PVT 启动、程序控制准备或硬件独占长调用期间会临时放宽，硬件独占窗口约在
3～15 s 内。该机制能减少大批量 PVT 上传造成的误停，但也会扩大真正卡死时
的检测延迟。迁移时应让硬件线程在长操作期间继续发布轻量心跳，而不是简单
把整个安全超时放大。

### 5.2 运动参与轴状态

运动开始后，SafetyMonitor 只检查 `monitored && motionParticipant` 的轴。
任一参与轴使能状态缺失或变为失能，立即按电机故障急停。这一项旧文档没有
完整记录，但对 8 绳 CDPR 很重要：运行中不能只让剩余轴继续执行旧命令。

### 5.3 张力与断绳

当前张力下限仍统一采用：

```text
forceMin = 预紧力 × 0.5
```

保护逻辑为：

| 情况 | 动作 |
|---|---|
| 张力非有限数 | EmergencyStop |
| 张力超过上限 1.15 倍 | EmergencyStop |
| 张力持续超过上限约 250 ms | SafetyStop |
| 张力持续低于下限约 250 ms | ControlledStop |
| 疑似断崖式失张 | EmergencyStop |

断崖式失张判据为：

```text
上一帧 > forceMin
当前帧 ≤ max(forceMin × 0.2, 0.1)
下降量 ≥ forceMin × 0.65
```

完整 MotionControl 中，疑似断绳已经直接触发 EmergencyStop；MainWindow 还会
把任何低于急停级别的 CableBreak 再升级为急停。旧文档中的“排除断绳轴后
计算力雅可比秩，若仍可控则受控停机”属于被取消的历史策略，不再代表现代码。

源码中仍有两段近似重复的断崖式失张判断，应视为维护缺陷，不应迁移。

### 5.4 电机位置、速度和实际转矩

位置保护有两层：

- ControlWorker 在 Trace 采样后快速检查，越界会直接请求全轴急停；
- SafetyMonitor 独立复核，位置无效或越界触发急停。

原工程还提供“位置超限恢复状态”：识别为可恢复的超限轴后，禁止普通轨迹，
只允许有限的恢复模式把轴移回安全区。这是有价值的状态机思想，但恢复命令
必须额外检查“只允许朝限位内运动”，不能简单放开所有限位。

速度保护为：反馈无效时急停；持续超过 `1.2 × motorVelMax` 约 250 ms 后
SafetyStop。它会抑制单帧毛刺，但 20% 超限裕量及 250 ms 持续时间必须依据
当前样机动能重新评估。

实际转矩达到软件限值并持续满足条件时，ControlWorker 会把命令置零、直接
调用全轴急停并上报 `MotorTorqueExceeded`。转矩 PID 内还有位置、速度、张力、
反馈超时等局部锁存保护，它们属于控制器内部快速保护，不应替代全局故障链。

### 5.5 工作空间

旧文档将其写成“末端实际位姿保护”，这是错误的。现代码明确只使用：

1. 当前活动轨迹显示点；或
2. 最近一次规划轨迹末点。

它不使用 Nokov 实际位姿，也不使用正运动学实际位姿。对应行为为：

| 情况 | 动作 |
|---|---|
| 接近边界 | Warning |
| 持续越界约 250 ms | SafetyStop |
| 严重越界 | EmergencyStop |
| 规划位姿缺失约 300 ms | EmergencyStop |
| 规划位姿非有限数 | EmergencyStop |

所以它本质上是“运行期规划命令边界复核”，不能证明真实平台未越界。当前工程
应把规划位姿用于命令预检，把 Nokov 或正运动学实际位姿用于运行期硬保护，
两者不可混为一项。

### 5.6 单绳调试降级

单绳力控调试会跳过工作空间、低张力和断绳检查，但仍保留高张力、位置、
速度、通信、快照和软件看门狗保护。迁移时任何降级调试模式都必须显式显示
被关闭的保护项，并限制速度、转矩和行程。

---

## 6. 运动前检查、锁存与复位

### 6.1 统一启动检查

主运行模式、PVT 下发、JOG 跟随、位置恢复和部分辅助运动会调用
`ensureSafetyReadyForMotion()`，其核心包含：

- 重新执行启动自检；
- 拒绝未清除的故障锁存；
- 拒绝软件保持/冲突运动状态；
- 在位置超限恢复期间禁止普通轨迹；
- 检查硬件连接、线程、快照和必要配置；
- 再由具体模式执行轴数、使能、限位、轨迹和工作空间预检。

启动自检覆盖 UI/入口、电机配置与限位、周期参数、硬件开关、控制线程、
PVT 线程、安全线程、主线程心跳、控制快照、执行机构通信和安全锁存等。

原工程仍有少数调试入口主要依赖自己的局部连接、使能和参数检查，没有完整
走统一入口。迁移后应规定：**任何能产生硬件运动的 public slot 都不得绕过
统一启动门禁。**

### 6.2 首故障锁存

原工程的故障处理具有以下行为：

- Warning 不锁存停机；
- 第一个非 Warning 故障成为主故障并锁存；
- 相同故障重复出现只写日志，不重复覆盖 UI；
- 后续不同故障不覆盖首故障原因；
- 后续故障等级更高时允许升级停机动作；
- 硬件停机 API 返回失败会独立记录。

首故障保留非常重要，否则“Trace 超时导致急停”之后产生的“轴失能”可能覆盖
真正根因，导致现场只能看到派生故障。

### 6.3 复位

排除控制盒部分后，原工程复位的有效原则是：

1. 当前必须存在锁存故障；
2. 所有运动任务已经停止；
3. 尝试清除各轴驱动错误码；
4. 急停级故障会使系统运行状态回到未启动；
5. 清锁存后不自动使能、不自动恢复运动；
6. 下一次启动仍须重新通过全部自检。

---

## 7. 与旧版 0523_final 文档的主要差异

| 旧文档表述 | MotionControl 现代码 | 修正结论 |
|---|---|---|
| 断绳后计算剩余力雅可比秩，可能受控停机 | 断绳直接 EmergencyStop，MainWindow 再强制升级 | 删除可控性恢复描述 |
| 工作空间检查真实末端位置 | 只使用活动规划点或规划末点 | 只能算命令边界复核 |
| 看门狗最低约 1.75 s | 当前最低约 5 s，另需连续周期确认 | 更新参数并提示检测偏慢 |
| 连接检查主要依赖软件连接标志 | 新增 EtherCAT 主站状态周期诊断 | 迁移真实总线状态检查 |
| 未突出参与轴失能 | 参与轴状态缺失/失能直接急停 | 纳入 8 轴整帧保护 |
| ControlledStop 与 SafetyStop 是不同硬件停机过程 | 当前均调用逐轴 `motorStop()` | 先区分语义，后续再实现不同动作 |
| 原工程具有完整实际工作空间保护 | 并不具备 | 当前工程不能据此宣称已实现 |

---

## 8. 当前 cdpr_control 已具备与缺失的安全能力

### 8.1 已具备

- `MotionControlWorker` 独占控制卡和 LTDMC 调用；
- 初始化时读取控制卡工作模式、EtherCAT 主站状态和在线轴数；
- 各测试模式基本具有互斥启动条件；
- 单轴和 8 轴速度闭环检查 Trace、轴状态机、错误码和 API 返回码；
- 8 轴速度闭环使用延迟对齐后的规划位置与 Trace 实际位置进行跟随误差保护；
- 8 轴 Trace 必须同帧，时间轴不可信或超时会整组停止；
- PVT 和 8 轴运行保存期望、反馈、事件与周期耗时；
- 窗口关闭/关闭控制卡时会先减速停止，超时再立即停止，随后失能并关卡；
- JSON 已配置 160 mm 绞盘和相对启动位置双向 6.5 圈的几何限制；
- `CdprRobotState` 已预留 `safetyFlags` 和 `safetyLatched`。

### 8.2 尚未形成完整全局安全链

- 没有已实现的 `CdprSafetySupervisor`；
- 没有统一的全局故障码、首故障锁存和人工复位流程；
- 全局急停按钮当前只发 `stopTestRequested(true)`，而 `stopTest()` 会按当前分支
  停止一个已识别模式，不是经验证的“无论状态如何扫描并停掉全部运动源”；
- `enterError()` 未完整处理 `cdprVelocityControlActive_`，而且部分状态清理分散在
  各 finish 函数中；需要统一、幂等的全模式停机入口；
- 安全关机逻辑尚未明确停止 `cdprVelocityControlTimer_` 和 8 轴闭环分支，应专项
  审计后统一；
- 绞盘 ±6.5 圈目前只有配置、校验和计算函数，尚未接入 8 轴实机周期保护；
- PVT 由板卡自主执行时，Trace 异常主要影响记录/分析，尚不能实时中止 PVT；
- 没有独立于 UI/控制周期的软件看门狗；
- 没有逐轴运行时位置、速度、实际转矩的统一复核层；
- 尚未接入真实张力、断绳、真实末端工作空间和姿态边界；
- 没有硬件 STO 状态反馈及实体急停链验收。

因此，当前程序适合继续做 8 台空载电机的小范围实验，但不能把现有保护描述
为完整 CDPR 实机安全系统。

---

## 9. 适合当前工程的迁移方案

### 9.1 模块边界

建议实现：

```text
CdprSafetySupervisor
  ├─ 接收不可变 CdprRobotState / HardwareSnapshot
  ├─ 计算 SafetyDecision（级别、故障码、轴、数值、阈值、时间）
  ├─ 锁存首故障，记录后续故障
  └─ 只请求停止，不直接调用 LTDMC

MotionControlWorker
  ├─ 唯一硬件执行者
  ├─ 每周期快速保护
  ├─ executeSafetyStop(decision)
  └─ 发布停机结果和最新硬件快照

CdprCoordinator
  ├─ 唯一机器人状态拥有者
  ├─ 统一运动模式状态机
  └─ 负责准备、运行、停止、故障锁存、复位转换
```

SafetySupervisor 可以独立线程运行，但不得绕过 `MotionControlWorker` 直接调用
雷赛库。急停请求应使用高优先级、可重入且幂等的硬件线程入口；同一故障重复
请求不能造成多次相互冲突的停止/失能调用。

### 9.2 第一阶段：8 台空载电机即可实施

第一阶段不依赖绳索、Nokov 或真实 F/T，优先补齐：

1. 统一 `MotionMode`，所有单轴、PVT、8 轴速度闭环、转矩和标定任务注册；
2. 统一 `SafetyFault` 和首故障锁存；
3. 统一全模式停机入口，扫描并冻结所有运动源和定时器；
4. 区分正常停止、受控安全停止和立即停止，并确认每轴停止结果；
5. 运动前检查真实 EtherCAT 主站、参与轴在线/使能/无错误、Trace 配置和锁存；
6. 8 轴周期检查同帧 Trace、反馈新鲜度、轴状态、命令有限性、速度、加速度、
   跟随误差、API 失败和调度连续超时；
7. 将 ±6.5 圈限制接入 8 轴速度闭环周期：以本次进入 Ready 时的八轴实际位置
   为基准，任一轴越界则整帧作废并整组停机；
8. PVT 执行期间增加轻量安全监视，不把 Trace 异常仅当作记录失效；
9. 增加控制/硬件线程心跳，UI 卡死不能阻止停机请求；
10. 故障记录至少包含主机时刻、Trace 序号、模式、8 轴位置/速度/错误码、
    当前命令、阈值、API 返回码和停机确认结果。

对单轴页面，仅检查实际参与轴；对完整 CDPR 模式必须要求 8 轴全部就绪，避免
“为了兼容单轴测试而放松 8 轴模式”的混合判断。

### 9.3 第二阶段：接入绞盘、绳索和平台后

再启用：

- 逐绳张力下限 10 N、逐绳上限和突降阈值；
- 断绳直接进入最高软件停机等级，暂不实现原工程已取消的可控性恢复；
- 规划工作空间预检；
- Nokov 或正运动学实际位姿工作空间/姿态保护；
- 正运动学收敛、残差和几何干涉检查；
- 绳长、绳速、绳加速度和张力分配可行性；
- F/T 数据新鲜度、非有限数、量程、跳变、零偏未就绪；
- Newmark 状态、逆运动学和 8 轴命令必须整帧有效，禁止部分轴下发。

规划边界与实际边界应分别记录：规划越界可在下发前拒绝；实际越界属于运行期
故障。不能照搬原工程只看规划点的做法。

### 9.4 第三阶段：硬件安全

软件功能完成后仍需独立验证：

- 实体急停和 STO；
- 驱动器过流、过速、跟随误差和编码器故障；
- 正负硬限位；
- 断电或失能时平台防坠落/机械制动；
- 安全回路状态反馈进入软件，但软件无权覆盖硬件保护。

---

## 10. 建议故障分级

| 故障 | 建议等级 | 说明 |
|---|---|---|
| 接近工作空间/行程边界 | Warning | 只提示，不锁存 |
| 单次调度抖动 | Warning/计数 | 不因 Windows 单次尖峰立即停机 |
| 持续跟随误差、持续超速、低张力 | Controlled/SafetyStop | 冻结新命令，8轴同步减速 |
| Trace 超时/时间轴失效 | EmergencyStop | 不能继续使用旧反馈闭环 |
| 参与轴失能、轴错误、API 失败 | EmergencyStop | 8轴协同关系已破坏 |
| 指令/状态非有限数 | EmergencyStop | 整帧命令无效 |
| 绞盘行程越界 | EmergencyStop | 不能继续向同方向运动；首版整组停机 |
| 疑似断绳 | EmergencyStop | 当前不做在线重构或秩恢复 |
| 实际位姿严重越界/碰撞风险 | EmergencyStop | 依赖实际反馈源 |
| UI 卡死而硬件控制仍活动 | EmergencyStop | 独立看门狗触发 |

具体阈值和持续时间应通过空载、带绞盘、带绳索三个阶段分别标定，不能直接
复制原工程的 1.15、1.2 和 250 ms。

---

## 11. 迁移验收顺序

1. 纯软件故障注入：NaN、旧帧、错序、缺轴、轴失能、API 失败；
2. 空载单轴：位置/速度边界、正常停止和立即停止；
3. 空载 8 轴：任一轴故障必须整组停止并保留首故障；
4. UI 人为阻塞：硬件线程/安全线程仍能触发停机；
5. 拔 EtherCAT：软件连接标志仍为真时也能靠主站诊断识别；
6. ±6.5 圈边界注入：边界前允许、边界外拒绝、越界时整组停机；
7. PVT 板卡自主运行期间注入 Trace/轴故障；
8. 带绞盘低速测试；
9. 带绳索张力与断绳信号注入；
10. 实体急停、STO、硬限位和断电防坠落验收。

每项测试都必须验证三件事：物理运动确实停止、软件状态确实锁存、日志能定位
首故障和停机 API 结果。仅看到 UI 显示“已停止”不算通过。

---

## 12. 最终评价

MotionControl 的安全框架值得借鉴，尤其是独立监控、快速保护、统一停机、首
故障锁存和结构化日志。但完整源码也表明，旧版总结对断绳恢复、工作空间来源、
看门狗参数和停机等级存在误读。

当前工程下一步最有价值的安全工作不是立即搬入全部张力和工作空间算法，而是
先建立 `CdprSafetySupervisor + 统一硬件停机入口 + 首故障锁存`，并让 8 台空载
电机的 PVT 和速度闭环都经过同一条安全状态机。完成这一步后，再逐层开放绞盘
行程、真实张力、真实末端位姿和硬件安全链。
