# CDPR 8绳6自由度力交互速度闭环控制架构

## 1. 目标与边界

软件面向一套8绳6自由度CDPR样机。运行时根据六维力传感器当前样本，
使用纯惯性刚体 Newmark-β 单步推进得到下一时刻期望末端状态，再转换为
8根绳索和8个电机的速度模式位置闭环命令。

首版控制链为：

```text
当前六维力样本（ZOH）
→ 坐标和零偏处理
→ Newmark-β单步推进
→ 期望末端位姿/速度/加速度
→ 直线绳段逆运动学
→ 8绳期望长度/速度
→ 速度前馈 + 位置PID
→ 全局安全检查
→ 8轴速度命令
```

反馈链为：

```text
8轴同帧Trace
→ 电机角度/速度
→ 8绳实际长度/速度
→ 正运动学
→ 实际末端位姿
→ 误差、安全、记录和低频显示
```

当前只采用框架出绳点到平台连接点的直线长度。滑轮包络、绞盘分层有效
半径和真实六维力传感器驱动在完成对应标定后再接入。

---

## 2. 核心设计原则

### 2.1 单机器人不等于静态共享状态

程序只创建一套 `CdprCoordinator`，由它组织唯一的机器人状态；数学模块
和硬件模块不得通过静态变量共享当前位姿、绳长、雅可比、PID状态或反馈。

- 配置和几何参数可复制为只读快照；
- 协调器是 `CdprRobotState` 的唯一写入者；
- UI、绘图和记录模块只读取完整快照；
- 数值求解器需要上下文时，通过显式参数或 `void *context` 传递；
- `static` 只用于常量、无状态工具函数和第三方SDK要求的回调入口。

### 2.2 整帧有效

8轴命令和8轴反馈必须以整帧传递。每帧至少包含：

- 连续序号；
- 单调时钟时间戳；
- 有效轴掩码；
- 期望、命令或反馈数组；
- 整帧有效标志。

任一关键轴数据过期或无效时，不能用上一帧值拼成“看似完整”的新帧。

### 2.3 单位集中转换

数学层统一使用：

- 长度：m；
- 时间：s；
- 角度：rad；
- 力：N；
- 力矩：N·m；
- 末端位姿：`[x,y,z,rx,ry,rz]`；
- 末端速度：`[vx,vy,vz,ωx,ωy,ωz]`，角速度在世界坐标系表达。

电机角度制、板卡unit和脉冲只在硬件映射边界转换。

---

## 3. 数据模型和所有权

`cdpr/CdprControlTypes.h` 定义固定维数结构：

- `CdprFrameStamp`：帧序号、单调时间和有效性；
- `CdprPlatformState6`：末端位姿、速度和加速度；
- `CdprCableState8`：8绳长度、速度和加速度；
- `CdprAxisCommandFrame8`：8轴位置/速度命令；
- `CdprAxisFeedbackFrame8`：8轴指令位置、实际位置和实际速度；
- `CdprRobotState`：一个控制周期的完整机器人快照。

`CdprRobotState` 中的静态初值不伪造运行时间戳；只有真实控制周期生成的
完整帧才能将 `stamp.valid` 置为 `true`。

### 3.1 配置

`CdprConfiguration` 保存一套样机固定参数：

- 8个框架出绳点全局坐标；
- 8个平台连接点局部坐标；
- 绳索到轴号映射和待标定方向；
- 无Nokov时使用的预设初始末端位姿；
- 动平台总质量、质心和惯量；
- 六维力传感器安装变换与符号；
- 绞盘直径和相对初始位置最大圈数；
- 控制周期和保护阈值。

配置由 `cdpr_configuration.json` 加载。G302参数只是首版来源，
`parameters_confirmed=true` 之前不能进入实机运行准备状态。

当前绞盘直径为160 mm，允许相对初始位置双向收放6.5圈：

\[
\Delta L_{max}=\pi dN=3.26725636\ {\rm m}
\]

该数值已有配置和检查函数，但尚未接入实际8轴周期停机链，因此还不是
已经生效的实机保护。

### 3.2 启动状态与固定配置分离

每次开机的实际初始状态不是固定配置。启动时根据用户选择：

- 预设模式：使用JSON中的预设位姿；
- Nokov模式：采集当帧全部 `LabeledMarkers`，由样机标记点几何重建位姿。

当前代码只完成Nokov标记点采集和时间戳封装，明确忽略SDK提供的刚体位姿；
标记点几何重建算法保留为接口，尚未输出有效位姿。获得初始位姿后，由同一
套逆运动学计算8绳启动参考长度。8轴编码器启动基准尚未接入，因此当前启动
状态不会被标成可实机运行。

---

## 4. 数学模块

### 4.1 Newmark-β

动力学使用纯惯性自由刚体模型，不添加回中刚度、虚拟阻尼或虚拟平衡
位置。每个周期只使用当前已经取得的力样本；样本在一个模型周期内按
ZOH保持，不能使用未来力样本做FOH。

详细公式和迭代约定见：

- `单刚体动力学正解_Newmark-beta设计文档.md`
- `CDPR_E3000_G302数学模块迁移与Newmark-beta实现约定.md`

当前 `CdprDynamics` 已实现无UI、无板卡依赖的平均加速度
Newmark-β单步模块，并完成静止和恒力离线测试。它目前只接入CDPR页的
“软件单步”，尚未接入1 ms连续控制循环。

### 4.2 六维力输入边界

力输入统一封装为带序号、单调时间戳和有效标志的六维帧。当前提供：

- 模拟六维力输入，用于软件单步和后续软件在环；
- Trace F/T占位输入，真实六维力传感器也由控制卡Trace功能采集，等待确定
  对象类型和六通道映射后实现。

首版约定Trace取得的六个原始数已经分别是真实的N和N·m，不再做比例换算
或整体反号，即 `scale=1`、`wrenchReactionSign=+1`。但传感器通电预热后
仍需估计并扣除当次运行零漂 `runtimeZeroOffset`。它属于运行状态，不写回
固定JSON配置。传感器坐标系到平台质心的坐标变换仍然需要执行，力矩同时
包含安装点到质心的 `r × F` 力臂项。

#### 4.2.1 预热、稳定判定和零漂

控制卡初始化并配置F/T Trace后，程序持续读取和记录六通道数据。真实力交互
输入采用以下状态机：

```text
未连接 → Trace采集中 → 预热中 → 候选稳定
        → 零漂已确认 → 可用于力交互控制
```

在“零漂已确认”之前，数据只能用于显示、记录和稳定性判断，不得送入
Newmark，也不得开放真实力交互运动。自动确认同时要求：

- 达到可配置的最短预热时间，初始建议15分钟；
- 最近30～60 s滑动窗口的Trace有效率满足要求，且无持续丢帧或超时；
- 六个通道各自的标准差低于阈值，用于限制稳定状态下的随机抖动；
- 六个通道各自的均值变化斜率低于阈值，用于排除仍在缓慢漂移的零点；
- 六个通道各自的峰峰值低于阈值，用于发现窗口中的突跳和尖峰；
- 上述条件连续满足一段确认时间，初始建议60 s。

稳定窗口中第 `j` 通道的零漂取窗口均值，不取单个瞬时样本：

\[
b_j=\frac{1}{N}\sum_{k=1}^{N}w_{raw,j}(k),\qquad
w_{corrected,j}=w_{raw,j}-b_j
\]

UI同时提供“确认当前零漂”和“清除零漂/重新预热”按钮。人工确认也使用最近
5～10 s有效窗口的均值；若窗口统计仍不稳定，必须警告并要求再次确认，
不能静默采用。确认操作只允许在8轴停止、平台无人工施力且Trace有效时执行。

零漂一旦确认，在当次力交互运行期间冻结，禁止随实际受力自动更新，否则
真实外力会被吸收到零漂中。软件重启、传感器重新上电或Trace重新配置后，
默认使零漂失效；历史零漂只随记录文件保存，不自动跨上电周期复用。

#### 4.2.2 稳定阈值的确定

阈值不在缺少数据时凭空写死。首轮标定应在传感器卸载、环境稳定的条件下，
从上电开始连续记录30～40分钟，并完成3～5次独立断电—上电试验。每次取
最后5～10分钟作为确认稳定段，使用与在线程序相同的滑动窗口统计标准差、
线性拟合斜率和峰峰值。

可先将稳定段各统计量的99%分位数乘1.5～2作为候选阈值，并分别配置三个
力通道和三个力矩通道。最终必须用完整预热数据回放验证：稳定前不得提前
通过，稳定后应能持续通过，多次上电的判定时间应与实际15～20分钟预热特性
基本一致。

### 4.3 直线绳段逆运动学

第 `i` 根绳索：

\[
P_i^W=p^W+R_{WB}b_i^B
\]

\[
c_i=A_i^W-P_i^W,\qquad L_i=\|c_i\|,\qquad
u_i=\frac{c_i}{L_i}
\]

其中 `A_i` 为框架出绳点，`b_i` 为平台局部连接点。

定义 `r_i=P_i-p`。因为本工程规定绳长增加为正，长度雅可比为：

\[
J_{L,i}=
\begin{bmatrix}
-u_i^T & -(r_i\times u_i)^T
\end{bmatrix}
\]

\[
\dot L=J_L
\begin{bmatrix}v\\\omega\end{bmatrix}
\]

电机收绳/放绳方向不进入运动学本体，由轴映射模块处理。

### 4.4 正运动学

首版使用8个直线绳长残差求6维位姿的阻尼非线性最小二乘解：

\[
\min_q \sum_{i=1}^{8}
\left(L_i(q)-L_{i,meas}\right)^2
\]

要求：

- 使用上一周期实际位姿作为初值；
- 设置固定最大迭代次数和残差阈值；
- 输出是否收敛、迭代次数、RMS残差、最大残差和耗时；
- 不收敛时不得把结果标成有效反馈；
- 正运动学不进入板卡独占线程，按20～100 Hz异步运行。

当前 `CdprKinematics` 已提供无共享状态的正逆运动学和长度雅可比。

### 4.5 绳加速度

解析式为：

\[
\ddot L=\dot J_L\dot q+J_L\ddot q
\]

当前代码尚未实现完整 `\dot J_L\dot q`，因此不会把不完整结果标成有效
绳加速度。后续可实现解析式，或在固定1 ms模型时间轴上对期望绳速差分。

---

## 5. 8轴执行模式

### 5.1 实时力交互速度闭环

每根绳索的控制律：

\[
v_{cmd,i}=K_{ff}\dot L_{ref,i}
+K_p e_i+K_i\int e_i\,dt+K_d\dot e_i
\]

\[
e_i=L_{ref,i}(t_f)-L_{actual,i}(t_f)
\]

`t_f` 是当前有效Trace反馈帧对应的逻辑时间。规划参考必须查询同一逻辑
时间，不能直接拿主机“现在时刻”的规划位置与延迟反馈相减。

8轴分别持有独立的PID状态，包括误差、积分、上一误差和上一速度命令；
首版8轴共用同一组 `Kp/Ki/Kd/Kff` 参数，后续允许逐轴覆盖。不得让8轴共享
同一个积分或微分状态。

首版虚拟绞盘统一假设 `s_i=+1`，定义为电机正方向对应放绳、几何绳长增加。
该符号保存在逐轴配置中，实物安装后只改配置。绞盘名义半径
`r=0.08 m`，换算为：

\[
\Delta\theta_i=s_i\frac{L_i-L_{i,0}}{r},\qquad
\omega_i=s_i\frac{\dot L_i}{r}
\]

空载电机反馈按同一关系反算“虚拟实际绳长”；它只用于验证8轴软硬件链，
不能代表真实CDPR末端运动。

Newmark步长与CDPR控制周期使用同一个 `T_c`。若UI选择1、5或10 ms，
Newmark、逆运动学、8轴PID、周期安全检查和速度命令更新均使用同一周期。
总线Trace仍按板卡配置的采样周期工作；控制周期到达时消费最新完整Trace帧，
F/T力样本在两个有效Trace样本之间按ZOH保持。UI、异步记录和低频正运动学
不跟随 `T_c` 同频运行。

8轴电机反馈配置为同一Trace帧内32个对象（每轴type03/04/05/06）。读取层
必须以板卡回读的对象总数和对象总字节数作为固定帧契约，不允许按对象槽宽猜测
帧数。对象/帧宽不符、硬件序号重复/回退/断裂、卡侧覆盖风险或本地丢帧时，
整帧反馈失效并触发实时8轴整组停机。产帧速率改用卡侧队列守恒关系估算，只
作为周期和批量读取诊断，不得仅凭其与主机时间的比例异常触发停机。

实时8轴启动前还必须处理Trace积压：期望轨迹和运行记录文件准备完成后重新
复位Trace，取得复位后的新鲜同帧反馈，才建立主机时钟与Trace序号锚点。每轴
最大跟随误差保护在该轴速度启动命令下发后收到至少一帧新Trace反馈时才启用；
阈值本身不因启动门控而放宽。

8轴命令进入硬件前统一执行：

- 整帧有效性检查；
- 单轴速度、加速度和位置限制；
- 绞盘相对初始位置行程限制；
- 8轴同步缩放，避免各轴独立削顶破坏末端方向；
- 命令时间戳和过期检查。

硬件边界仍采用 `dmc_vmove` 启动速度模式，并通过
`dmc_change_speed_unit` 周期更新速度。所有板卡API由一个硬件线程串行
调用。

当前用于8台空载电机联调的“已知末端五次轨迹”只是实时速度闭环的参考源：
它通过独立入口生成主机参考轨迹，执行时仍逐控制周期查询位置/速度并在线变速，
不调用PVT装表API，也不受离线PVT的5000点策略限制。离线PVT计划与速度闭环
参考轨迹必须分别缓存；修改控制周期后只使速度闭环参考失效。未来接入实时力交互
后，参考源替换为Newmark逐周期结果，不再依赖有限长度的预生成轨迹。

### 5.2 离线轨迹位置控制（PVT）

程序另设与实时力交互互斥的离线轨迹位控模式。它用于整条期望轨迹在启动前
已经确定的试验，不读取实时外力来改变当次轨迹：

```text
离线平台位姿时间历程
→ 每个时刻做8绳逆运动学
→ 虚拟绞盘换算为8轴位置时间表
→ PVT表下发
→ 8轴同步启动
→ Trace记录位置跟踪结果
```

第一版参考原工程已经使用的调用链：

1. 检查时间戳严格递增、8轴点数一致、数值有限、轴在线和全轨迹行程；
2. 以执行起点为相对零点，为每个轴调用
   `dmc_pvts_table_unit` 装入位置—时间表和起止速度；
3. 只有全部8轴表均成功后，调用一次 `dmc_pvt_move` 同步启动轴列表；
4. 使用PVT运行索引和 `dmc_check_done` 监视进度；
5. 正常结束或异常时统一调用 `dmc_stop`，并记录完整Trace。

该模式首版整条轨迹一次生成并装表，不模拟实时产点，也不与
`dmc_vmove + dmc_change_speed_unit` 同时运行。待基础PVT链验证稳定后，再
决定是否使用显式逐点速度的 `dmc_pvt_table_unit`。

---

## 6. 线程与数据流

推荐职责：

| 模块 | 周期/方式 | 职责 |
|---|---:|---|
| F/T Trace采集 | 总线/Trace周期 | 从Trace六通道生成带帧序号的六维力样本 |
| CDPR控制逻辑 | UI选择的1/5/10 ms | Newmark、逆运动学、8轴控制和周期安全检查 |
| 板卡硬件线程 | 独占串行 | 电机与F/T Trace读取、命令执行、使能和停机 |
| 正运动学估计 | 20～100 Hz | 用最新同帧8绳长度估计实际末端位姿 |
| 数据记录 | 异步批量 | 保存高频完整状态帧 |
| UI | 约20 Hz | 状态、曲线、日志和人工操作 |

线程间只传递有界队列或最新快照：

```text
传感器帧 → 控制逻辑
Trace整帧 → 控制逻辑
8轴命令帧 → 硬件线程
机器人快照 → 记录/UI/正运动学
安全事件 → 协调器和硬件停机入口
```

周期线程不直接写文件或追加UI。运行状态最多按10 Hz发布，普通性能摘要每2 s
生成一次；逐周期原始耗时只写入无锁SPSC队列，由记录线程批量落盘。

实时速度闭环和离线PVT都必须通过同一个硬件线程访问板卡，并由模式状态机
保证二者互斥。

### 6.1 八轴周期性能统计

八轴速度闭环对每个成功完成的控制周期使用单调高精度时钟记录：

- 回调开始到下一次回调开始的实际调度间隔；
- `pollTraceFeedback()` 的Trace读取耗时和本次返回帧数；
- 轨迹查询、误差、安全判定及8个PID的合计计算耗时；
- 8次速度API的逐轴耗时、合计耗时及最慢物理轴；
- 从回调入口到控制和硬件调用结束的完整控制主体耗时。

“执行超时”定义为完整控制主体耗时超过设定周期；“调度超时”定义为实际
回调间隔超过设定周期的1.5倍。估算漏周期按实际间隔最接近的周期整数倍减一
累计，只用于诊断，不直接触发运动停机。

运行目录新增 `control_cycle_timing.csv`，逐周期保存上述数据以及8个逐轴API
耗时。该CSV与Trace共用异步写盘线程，周期线程仅向固定容量环形队列复制一条
定长记录；队列溢出只报告丢弃计数，不允许阻塞硬件控制。UI显示均值/最大值，
运行日志每2 s和结束时输出汇总。该统计用于区分Windows/Qt调度、Trace读取、
控制计算与雷赛串行API四类瓶颈，不能把原先只覆盖部分循环的“最大调用耗时”
继续当作完整周期耗时。

---

## 7. 安全架构

### 7.1 启动条件

所有能产生硬件运动的入口都必须经过同一个启动门禁，不能只依赖按钮禁用。
单轴调试只要求所选轴就绪；离线PVT、8轴速度闭环和最终力交互模式必须要求
8个映射轴全部就绪。进入完整CDPR实机运行前必须同时满足：

- JSON配置已确认并通过校验；
- 直线绳段正逆运动学闭环自检通过；
- 控制卡已打开，EtherCAT主站状态真实有效，而不只是软件初始化标志为真；
- 8轴在线、无轴错误且使能状态符合当前模式要求；
- Trace 8轴同帧采集有效；
- 电机方向、软件零位和绳长映射已标定；
- 初始位姿、初始绳长和绞盘剩余行程有效；
- 力传感器零偏和坐标变换有效；
- 控制线程、硬件线程和安全监控心跳有效；
- 软件停机、实体急停和STO链路已经按当前试验阶段验收；
- 没有未复位的安全锁存。

当前空载电机阶段尚不要求Nokov、真实F/T和绳索张力就绪，但必须在运行状态中
明确标记这些保护尚未启用，不能把降级试验状态当成完整机器人Ready状态。

### 7.2 周期保护

控制线程每周期执行低延迟快速保护，独立安全监控模块使用不可变机器人快照
交叉复核。每周期至少检查：

- 力、状态、命令和反馈是否有限；
- 输入帧是否超时、乱序或缺轴；
- 运动参与轴是否仍在线、使能且无轴错误；
- 8绳长度、速度和加速度是否越界；
- 绞盘相对初始位置是否超过±6.5圈；
- 8轴位置跟随误差是否超限；
- 末端工作空间、正运动学残差和收敛状态；
- 控制周期超时和连续丢帧；
- 张力可行性和绳索干涉结果。

任何关键项失败时整帧命令无效，不能只停一根绳索后让其余轴继续。

完整动平台工作空间大边界、姿态范围和实体碰撞模型保留为后续安全阶段。
当前优先完成8台无负载电机的小范围运动，只先启用轴在线/错误、Trace超时、
速度、加速度、虚拟绞盘行程和整组停机等不依赖真实绳索的平台外保护。

工作空间必须拆成两个不同判据：规划位姿用于命令下发前预检；Nokov或正运动学
得到的实际位姿用于运行期保护。原工程MotionControl只用活动规划点或规划末点
复核运行边界，这一做法不能作为当前工程的真实末端越界保护。

### 7.3 停机与锁存

停机分为：

1. 正常停止：命令平滑降至零；
2. 受控安全停止：短时减速，随后8轴失能；
3. 立即停止：严重通信、超限或外部急停；
4. 进程退出保护：停止运动、失能并关闭控制卡。

所有停止动作都由 `MotionControlWorker` 在唯一硬件线程内执行。独立安全模块
只能提交停机决定，不能从另一个线程直接调用LTDMC。停机入口必须幂等，并且
冻结所有运动源、定时器和后续命令；不能只根据当前页面猜测一个活动模式。

安全故障必须锁存并保留首故障。后续派生故障只记录，只有更高停机等级才触发
动作升级，不能覆盖首故障原因。只有故障原因消失、轴静止且操作者明确复位后，
才能重新进入准备状态；复位不得自动使能或自动恢复运动。

原工程当前把断绳直接升级为软件急停，旧版“剩余绳索秩足够则在线恢复”的描述
已经过时。当前工程首版同样按断绳整组急停设计，不实现运行中重构。

### 7.4 原工程迁移边界与当前优先级

`0523_final_安全逻辑总结与评估.md` 已按完整 `MotionControl/` 源码重审，只作为
来源审计。当前工程迁移其安全状态机、判据分层、首故障锁存和结构化记录，
不迁移以下实现：

- 由MainWindow集中保存大量运行安全状态；
- SafetyMonitor绕过当前硬件线程独占边界直接操作控制卡；
- 用预紧力一半作为全部绳索张力下限；
- 用规划点代替真实末端位姿；
- 控制盒/PLC串口和按钮联锁。

在继续接入真实绳索之前，优先完成 `CdprSafetySupervisor`、统一全模式停机入口、
首故障锁存/人工复位、真实EtherCAT状态检查、参与轴状态检查、8轴PVT安全监视
以及±6.5圈行程进入实机周期保护。实体急停、STO、驱动器保护和机械防坠落始终
独立于软件，不因上位机保护完成而省略。

---

## 8. 软件目录和职责

```text
cdpr/
  CdprControlTypes.*       固定维数帧和机器人快照
  CdprConfiguration.*      JSON配置、校验和配置摘要
  CdprKinematics.*         直线绳长、雅可比和正运动学
  CdprCoordinator.*        单机器人状态机与模块协调
  CdprDynamics.*           纯惯性Newmark-β单步
  CdprForceInput.*         模拟力、Trace F/T占位和坐标变换
  NokovMarkerProvider.*    标记点采集和位姿重建接口
  CdprSafetySupervisor.*   全局安全与锁存（待实现）
  CableMotorMapper.*       绳长/绳速与电机单位映射（待实现）
  CdprController.*         8轴速度闭环（待实现）
```

现有单轴速度闭环、Trace延迟标定、硬件接口和异步记录继续作为可复用底层，
但不能让旧测试页状态直接成为8轴机器人运行状态。

---

## 9. 当前进度

截至本次整理：

| 项目 | 状态 |
|---|---|
| JSON配置v4、G302结构参数和校验 | 已完成 |
| 预设/运行时初始位姿分离 | 已完成 |
| 初始绳长由启动位姿计算 | 已完成 |
| 160 mm绞盘与±6.5圈配置/计算 | 已完成 |
| 固定维数平台、绳索、命令、反馈和机器人状态帧 | 已完成 |
| 无静态共享状态的直线绳段逆运动学 | 已完成 |
| 绳长雅可比和绳速符号 | 已完成并有有限差分测试 |
| 阻尼最小二乘正运动学 | 已完成离线版 |
| 初始正逆运动学闭环自检 | 已接入配置协调器 |
| 绳加速度完整解析式 | 待实现 |
| Newmark-β C++单步模块 | 已完成，尚未接1 ms循环 |
| 模拟六维力、标定和坐标变换 | 已完成软件入口 |
| Nokov全部标记点采集 | 已完成 |
| Nokov标记点几何位姿重建 | 接口已留，算法待实现 |
| Trace F/T采集 | 接口已留，对象类型待定 |
| 实际8轴同帧Trace和电机映射 | 已实现固定帧解析；正在实机校验序号连续性与队列守恒产帧诊断 |
| 8轴离线PVT轨迹位控 | 已完成8台空载电机运行和虚拟运动学一致性分析；Trace异常仅使记录/分析失效，不停止板卡PVT |
| 绞盘行程进入实际周期安全链 | 待实现：当前只有配置、校验和计算函数，不是生效的实机保护 |
| 8轴速度闭环与整帧停机 | 已实现首版；已加入调度/Trace/计算/逐轴API/完整周期异步耗时统计，待据实测确定可用控制周期 |
| 全局安全监督、首故障锁存与人工复位 | 待实现 |
| 统一全模式停机入口 | 待实现：现有各模式可停止，但尚未形成单一幂等安全执行器 |
| PVT板卡自主执行期的实时安全监视 | 待实现 |
| 软件在环和实机CDPR控制 | 待实现 |

当前UI中的最终力交互启动按钮保持禁用。CDPR页已按职责拆分为“结构参数”
“初始化与输入”“预设轨迹测试”“软件验证”“力交互控制”“8绳监视”六个
子页。其中“预设轨迹测试”同时容纳离线PVT和已知轨迹在线速度闭环，作为
整机联调手段；“力交互控制”专门承载未来无固定终点的F/T—Newmark—8轴
连续控制链，避免再与预设轨迹模式混淆。

---

## 10. 下一步

当前程序完善的第一目标不是立即接Nokov或真实F/T，而是完成“模拟六维力
驱动8台空载电机”的硬件在环最小闭环：

```text
模拟六维力
→ 固定控制周期Newmark
→ 直线绳段逆运动学
→ 虚拟绞盘映射
→ 8个独立PID
→ 8轴速度命令
→ 8轴同帧Trace
→ 虚拟实际绳长和跟踪误差
```

该阶段只使用预设初始位姿、统一的临时方向 `s_i=+1` 和基础轴安全保护，
不依赖Nokov、真实绳索、工作空间大边界或真实六维力传感器。建议按以下
顺序继续：

1. 在现有8轴PVT和速度闭环基础上实现 `CdprSafetySupervisor`、统一运动模式、
   首故障锁存、人工复位和单一全模式停机入口；
2. 将真实EtherCAT主站、参与轴状态、8轴同帧Trace、±6.5圈绞盘行程和PVT
   自主执行期监视接入安全链，并完成空载故障注入；
3. 实现首版 `CableMotorMapper`：8轴先统一 `s_i=+1`，完成虚拟绞盘角度、
   速度和绳长的双向换算；
4. 增加独立周期控制调度器，将Newmark从UI软件单步改为与UI所选1/5/10 ms
   一致的连续循环；
5. 复用已经完成的8个独立PID状态和8轴同帧Trace，以低速、小范围驱动空载
   电机，核对方向、周期、速度、API耗时和虚拟绳长；
6. 确定F/T Trace对象类型和六通道映射，加入持续预热、自动稳定判断和人工
   零漂确认，再把真实力帧接入Newmark；
7. 后续再实现Nokov标记点位姿重建、实际工作空间/姿态、真实张力和实体干涉
   保护，并验收实体急停、STO与机械防坠落措施。

每完成一阶段，都必须先通过离线测试和日志审查，再开放下一阶段的UI运动
入口。
