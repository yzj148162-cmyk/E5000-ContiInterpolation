# CDPR E3000 G302数学模块迁移与Newmark-beta实现约定

## 1. 文档目的

本文档用于统一后续8绳6自由度CDPR力交互控制软件的数学约定和实现边界。

后续软件采用以下组合：

- CDPR结构参数、坐标关系、六维力转换、绳索几何、干涉检测和张力分配以
  `MATLAB代码/CDPR_dynamics_solve/G302_current` 为主要参考；
- 单刚体动力学时间推进统一使用 Newmark-beta；
- 不迁移 `G302_current` 当前采用的RK4积分器；
- Newmark动力学、CDPR运动学、张力可行性和真实电机执行层相互分离。

本文档记录当前已经确认的约定。未确认的实物参数必须保留为无效占位值，
不得通过正式运行校验。

---

## 2. 已确认的总体约定

### 2.1 动力学

- 目标是模拟微重力环境中的自由漂浮刚体；
- 使用纯惯性 Newmark-beta 模型；
- 不加入回中刚度；
- 不加入虚拟阻尼；
- 不加入人为速度衰减；
- 推荐参数为 `beta=0.25`、`gamma=0.5`；
- 每个控制周期依据当前六维力推进得到下一周期期望状态。

### 2.2 绳长和电机方向

- 几何绳长增加定义为正；
- 绳速定义为几何绳长对时间的导数，伸长为正、缩短为负；
- 电机正转究竟对应收绳还是放绳，由实物安装方式决定；
- 每根绳索单独保存轴号、方向、零位和卷筒映射；
- 电机方向符号只在 `CableMotorMapper` 中使用，不进入CDPR运动学本体。

### 2.3 张力

- 启动前执行预紧建立和确认；
- 预紧是启动工序，不是运行期间叠加的控制目标；
- 运行期张力分配只承担真实动平台的重力平衡；
- 运行期张力可行域下限暂定为 `10 N`；
- 张力上限暂按G302算例的 `500 N`，后续根据绳索、传感器和结构额定值确认；
- `10 N`下限是运行期防松绳约束，不称为运行预紧控制。

### 2.4 干涉模型

- 绳索间采用空间线段最短距离和动态穿越检测；
- 第一版绳索—动平台干涉采用圆柱简化模型；
- 圆柱半径、轴线端点、绳径和安全间隙必须配置化；
- 后续可替换为完整动平台几何碰撞模型。

---

## 3. G302_current当前动力学方法

`G302_current` 当前不是Newmark-beta，而是四元数RK4。

主要证据如下：

- `interaction_dynamics_step_G302.m` 明确说明内部使用四元数RK4；
- 状态定义为

\[
x=
\begin{bmatrix}
p_W & v_W & q_{WB} & \omega_B
\end{bmatrix}^{T}
\]

- 每步计算 `k1`、`k2`、`k3`、`k4`；
- 最后按

\[
x_{k+1}=x_k+\frac{h}{6}(k_1+2k_2+2k_3+k_4)
\]

推进，并归一化四元数。

`G302.m` 当前还提供：

- `zoh`；
- `foh`；
- `foh_jump`；

三种力输入保持方式。默认算例使用 `foh_jump` 和 `50 ms` 时间步长。

这些属于MATLAB离线验证策略，不能直接作为实时控制实现。真实控制中无法提前获得
下一周期传感器值，因此第一版Newmark实时控制采用因果ZOH：本周期测得的力旋量在
本次积分步内保持不变。

这里的ZOH不是Newmark-beta算法的一部分。两者职责不同：

- Newmark-beta决定如何由 \(q_k,\dot q_k,\ddot q_k\) 推进状态；
- ZOH只说明离散传感器样本 \(W_k\) 在区间 \([t_k,t_{k+1})\) 内如何解释。

Newmark公式中的 \(\ddot q_{k+1}\) 是根据动力学方程求解的下一状态加速度，并不表示
软件已经取得未来传感器样本 \(W_{k+1}\)。若强行使用 \(W_{k+1}\) 对本区间做FOH，
就必须等下一帧到达后再回算上一周期，从而引入至少一个采样周期的控制延迟。

---

## 4. 坐标系和姿态约定

### 4.1 坐标系

- `{W}`或G：世界/全局坐标系；
- `{B}`或E：动平台局部坐标系，原点位于动平台质心；
- `{S}`：六维力传感器坐标系。

所有几何长度在数学核心中统一使用米，姿态统一使用弧度。

### 4.2 位姿

末端位姿定义为：

\[
q=
\begin{bmatrix}
x&y&z&a_x&a_y&a_z
\end{bmatrix}^{T}
\]

其中旋转矩阵采用ZYX顺序：

\[
R_{WB}=R_z(a_z)R_y(a_y)R_x(a_x)
\]

平台局部点到世界坐标的转换为：

\[
P_i^W=p^W+R_{WB}b_i^B
\]

长期实现中应使用旋转矩阵或四元数保存内部姿态，只在UI、配置兼容和结果显示时
转换为ZYX欧拉角。

### 4.3 G302主要结构参数

当前 `fun_cdpr_params_G302.m` 给出的主要参数为：

- 动平台质量：`30 kg`；
- 动平台惯量：

\[
I_B=\operatorname{diag}(0.2370666667,\ 0.385,\ 0.385)\ \mathrm{kg\cdot m^2}
\]

- 平台连接点已经从几何中心坐标平移为质心局部坐标；
- 传感器原点相对质心：

\[
r_{BS}^{B}=[0,\ 0,\ 0.2333333333]^T\ \mathrm{m}
\]

- 传感器坐标到平台坐标的旋转：

\[
R_{BS}=\operatorname{diag}(1,-1,-1)
\]

- 传感器符号：`sensor_sign=1`。

8个框架出绳点、8个平台连接点和初始位姿已经写入
`cdpr_control/cdpr_configuration.json`。
8根初始绳长不再重复写入配置，而是在加载时由初始位姿按逆运动学计算。
卷筒直径为160 mm，每根绳从启动位置起允许收绳和放绳各6.5圈，
对应双向最大绳长变化量均为约3.2673 m。

---

## 5. 六维力处理

### 5.1 传感器坐标转换

原始六维力定义为：

\[
W_S=
\begin{bmatrix}
F_S\\M_S
\end{bmatrix}
\]

先应用传感器符号，再转换到平台局部坐标系：

\[
F_B=R_{BS}(sF_S)
\]

\[
M_{B,S}=R_{BS}(sM_S)
\]

### 5.2 力矩参考点平移

传感器力矩需要从传感器原点平移到动平台质心：

\[
M_{B,C}=M_{B,S}+r_{BS}^{B}\times F_B
\]

送入Newmark的力旋量为：

\[
W_B=
\begin{bmatrix}
F_B\\M_{B,C}
\end{bmatrix}
\]

平动力转换到世界坐标系：

\[
F_W=R_{WB}F_B
\]

转动力矩继续在体坐标系下参与刚体欧拉方程。

### 5.3 必须记录的数据层级

为便于排查符号、安装方向和零偏问题，应分别记录：

1. 传感器原始六维值；
2. 零偏和温漂补偿后的值；
3. 传感器坐标转换后的值；
4. 平移到质心后的力旋量；
5. 最终送入Newmark的力旋量。

---

## 6. Newmark-beta单步动力学

### 6.1 自由刚体方程

Newmark纯惯性刚体平动方程：

\[
m\ddot p_W=F_W
\]

Newmark纯惯性刚体转动方程：

\[
I_B\dot\omega_B+\omega_B\times(I_B\omega_B)=M_B
\]

Newmark模型不包含真实装置重力。真实动平台重力由绳索张力和机械系统承担，
不能重复加入自由漂浮期望运动模型。当前模型直接使用已确认的动平台总成质量和惯量，
不再保存一套重复的“虚拟刚体质量/惯量”。

### 6.2 单周期输入输出

输入：

- 当前周期时间戳和帧号；
- 当前期望状态；
- 当前六维力旋量；
- 动平台总成质量和惯量；
- 固定控制周期；
- `beta`、`gamma`、迭代次数和收敛阈值。

输出：

- 下一周期期望位姿；
- 下一周期期望线速度和角速度；
- 下一周期期望线加速度和角加速度；
- 是否收敛；
- 迭代次数；
- 最终残差；
- 计算耗时。

### 6.3 单步流程

1. 读取上一周期有效状态；
2. 使用当前力旋量作为本步ZOH输入；
3. 计算Newmark预测位置和预测速度；
4. 对下一时刻加速度执行有界固定点迭代；
5. 校正下一时刻位置、速度和加速度；
6. 检查有限性、收敛、姿态奇异性和状态边界；
7. 结果有效时提交新状态；
8. 结果无效时保持旧状态并请求保护停止，不提交半有效状态。

第一版使用 `beta=0.25`、`gamma=0.5`，固定点迭代上限建议为2～4次。

---

## 7. CDPR逆运动学和绳速

### 7.1 绳索几何

第`i`根绳索：

\[
l_i^W=A_i^W-P_i^W
\]

\[
L_i=\|l_i^W\|
\]

\[
u_i=\frac{l_i^W}{L_i}
\]

其中：

- \(A_i^W\)：框架出绳点；
- \(P_i^W\)：平台连接点；
- \(u_i\)：从平台连接点指向框架出绳点的单位向量。

### 7.2 Jacobian

平台连接点相对质心的位置：

\[
r_i^W=P_i^W-p^W
\]

G302结构矩阵第`i`列为：

\[
J_{T,i}=
\begin{bmatrix}
u_i\\
r_i\times u_i
\end{bmatrix}
\]

并定义：

\[
J=J_T^T
\]

### 7.3 绳速符号修正

G302当前代码使用：

\[
v_{cable}=J
\begin{bmatrix}
v_W\\\omega_W
\end{bmatrix}
\]

但在 \(l_i=A_i-P_i\) 的定义下，几何绳长变化率应为：

\[
\dot L=
-J
\begin{bmatrix}
v_W\\\omega_W
\end{bmatrix}
\]

后续软件统一使用“绳长增加为正”，因此必须采用负号。电机收放绳方向由
`CableMotorMapper` 再单独处理。

### 7.4 绳加速度

优先目标为解析形式：

\[
\ddot L=\dot J_L\dot q+J_L\ddot q
\]

第一阶段允许使用固定1 ms时间步长差分期望绳速，但必须：

- 使用固定模型周期，不使用主机调度实测间隔；
- 记录差分结果供诊断；
- 不将高噪声差分值直接作为唯一急停判据。

---

## 8. 绳索—电机映射

固定半径卷筒第一版采用：

\[
\Delta\theta_i=s_i\frac{\Delta L_i}{r_i}
\]

其中：

- \(s_i\in\{-1,+1\}\)：电机方向标定值；
- \(r_i\)：卷筒有效半径；
- \(\Delta L_i\)：相对初始绳长的变化量。

必须逐轴标定：

1. 绳索号；
2. 控制卡轴号；
3. 电机正转方向；
4. 正转对应收绳还是放绳；
5. 软件零位；
6. 初始实际绳长；
7. 卷筒有效半径；
8. 是否存在分层缠绕。

实物方向未确认前，配置必须保持 `parameters_confirmed=false`，不得启动8轴力交互。

---

## 9. 绳索干涉检测

### 9.1 绳索—绳索静态距离

每周期将8根绳索表示为空间有限线段，检查28对绳索的最短距离。

若：

\[
d_{ij}<d_{\mathrm{rope,min}}
\]

则判定进入干涉危险区或已经接触。阈值应由绳径、安装误差、结构变形和安全裕量确定，
不得长期硬编码为MATLAB中的 `0.006 m`。

### 9.2 动态穿越

G302通过比较上一周期和当前周期的有向体积符号判断两根绳索是否发生穿越。
迁移时需要修正一个现有不一致：

- `G302.m` 构造线段时顺序为“平台动点、框架定点”；
- `checkCableInterferenceFull.m` 当前配置为“框架定点在前”。

C++版本应采用明确结构体：

```cpp
struct CableSegment {
    Vector3 platformPointWorld;
    Vector3 frameAnchorWorld;
};
```

禁止继续依赖6元素数组的隐含端点顺序。

### 9.3 绳索—动平台

第一版将动平台近似为有限圆柱体。检测至少需要：

- 圆柱轴线的两个世界坐标端点；
- 圆柱半径；
- 绳索半径；
- 安全间隙；
- 全部8根绳索，而不是只检查奇数编号绳索。

当前MATLAB中 `0.08 m` 距离阈值与绘图所用 `0.1 m` 圆柱半径并不一致，
不能直接视为最终实物参数。

实时控制线程只计算并返回结构化结果，不执行 `plot`、`disp` 或高频日志输出。

---

## 10. 张力分配和可行性

### 10.1 平衡方程

按照G302的绳向量方向，张力平衡写为：

\[
J_TT+w=0
\]

或：

\[
J^TT+w=0
\]

其中 \(T\in\mathbb{R}^{8}\)。

### 10.2 运行期外力项

当前确认的运行期张力分配只承担真实动平台重力：

\[
w=
\begin{bmatrix}
0\\0\\-m_pg\\0\\0\\0
\end{bmatrix}
\]

其中 \(m_p\) 是真实动平台、传感器、工装和实际负载的总质量，也是当前Newmark纯惯性求解使用的质量参数。

交互力用于生成自由漂浮期望运动，不在张力分配中重复叠加。按当前建模假设，
外部交互力与真实动平台完成相应加速所需的惯性项相抵消，因此张力分配只处理重力。

原 `G302.m` 中“忽略交互力，但保留交互力矩”的写法不一致，后续不得照搬。
当前统一使用：

\[
F_{\mathrm{allocation}}=[0,0,-m_pg]^T
\]

\[
M_{\mathrm{allocation}}=[0,0,0]^T
\]

### 10.3 张力边界

第一阶段采用：

\[
10\ \mathrm{N}\le T_i\le500\ \mathrm{N}
\]

含义如下：

- 启动前预紧：建立可运行初始状态；
- 运行期10 N下限：防止松绳的可行性约束；
- 运行期不额外叠加“预紧控制量”；
- 500 N暂为算例上限，实机前必须按硬件额定值确认。

### 10.4 求解方法

首选参考 `bary_center.m`：

- 求特解；
- 求 \(J^T\) 的二维零空间；
- 将8个张力上下界转换为二维可行多边形；
- 选择可行多边形重心；
- 恢复8绳张力。

该方法只适用于：

- 8绳6自由度；
- \(J_T\) 满秩；
- 零空间维数严格为2；
- 可行多边形非退化。

实现时使用QR、SVD或线性方程求解，禁止显式计算 `inv()`。

后备方法参考 `pseudo_inverse_new.m`：

1. 尝试最小范数伪逆解；
2. 检查张力上下界；
3. 不满足时求解带等式约束和上下界的QP；
4. 检查平衡残差和求解器状态。

### 10.5 在速度闭环中的作用

当前执行器使用速度模式位置闭环，张力分配结果第一阶段不直接作为电机指令，
而用于：

- 初始位姿可行性检查；
- 下一期望位姿可行性检查；
- 松绳风险预测；
- 工作空间筛选；
- 安全诊断和数据记录。

---

## 11. 工作空间与离线验证

`G302_tension_feasible_workspace.m` 可作为离线验证参考，检查：

1. 位姿是否产生有限、非零绳长；
2. 绳索之间是否干涉；
3. 绳索是否与圆柱平台干涉；
4. Jacobian是否满秩；
5. 是否存在满足 `10～500 N` 的重力平衡张力；
6. 初始位姿是否处于安全工作空间内部。

蒙特卡洛范围、姿态范围和干涉阈值属于试验配置，不能直接固化为设备安全边界。

---

## 12. 推荐的软件模块

```text
robot/
├─ RobotConfiguration
├─ RobotConfigurationValidator
├─ RobotControlCoordinator
├─ RobotSafetySupervisor
└─ RobotControlTypes

dynamics/
├─ NewmarkBetaStepper
├─ RigidBodyDynamics
└─ WrenchProcessor

kinematics/
├─ CdprKinematics
├─ CableGeometry
├─ CableMotorMapper
└─ ForwardKinematicsEstimator

safety/
├─ CableInterferenceChecker
├─ PlatformInterferenceChecker
└─ TensionFeasibilityChecker

allocation/
└─ CableTensionAllocator
```

模块职责：

- `WrenchProcessor`：力传感器补偿、坐标转换和力矩参考点平移；
- `NewmarkBetaStepper`：纯惯性单步期望状态推进；
- `CdprKinematics`：位姿、绳长、绳速和Jacobian；
- `CableMotorMapper`：绳长与电机方向、零位、卷筒之间的转换；
- `CableInterferenceChecker`：绳索间静态/动态干涉；
- `PlatformInterferenceChecker`：圆柱简化平台干涉；
- `CableTensionAllocator`：重力平衡张力和可行域计算；
- `RobotSafetySupervisor`：汇总数学有效性、硬件状态和安全边界。

---

## 13. 每周期控制链路

```text
同一周期六维力帧
→ 零偏/温漂/符号处理
→ 传感器坐标转换与质心力矩平移
→ Newmark-beta推进下一期望末端状态
→ CDPR逆运动学
→ 8绳期望长度、伸长速度和加速度
→ 干涉、工作空间和张力可行性检查
→ 8绳到8轴方向/零位/卷筒映射
→ 8轴速度前馈+位置PID
→ 硬件线程批量下发速度命令
→ 同帧Trace读取8轴实际位置
→ 实际绳长
→ 正运动学
→ 期望/实际末端状态对比
```

若Newmark、运动学、干涉、张力可行性或命令帧任一环节失败，本周期不得下发部分有效的
8轴命令。

---

## 14. 不得直接照搬的G302_current内容

1. 四元数RK4积分器；
2. `50 ms`离线演示步长；
3. 实时不可获得未来样本的无延迟FOH；
4. `ideal_cable_v=Jv`对应的绳速符号；
5. 绳索线段动点/定点顺序不一致；
6. 只检查奇数绳的平台干涉循环；
7. 干涉函数内部绘图和控制台输出；
8. 张力下限在 `0 N` 和 `10 N` 之间不一致；
9. 张力平衡中忽略交互力却保留交互力矩；
10. `inv()`和未经条件数检查的矩阵求逆；
11. 把MATLAB工作空间抽样范围直接当作实机安全范围；
12. 在当前样机需求下重复保存物理平台和“虚拟刚体”两套相同质量惯量，造成参数来源不唯一。

---

## 15. 第一阶段测试要求

### 15.1 数学单元测试

- 传感器力旋量变换手算对照；
- ZYX姿态变换与MATLAB逐点对照；
- 位姿到8绳长度逐点对照；
- 绳速与绳长有限差分符号对照；
- Newmark单步与MATLAB实现对照；
- 重力张力分配残差对照；
- 干涉距离和动态穿越构造算例；
- 位姿→绳长→正运动学位姿回环。

### 15.2 软件在环

- 使用模拟六维力源；
- 使用1 ms固定模型周期；
- Newmark每周期只产一个下一状态；
- 模拟8轴反馈；
- 检查帧号和时间戳连续性；
- 验证外力撤除后自由漂移；
- 验证工作空间、干涉和张力不可行时冻结产点并保护停止。

### 15.3 实机前必须确认

- 8根绳索与8个控制轴的映射；
- 每轴正转对应收绳还是放绳；
- 8轴软件零位与初始实际绳长；
- 卷筒有效半径及分层模型；
- 绳长、轴位置、速度和加速度边界；
- 圆柱平台实际尺寸和干涉安全间隙；
- 实际张力传感器量程、噪声和采样周期；
- 张力上限500 N是否符合实物额定值。

---

## 16. 与既有文档的同步原则

本文件是G302数学模块迁移和实现约定的集中来源。既有文档只需同步与各自职责相关的
结论，避免把全部推导重复复制到多份文档中。

- 安全文档同步张力边界、启动预紧/运行防松绳的区别、干涉保护和数学失败处理；
- 单刚体动力学文档同步实时ZOH、纯惯性模型及G302当前RK4只作对照的结论；
- 8轴力交互架构文档同步绳速正方向、重力张力分配、圆柱干涉模型和模块来源。
