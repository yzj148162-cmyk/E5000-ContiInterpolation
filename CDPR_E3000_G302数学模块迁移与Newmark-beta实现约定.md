# G302数学模块迁移与Newmark-β实现约定

## 1. 用途

本文规定从 `MATLAB代码/CDPR_dynamics_solve/G302_current` 迁移到
`cdpr_control` 的数学定义。它是代码实现和离线对照的数学合同，不记录
已经放弃的控制路线。

首版来源：

- 结构与坐标：`fun_cdpr_params_G302.m`；
- 直线绳段、雅可比和干涉：`G302.m` 及工作空间函数；
- 样机配置快照：`cdpr_control/cdpr_configuration.json`；
- 动力学积分：Newmark-β设计文档。

---

## 2. 已确认约定

### 2.1 坐标、位姿和单位

- 世界坐标系采用右手系；
- 位姿为 `[x,y,z,rx,ry,rz]`；
- 旋转矩阵为 `Rz*Ry*Rx`；
- 框架出绳点在世界坐标系表达；
- 平台连接点在平台局部坐标系表达；
- 长度m、时间s、角度rad、力N、力矩N·m；
- 绳长增加为正，缩短为负；
- 电机方向符号只在绳索—电机映射层使用。

### 2.2 动力学

- 使用纯惯性自由刚体模型；
- 不设置回中刚度、虚拟阻尼或虚拟平衡点；
- 运行期外力来自六维力传感器当前有效样本；
- 一个模型周期内按ZOH保持当前样本；
- 重力由绳索张力承担；
- 运行期间不额外施加预紧，预紧只属于启动前准备。

### 2.3 几何与安全

- 第一版绳长采用出绳点—连接点直线距离；
- 暂不加入滑轮包络长度；
- 圆柱体平台模型、绳索干涉和张力可行性沿用G302定义重写；
- 张力下限首版为10 N；
- 首版空载验证先统一假设 `s_i=+1`，即电机正方向对应放绳、绳长增加；
  实物安装后再逐轴改为实际方向；
- 完整工作空间和实体碰撞边界暂缓，先完成8轴空载、小范围受控运动。

---

## 3. 配置参数

结构参数只在 `cdpr_configuration.json` 保存一份，代码和文档不再复制一套
可漂移的数值。配置至少包含：

- 8个 `frame_anchor_m`；
- 8个 `platform_anchor_m`；
- 无Nokov时使用的预设初始平台位姿；
- 动平台总质量、质心和惯量；
- 传感器受力/测量参考点位置及传感器到平台的旋转；其中位置已实测为平台
  局部 `+Z` 方向 `325.48 mm`，旋转仍待标定；
- 8绳到8轴的映射和方向；
- 卷筒直径160 mm；
- 相对初始位置双向最大6.5圈；
- 控制周期默认1000 μs，首阶段允许UI选择1、5或10 ms；Newmark、逆运动学、
  8轴PID和速度命令更新统一使用当次选择的控制周期。

初始8绳长度不作为独立固定配置项。每次启动先从预设值或Nokov标记点重建
获得当次初始位姿，再通过同一套逆运动学计算启动参考绳长，避免配置值与
几何参数不一致。

---

## 4. 六维力处理

真实六维力传感器由控制卡Trace功能采集。首版将Trace六通道原始数直接视为
真实的力/力矩值，单位已经是N和N·m，因此使用 `scale=1` 和
`wrenchReactionSign=+1`。这里的“不做幅值标定”不等于零漂为零：传感器
通电预热稳定后，程序仍需用稳定窗口均值建立当次运行
`runtimeZeroOffset` 并逐通道扣除。该值不写回固定JSON配置。传感器安装
坐标到平台质心的旋转和平移仍必须执行。

F/T预热和零漂估计遵守以下数学约定：

- 从Trace配置成功开始连续采样，零漂确认前不得向动力学发布有效真实力帧；
- 每通道在同一滑动窗口内计算标准差、线性回归斜率和峰峰值；
- 自动确认需要最短预热时间、六通道同时稳定和连续稳定时长均满足；
- 零漂是稳定窗口均值，不是某一帧瞬时值；
- 人工确认按钮同样对最近有效窗口求均值，并在波动超限时警告；
- 运行期间冻结零漂，不得用实际受力样本更新；
- 软件重启、传感器重新上电或Trace重新配置后默认重新建立零漂。

稳定阈值通过3～5次完整上电预热数据确定。每次记录30～40分钟，取末段
5～10分钟作为稳定基准；以在线窗口重算统计量，并用稳定统计量99%分位数的
1.5～2倍作为候选值。力通道阈值使用N、N/s，力矩通道使用N·m、N·m/s，
最终以完整预热数据回放是否提前误判和能否持续稳定为准。

设传感器坐标系力旋量为：

\[
w_S=\begin{bmatrix}F_S\\M_S\end{bmatrix}
\]

传感器到平台的旋转为 `R_BS`，传感器原点相对平台动力学参考点为 `r_BS`：

\[
r_{BS}=\begin{bmatrix}0&0&0.32548\end{bmatrix}^{T}\ \mathrm{m}
\]

这里把实测受力点作为传感器六维力的测量参考原点；若传感器手册定义的力矩
参考原点不同，实物标定时必须修正 `r_BS`，不能直接沿用受力点坐标。

\[
F_B=R_{BS}F_S
\]

\[
M_B=R_{BS}M_S+r_{BS}\times F_B
\]

若动力学在世界坐标系计算，再用当前平台姿态转换：

\[
F_W=R_{WB}F_B,\qquad M_W=R_{WB}M_B
\]

必须分别记录：

- 原始六通道；
- 去零偏后的传感器坐标系力旋量；
- 平台坐标系力旋量；
- 世界坐标系动力学输入；
- 样本时间戳和有效性。

不得用未来样本插值当前周期。传感器帧过期、乱序或无效时，安全层应拒绝
生成新的运动命令。

---

## 5. Newmark-β单步推进

每周期输入：

- 上一状态 `q_k, qdot_k, qddot_k`；
- 当前有效外力旋量；
- 质量、质心和惯量；
- 固定模型步长 `Δt`。

输出：

- `q_{k+1}`；
- `qdot_{k+1}`；
- `qddot_{k+1}`；
- 迭代收敛、残差和耗时。

首版参数采用平均加速度格式：

\[
\beta=\frac14,\qquad \gamma=\frac12
\]

具体刚体方程、欧拉角运动学和隐式校正流程见
`单刚体动力学正解_Newmark-beta设计文档.md`。

实现要求：

- 固定最大迭代次数；
- 所有中间量做有限数检查；
- 不收敛时整帧无效；
- 不在数学模块内部访问UI、板卡或全局机器人状态；
- 模块输入输出使用固定维数结构。

---

## 6. 直线绳段逆运动学

第 `i` 根绳索的框架出绳点为 `A_i^W`，平台局部连接点为 `b_i^B`：

\[
P_i^W=p^W+R_{WB}b_i^B
\]

\[
c_i=A_i^W-P_i^W
\]

\[
L_i=\|c_i\|,\qquad u_i=\frac{c_i}{L_i}
\]

连接点相对平台原点：

\[
r_i=P_i-p
\]

G302结构矩阵对应行为：

\[
J_i=
\begin{bmatrix}
u_i^T & (r_i\times u_i)^T
\end{bmatrix}
\]

由于本工程规定“几何绳长增加为正”，代码中的长度雅可比必须是：

\[
J_{L,i}=-J_i
\]

\[
\dot L=J_L
\begin{bmatrix}v_W\\\omega_W\end{bmatrix}
\]

这一定义已经在 `CdprKinematics` 中实现，并用中心有限差分校验符号。

完整绳加速度为：

\[
\ddot L=\dot J_L\dot q+J_L\ddot q
\]

在 `\dot J_L\dot q` 实现前，代码不得把不完整的绳加速度标成有效。

---

## 7. 绳索—电机映射

运动学只处理几何绳长。电机映射单独完成：

```text
绳索号
→ 控制卡轴号
→ 收绳/放绳方向
→ 初始软件零位
→ 卷筒有效半径
→ 电机角度、速度和板卡unit
```

理想定半径模型：

\[
\Delta L_i=s_i r_i\Delta\theta_i
\]

其中 `s_i∈{-1,+1}` 由实物标定。当前直径160 mm，因此名义半径为
0.08 m；未来若卷筒分层造成有效半径明显变化，必须用标定模型替换固定
半径。

卷筒相对初始位置限制：

\[
|\Delta L_i|\le\pi\cdot0.16\cdot6.5
=3.26725636\ {\rm m}
\]

---

## 8. 正运动学

由8根实测绳长求6维位姿：

\[
\min_q
\sum_{i=1}^{8}\left(L_i(q)-L_{i,meas}\right)^2
\]

首版 `CdprKinematics::forward` 使用阻尼最小二乘、数值位姿雅可比和回溯
步长。调用方必须：

- 用上一有效位姿做初值；
- 检查 `valid` 和 `converged`；
- 记录迭代次数、RMS残差和最大残差；
- 不收敛时保留上一显示值，但把本帧实际末端位姿标成无效；
- 在独立低频任务中运行，不占用板卡线程。

原工程固定 `[0,0,400,0,0,0]` 初值和静态共享几何的实现不直接迁移。

---

## 9. 张力分配

结构矩阵：

\[
J_T=
\begin{bmatrix}
u_1&\cdots&u_8\\
r_1\times u_1&\cdots&r_8\times u_8
\end{bmatrix}
\]

静力平衡：

\[
J_TT+w=0
\]

首版张力边界：

\[
10\ {\rm N}\le T_i\le T_{max,i}
\]

张力分配用于：

- 启动位姿承载可行性检查；
- 运行期当前位姿可行性诊断；
- 干涉或故障后的安全判断。

速度闭环命令仍由绳长和绳速生成，张力优化不直接替代位置/速度控制。

---

## 10. 干涉和工作空间

从G302重写以下检查，并统一使用同一位姿生成的8条直线段：

1. 绳索长度有效且大于最小阈值；
2. 绳索—绳索最短距离；
3. 连续两帧绳段的动态穿越；
4. 绳索—圆柱体动平台干涉；
5. 平台位置和姿态工作空间；
6. Jacobian秩和条件数；
7. 张力分配可行性。

所有检查必须返回结构化结果：有效性、故障项、相关绳索编号和数值，不在
数学函数内部直接急停或弹窗。

---

## 11. 当前代码映射

| 数学职责 | 当前文件 | 状态 |
|---|---|---|
| 样机配置与初始绳长 | `CdprConfiguration.*` | 已完成 |
| 固定维数状态帧 | `CdprControlTypes.h` | 已完成 |
| 直线绳长、绳速雅可比 | `CdprKinematics.*` | 已完成 |
| 正运动学离线求解 | `CdprKinematics.*` | 已完成 |
| 初始正逆解闭环自检 | `CdprCoordinator.*` | 已完成 |
| 绳加速度完整项 | `CdprKinematics.*` | 待实现 |
| Newmark-β单步推进 | `CdprDynamics.*` | 已完成软件单步 |
| 模拟力及传感器坐标变换 | `CdprForceInput.*` | 已完成 |
| Nokov标记点采集 | `NokovMarkerProvider.*` | 已完成 |
| Nokov标记点位姿重建 | `CdprMarkerPoseEstimator` | 接口已留，待实现 |
| Trace F/T采集 | `TraceFtSensorSource` | 占位，类型待定 |
| 张力分配和干涉 | 独立数学模块 | 待实现 |
| 绳索—电机映射 | `CableMotorMapper.*` | 待实现 |
| 8轴离线PVT位置轨迹 | 独立执行模式 | 参考原工程调用链，待实现 |

---

## 12. 离线验收

进入软件在环前必须通过：

- G302初始8绳长度逐项对照；
- 直线绳长有限数和零长度检查；
- 解析绳速与中心有限差分对照；
- 正运动学从扰动初值回到已知位姿；
- 两个运动学实例之间无状态污染；
- Newmark静止、恒力、恒力矩和能量行为测试；
- 坐标变换、力矩平移和符号测试；
- 张力边界、干涉和奇异位姿测试。

对应代码测试入口为：

- `cdpr_control/tests/cdpr_kinematics_tests.pro`
- `cdpr_control/tests/cdpr_dynamics_tests.pro`
