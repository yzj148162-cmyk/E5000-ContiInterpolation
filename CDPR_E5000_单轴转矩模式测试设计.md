# CDPR E5000 单轴转矩模式测试设计

## 1. 目标与边界

本页只用于验证单个 EtherCAT 伺服轴的转矩模式调用、实际转矩反馈和安全停止，不参与速度闭环、连续插补或多轴协同控制。

调用依据：

- 《雷赛 EtherCAT 控制卡转矩功能应用说明》中的 `nmc_torque_move`、`nmc_change_torque`、`nmc_get_torque` 与 `dmc_stop`；
- 《ISMC Diamond 系列伺服使用手册 V21》第 7.2 节的默认 EtherCAT PDO 映射；
- 原工程 `0523_final` 的转矩换算及独立监测逻辑。

## 2. 分层与线程

- UI 线程：采集参数、显示状态、以 20 Hz 左右批量刷新曲线；
- `ContiWorker` 控制线程：转矩测试状态机、Trace/转矩反馈监测和安全判定；
- `E5000HardwareInterface` 独占硬件线程：唯一允许调用 LTDMC 的边界。

目标转矩不会在每个监测周期重复下发：

- 启动时调用一次 `nmc_torque_move`；
- 用户点击“在线更新转矩”时调用一次 `nmc_change_torque`；
- 监测周期只读取 Trace 与 `nmc_get_torque`。

## 3. 单位换算

CiA402 转矩值按千分额定转矩表示：

\[
TorqueRaw=\operatorname{round}\left(
\frac{Torque_{Nm}}{RatedTorque_{Nm}}\times1000
\right)
\]

实际转矩反算为：

\[
Torque_{Nm}=\frac{TorqueRaw}{1000}\times RatedTorque_{Nm}
\]

当前测试电机及传动参数为：

- 电机额定转矩：`222 mN·m = 0.222 N·m`；
- 电机堵转转矩：`2910 mN·m = 2.910 N·m`；
- 电机额定电流：`3.2 A`，转矩常数：`84.9 mN·m/A`；
- 增量式编码器：`1024 line × 4 = 4096 pulse/电机圈`；
- 减速器减速比：`44:1`；
- 减速器输出端：`4096 × 44 = 180224 pulse/圈`，即
  `180224 / 360 = 500.622222 pulse/deg`。

界面的目标转矩、命令限幅和实际转矩均指**电机轴转矩**。理论减速器输出转矩为
`电机轴转矩 × 44 × 减速器效率`；由于当前没有可靠的减速器效率参数，程序不显示虚构的
输出端实际转矩。位置和速度统一按减速器输出端角度显示。

## 4. 启动流程

1. 初始化控制卡并确认测试轴在线；
2. 使能测试轴；
3. 检查控制卡过程数据能否读取 RxPDO 6071h 和 TxPDO 6077h；
4. 根据实际硬件需要，手动写入并读回最大电机速度 SDO 6080h；
5. 设置非零目标转矩，确认命令限幅、最大行程、最大速度和最长运行时间；
6. 点击“开始转矩测试”，程序会再次执行 PDO 启动前检查；
7. 程序重配该轴 Trace，取得有效首帧；
8. 记录实际起点，根据转矩方向计算绝对位置限位；
9. 调用 `nmc_torque_move` 进入转矩模式；
10. 周期监测实际转矩、位置、速度、轴状态和 Trace 新鲜度；
11. 停止时调用 `dmc_stop`，由控制卡使伺服返回位置模式。

程序不会自动使能轴，也不会在启动转矩测试时自动改写伺服 OD。

## 5. Diamond PDO 与最大速度 SDO

Diamond V21 手册给出的默认 EtherCAT 映射为：

- RxPDO `6071h:00h`：目标转矩，INT16；
- TxPDO `6077h:00h`：实际转矩，INT16。

`nmc_torque_move` 和 `nmc_change_torque` 通过 6071h 下发目标转矩，
`nmc_get_torque` 通过 6077h 读取实际转矩。程序在手动检查和每次启动前使用只读 PDO API
核对这两个对象是否已进入控制卡过程数据；检查失败时不得启动，需在 Motion 中修正映射并重新下载配置。

原工程使用的 220Bh 属于 Servotronix 驱动器厂商对象，不适用于当前 ISMC Diamond，
已从本程序删除。6080h 仅作为可选的最大电机速度 SDO，通过
`nmc_set_node_od`/`nmc_get_node_od` 手动写入和读回；它不是周期 PDO，
也不会在转矩测试启动时自动修改。当前按电机空载转速将默认值设为 `5540 rpm`，
最终仍须以当前 Diamond 对象字典和驱动器配置的单位定义为准。`180224` 是减速器输出端
每圈脉冲数，不是最大电机速度，禁止将其写入 6080h。

## 6. 安全规则

以下任一条件触发停止：

- 目标转矩超过命令限幅；
- `nmc_get_torque` 换算后的实际转矩超过反馈保护上限；
- 实际相对位移达到最大行程；
- Trace 实际速度超过软件速度上限；
- Trace 在设置时间内没有新帧；
- 轴状态机不是 4 或轴错误码非零；
- 达到最长运行时间；
- 读取实际转矩或控制卡 API 调用失败。

硬件位置限位启用时，`nmc_torque_move` 同时接收按运动方向计算的绝对限位；软件相对行程监测始终保留。运行中禁止直接改变转矩符号，因为硬件限位方向在启动时已经固定；反向前必须先停止并重新启动。

## 7. 首轮测试建议

- 脱离高负载机构或采用可靠机械限位；
- 目标转矩从额定转矩的很小比例开始；
- 命令限幅、行程、速度和最长运行时间取保守值；
- 先验证“立即停止”和“减速停止”；
- 再验证 `nmc_get_torque` 的正负号、比例和重复性；
- 最后测试同方向在线调整转矩。
