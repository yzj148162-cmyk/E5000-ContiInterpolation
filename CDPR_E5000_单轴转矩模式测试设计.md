# CDPR E5000 单轴转矩模式测试设计

## 1. 目标与边界

本页只用于验证单个 EtherCAT 伺服轴的转矩模式调用、实际转矩反馈和安全停止，不参与速度闭环、连续插补或多轴协同控制。

调用依据：

- 《雷赛 EtherCAT 控制卡转矩功能应用说明》中的 `nmc_torque_move`、`nmc_change_torque`、`nmc_get_torque` 与 `dmc_stop`；
- 《ISMC Diamond 系列伺服使用手册 V21》第 7.2 节的默认 EtherCAT PDO 映射；
- 原工程 `0523_final` 的转矩换算及独立监测逻辑。

## 2. 分层与线程

- UI 线程：采集参数、显示状态、以 20 Hz 左右批量刷新曲线；
- `ContiWorker` 控制线程：转矩测试状态机、Trace/转矩反馈监测和安全判定；
- `E5000HardwareInterface` 独占硬件线程：唯一允许调用 LTDMC 的边界。

目标转矩不会在每个监测周期重复下发：

- 启动时调用一次 `nmc_torque_move`；
- 用户点击“在线更新转矩”时调用一次 `nmc_change_torque`；
- 监测周期只读取 Trace 与 `nmc_get_torque`。

## 3. 单位换算

Diamond/CiA402 的 6071h 和 6077h 均以千分额定转矩表示。界面直接使用带符号的
额定转矩百分比，不再依赖额定转矩的 N·m 数值：

\[
TorqueRaw=\operatorname{round}(TorquePercent\times10)
\]

\[
TorquePercent=\frac{TorqueRaw}{10}
\]

例如 `+10%` 对应 `raw=+100`，`-10%` 对应 `raw=-100`，`100%` 对应
`raw=1000`。目标转矩、命令限幅、实际转矩保护和曲线均使用 `%`；正负号表示方向。

机构运动量只按减速器输出轴表示：

- 减速比：`44:1`；
- 减速器输出轴：`180224 pulse/圈`；
- 输出轴脉冲当量：`180224 / 360 = 500.622222 pulse/deg`。

电机侧编码器参数只参与上述传动参数的来源推导，不作为操作界面的控制单位。位置、速度、
行程限位和曲线均统一为减速器输出轴的角度及角速度。

## 4. 启动流程

1. 初始化控制卡并确认测试轴在线；
2. 使能测试轴；
3. 根据实际硬件需要，手动写入并读回最大电机速度 SDO 6080h；
4. 设置非零目标转矩百分比，确认命令限幅、实际转矩保护、最大行程、最大速度和最长运行时间；
5. 点击“开始转矩测试”；
6. 程序重配该轴 Trace，取得有效首帧；
7. 记录实际起点，根据转矩方向计算绝对位置限位；
8. 调用 `nmc_torque_move` 进入转矩模式；
9. 周期调用 `nmc_get_torque`，并监测实际转矩、位置、速度、轴状态和 Trace 新鲜度；
10. 停止时调用 `dmc_stop`，由控制卡使伺服返回位置模式。

程序不会自动使能轴，也不会在启动转矩测试时自动改写伺服 OD。

### 4.1 `nmc_torque_move` 的位置限位参数

未启用板卡硬件位置限位时，必须严格采用雷赛转矩功能说明和官方例程的特殊调用：

```cpp
nmc_torque_move(card, axis, torqueRaw, 0, 0x80, 0);
```

其中第五参数 `0x80` 不是实际限位位置，而是强制由位置模式切换到转矩模式的特殊值。

启用板卡硬件绝对位置限位时，才把实际限位位置传给第五参数：

```cpp
nmc_torque_move(card, axis, torqueRaw, 1, limitPositionCardUnit, 1);
```

正转矩要求限位位置大于当前位置，负转矩要求限位位置小于当前位置。程序运行日志必须输出
上述六个实际调用参数和返回码，以便确认特殊值或真实限位位置没有混用。

## 5. Diamond PDO 与最大速度 SDO

Diamond V21 手册给出的默认 EtherCAT 映射为：

- RxPDO `6071h:00h`：目标转矩，INT16；
- TxPDO `6077h:00h`：实际转矩，INT16。

`nmc_torque_move` 和 `nmc_change_torque` 通过 6071h 下发目标转矩，
`nmc_get_torque` 通过 6077h 读取实际转矩。程序不再使用
`nmc_read_rxpdo`/`nmc_read_txpdo` 执行额外预检查，也不据此阻止启动；目标转矩下发和实际
转矩读取是否有效，直接以 `nmc_torque_move`、`nmc_change_torque` 和
`nmc_get_torque` 的返回码为准。

原工程使用的 220Bh 属于 Servotronix 驱动器厂商对象，不适用于当前 ISMC Diamond，
已从本程序删除。6080h 仅作为可选的最大电机速度 SDO，通过
`nmc_set_node_od`/`nmc_get_node_od` 手动写入和读回；它不是周期 PDO，
也不会在转矩测试启动时自动修改。6080h 由驱动器直接作用于电机，因此必须填写电机侧
转速；当前默认采用电机额定转速 `5370 rpm`。按 `44:1` 减速比折算，其减速器输出轴上限为：

- `5370 / 44 = 122.045 rpm`；
- `122.045 × 6 = 732.273 deg/s`。

界面同时显示该输出轴等效值，但写入 6080h 的仍是 `5370 rpm`。最终仍须以当前 Diamond
对象字典和驱动器配置的单位定义为准。`180224` 是减速器输出轴每圈脉冲数，不是最大电机
速度，禁止将其写入 6080h。

## 6. 安全规则

以下任一条件触发停止：

- 目标转矩超过命令限幅；
- `nmc_get_torque` 换算后的实际转矩百分比超过反馈保护上限；
- 实际相对位移达到最大行程；
- Trace 实际速度超过软件速度上限；
- Trace 在设置时间内没有新帧；
- 轴状态机不是 4 或轴错误码非零；
- 达到最长运行时间；
- 读取实际转矩或控制卡 API 调用失败。

硬件位置限位启用时，`nmc_torque_move` 同时接收按运动方向计算的绝对限位；软件相对行程监测始终保留。运行中禁止直接改变转矩符号，因为硬件限位方向在启动时已经固定；反向前必须先停止并重新启动。

## 7. 首轮测试建议

- 脱离高负载机构或采用可靠机械限位；
- 目标转矩从很小的额定转矩百分比开始；
- 命令限幅、行程、速度和最长运行时间取保守值；
- 先验证“立即停止”和“减速停止”；
- 再验证 `nmc_get_torque` 的正负号、比例和重复性；
- 最后测试同方向在线调整转矩。
