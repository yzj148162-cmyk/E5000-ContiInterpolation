# CDPR-G302 六维力输入与实时控制改造设计

## 1. 文档目的

本文说明如何在 `CDPR-G302` 中保留现有末端遥控的八轴在线速度执行链，
把“人工方向输入产生末端运动”扩展为：

```text
模拟六维力 / 真实六维 F/T Trace
→ 力旋量校正与坐标变换
→ 纯惯性 Newmark-β 单步
→ 末端期望位姿、速度、加速度
→ 补偿运动学与八绳逆运动学
→ 八轴速度前馈 + 位置闭环修正
→ 同帧 Trace 安全校验
→ 八轴在线速度命令
```

本文既说明总体改造路线，也记录实际完成进度。阶段 A 的纯软件数学模块和
“只计算”验证入口已经写入 `CDPR-G302`；阶段 B 的模拟力八电机空载最小闭环
已经完成首版代码与界面接入，等待实机空载验收；阶段 C、D 尚未接入。

## 2. 已确认边界与待定参数

### 2.1 已确认

- 目标工程是 `CDPR-G302`，不是把它迁入 `cdpr_control`。
- 动力学模型采用纯惯性自由刚体，不加入回中刚度或人为阻尼。
- 实时力输入在一个模型周期内使用当前已取得样本，按 ZOH 保持；不得使用
  未来样本做无延迟 FOH。
- 首阶段可使用模拟六维力；真实传感器接入后由控制卡 Trace 取得六个通道。
- 当前样机的软件验证和后续力交互开发统一选择“控制界面”的 `Lite` 模板；
  阶段 A 在非 Lite 模板下拒绝启动，避免混用 G3 几何和边界。
- 八轴使用同一组 PID 参数，但各轴必须保存独立的积分、上次误差和上次命令
  状态，不能共用一份 PID 动态状态。
- 现有遥控链的同帧 Trace 校验、批量在线变速、会话令牌、停机邮箱及硬件线程
  串行访问应尽量保留。

### 2.2 必须保持“未配置”的项目

以下内容在实物尺寸或对象定义确定前不得填入猜测值：

1. 传感器正负方向约定 `sensorSign`；
2. 六个 F/T Trace 对象的对象类型、索引、子索引、从站号、数据长度、
   有符号性和编码方式；
3. 六个对象与 `Fx、Fy、Fz、Mx、My、Mz` 的实际通道对应关系。

传感器受力/测量参考点的位置已经实测：它位于正二十面体几何中心正上方
`325.48 mm`。以动平台局部系 `E` 的原点作为几何中心、`+Z_E` 表示正上方，
且传感器坐标系三轴与动平台局部坐标系三轴完全同向。代码采用：

```text
R_ES = I
r_ES_E = [0, 0, 0.32548]^T m
```

当前将该实测点作为传感器坐标系 `S` 的测量参考原点；若传感器手册给出的
六维力矩参考原点与该受力点不同，实物标定时必须按两点之间的位移修正。

配置文件中应使用 `null`、空数组或显式 `configured=false` 表示其余未知项目，
不能用索引 0 等“看似可运行”的默认值代替。已知的单位旋转和力臂不得再回退
为零矩阵或零位矢。真实 F/T 模式在任一必要项目未配置时必须保持不可启动；
模拟力模式不受这些未知项阻塞。

## 3. 六维力坐标链

### 3.1 坐标系

- `G`：CDPR 全局坐标系；
- `E`：动平台局部坐标系，原点位于动力学使用的动平台质心；
- `S`：六维力传感器坐标系，原点位于传感器测量参考点。

原始传感器力旋量定义为：

```text
W_S = [F_S; M_S] = [Fx, Fy, Fz, Mx, My, Mz]^T
```

在扣除本次运行零漂 `b_S` 并应用方向约定后：

```text
F_S' = sensorSign · (F_S - b_F)
M_S' = sensorSign · (M_S - b_M)
```

从传感器坐标系变换到动平台质心局部坐标系：

```text
F_E = R_ES · F_S'
M_E = R_ES · M_S' + r_ES_E × F_E
W_E = [F_E; M_E]
```

当前实测安装关系为 `R_ES=I`，所以 `F_E=F_S'`，传感器原点处力矩也无需
旋转；但从传感器原点平移到平台质心所产生的 `r_ES_E × F_E` 仍不能省略。

其中 `r_ES_E` 是“从动平台质心指向传感器原点”的向量，在 `E` 系表达。
交叉项不能省略；否则只要力的作用点不在质心，质心处力矩就会计算错误。

代入当前实测力臂 `r_ES_E=[0,0,0.32548]^T m`，若
`F_E=[F_x,F_y,F_z]^T`，则：

```text
r_ES_E x F_E = [-0.32548 F_y, 0.32548 F_x, 0]^T N*m
```

因此沿局部 `X/Y` 方向的力即使传感器自身力矩读数为零，也会在动平台质心处
分别产生 `+Y/-X` 方向的附加力矩；沿局部 `Z` 方向的力不产生该平移力矩。

若记录或显示需要全局力旋量，可使用当前动平台姿态 `R_GE`：

```text
F_G = R_GE · F_E
M_G = R_GE · M_E
```

当前 Newmark 模块直接接收质心处、平台局部坐标系表达的 `W_E`，但平动与
转动的内部坐标约定不同：平动力在动力学内部用 `R_GE` 转到全局系后求全局
线加速度；力矩保留在平台局部系，与局部惯量矩阵和局部角速度共同求角加速度。
因此不是“六个分量先全部转到全局系再求解”。`W_G` 主要用于诊断、绘图或
其他明确要求全局表达的算法，避免重复旋转和误用惯量坐标系。

上述关系与 MATLAB 文件
`MATLAB代码/CDPR_dynamics_solve/G302_current/transform_sensor_wrench_to_ee.m`
一致；其中现有 `fun_cdpr_params_G302.m` 的安装参数仅是历史计算值，不能直接
当作当前实物标定结果。

## 4. 数据接口与模块职责

### 4.1 公共数据帧

已在 `pose_control_module/forceinteractiontypes.h` 中建立不依赖 UI 和板卡 SDK
的公共数据类型，其职责等价于：

```cpp
struct FrameStamp {
    quint64 traceSequence;
    qint64 traceTimeUs;
    qint64 hostReceiveTimeUs;
    bool valid;
};

struct WrenchSample6D {
    FrameStamp stamp;
    std::array<double, 6> value;
    WrenchCoordinate coordinate;
    bool valid;
};

struct EndpointDesiredState {
    FrameStamp stamp;
    std::array<double, 6> pose;
    std::array<double, 6> twist;
    std::array<double, 6> acceleration;
    bool valid;
};
```

所有六维力、末端状态、八轴命令和反馈都携带同一周期身份，禁止把不同 Trace
帧或不同控制周期的数据拼成一帧。

### 4.2 力输入接口

已增加统一的 `IWrenchSource`，使动力学层不关心数据来自 UI 还是硬件：

- `SimulatedWrenchSource`：常值、脉冲、正弦或预编译公式；
- `TraceFtWrenchSource`：接收硬件层解码后的六通道同帧数据；
- 后续若增加文件回放，只新增输入实现，不改 Newmark 和控制器。

模拟输入与未来真实 F/T Trace 采用同一语义：均表示传感器测量参考点、传感器
坐标系 `S` 下的原始六维力旋量。两者都必须经过同一个 `WrenchTransformer`，
使用唯一的实测安装关系 `R_ES=I`、`r_ES_E=[0,0,0.32548]^T m`，再把质心处
平台局部力旋量送入 Newmark。不得再保留“模拟量已经位于质心、可以绕过安装
变换”的第二条路径。

阶段 A 提供“仅平动调试”开关。该开关仍先执行完整安装变换和 `r×F` 计算，
随后才将质心处 `Mx/My/Mz` 清零；它只用于前期隔离转动因素，不能代表正式
六自由度模型。

### 4.3 安装变换模块

已增加独立 `WrenchTransformer`：

- 输入：去零漂后的 `W_S`、实测 `R_ES=I`、实测
  `r_ES_E=[0,0,0.32548]^T m`、`sensorSign`；
- 输出：质心处平台局部力旋量 `W_E`，可选输出 `W_G`；
- 只做确定性的数学变换，不读取 UI、Trace 或全局变量；
- 配置无效时返回明确错误，不输出全零有效帧。

可参考 `cdpr_control/cdpr/CdprForceInput.*` 的数据契约，但迁移到
`CDPR-G302` 时应重新适配命名空间、配置体系和现有线程模型，不能直接让两个
工程相互包含源文件。

### 4.4 Newmark-β 模块

已增加独立、可单元测试的纯惯性 `CdprDynamics`：

- 输入：当前有效 `W_E`、固定模型步长 `T_c`；
- 状态：末端位姿、速度、加速度；
- 输出：下一控制格点的 `EndpointDesiredState`；
- `β=0.25、γ=0.5`，不加入回中刚度；
- 每次启动由可信初始位姿重置，运行过程中只有控制线程能够修改内部状态。

可审查并迁移 `cdpr_control/cdpr/CdprDynamics.*`，同时保留其静止、恒力、
恒力矩、姿态奇异性和不收敛测试。

## 5. 对现有遥控架构的改造方式

### 5.1 保留的部分

保留以下已经验证过的机制：

- `ControlWorker` 的会话互斥、停机邮箱和调度入口；
- HardwareThread 对所有雷赛 API 的独占串行访问；
- 同一硬件任务内“读 Trace → 校验 → 下发八轴速度”的复合操作；
- 首次非零速度使用 `dmc_vmove`，运行期使用
  `dmc_change_speed_unit`；
- 八轴统一限速比例、加速度约束、软件行程和状态字检查；
- 命令成功后才提交运动学候选状态的事务式处理。

### 5.2 不直接复用的部分

`EndpointRemoteInputSupervisor` 的方向按键、50 ms 心跳和 X56 轮询只属于人工
遥控，不能放进六维力控制周期。`EndpointRemoteControl` 当前还把方向语义、
末端速度生成、边界检查、运动学和命令规划集中在一个类中，且主要是速度前馈，
不应直接改成读取六维力。

应新增 `EndpointForceInteractionControl`，并逐步抽取一个遥控和力交互共同使用
的八轴执行层：

```text
末端期望状态提供者
  ├─ 遥控提供者：方向 → 末端速度/位姿
  └─ 力交互提供者：六维力 → Newmark → 末端状态
                ↓
共同的边界/补偿运动学/八轴闭环/命令提交层
                ↓
HardwareInterface
```

首轮实现可以不立即重写旧遥控类，但六维力控制不得复制一套新的雷赛 API 调用
链；应调用同一个批量在线速度硬件入口。

### 5.3 八轴闭环

Newmark 输出经过补偿运动学得到八轴期望位置和速度。每轴命令为：

```text
v_cmd,i = Kff · v_ref,i
          + Kp · e_i
          + Ki · integral(e_i)
          + Kd · derivative(e_i)
```

首版控制误差使用本控制步新计算的参考位置减去最新可靠 Trace 实际位置；这是
控制器当时真实可获得的因果反馈，包含采样和执行链延迟。后处理可按 Trace 时间
计算延迟对齐诊断误差，但不得用“未来反馈”替换在线控制输入。八轴共享一组
`Kff/Kp/Ki/Kd` 与限幅参数，但保留八份积分和微分状态。轴速超过执行能力时仍
按共同倍率进行硬限幅，不能逐轴随意截断而破坏八绳协同；不对每周期速度命令
增量施加八轴公共比例加速度限制，以免改变 Newmark 纯惯性模型的时间响应和
等效质量。在线变速时间仅保留为板卡接口参数，并应设置为不大于一个控制周期。

### 5.4 虚拟 CDPR 运动学一致性

阶段 B 中八个电机尚未连接绞盘和绳索，不能用实物平台验证运动，因此增加一条
只用于诊断的“虚拟 CDPR”链：

```text
固定初始位姿 q0
→ 逆运动学得到初始绳长 L0
→ 启动时锁存八轴 Trace 初始位置 θ0
→ Trace 实际电机位置 θact
→ 按临时方向、传动和绞盘模型换算虚拟实际绳长 Lact
→ ForwardKinematicsSolver
→ 虚拟实际末端位姿 qvirtual
```

比较量包括：

- Newmark 期望位姿与 `qvirtual` 的平移、姿态误差；
- 期望绳长与虚拟实际绳长误差；
- 八轴期望位置与 Trace 实际位置误差；
- 正运动学方程残差、有效方程数和收敛状态。

工程已经具备 `CompensatedCableKinematics` 和独立的
`ForwardKinematicsSolver`。改造时应先用现有模板参数完成逆运动学/正运动学
往返校验，再直接复用，不能在力交互控制器中另写一份运动学算法。

高频控制线程只完成控制所需的逆运动学、八轴闭环和轻量有效性检查。正运动学
求解不作为速度命令下发的前置条件：完整 Trace 数据进入异步记录队列，UI线程
按 5 Hz 使用现有 `ForwardKinematicsSolver` 计算虚拟位姿。CSV 已保存未来全量
离线复算所需字段，但首版尚未在会话结束时自动执行全量一致性分析。这样既能
检查空转状态下的数学/映射一致性，又不会让迭代正运动学破坏控制周期。

虚拟一致性良好只能说明软件数学链、轴映射、命令和反馈基本一致，不能证明真实
平台的绳索弹性、张力、摩擦、绞盘排绳和结构变形满足要求。

## 6. Trace 对象与硬件层改造

### 6.1 独立使用语义

阶段 B 首版复用现有在线速度 Runtime Trace 配置，读取八轴实际位置和实际速度，
并通过既有八轴批量速度入口下发命令，不新增第二套板卡 API 链。阶段 C 接入真实
F/T 时，再在 `HardwareInterface::RuntimeTraceUsageProfile` 中增加：

- `ForceInteractionTransition`；
- `ForceInteractionRunning`。

不能沿用目前的 `EndpointRemoteRunning` 后再临时打开旧“力传感器”开关。现有
G302/Lite force-sensor map 主要服务绳索张力通道，并非动平台六维 F/T，二者
必须使用不同的数据结构和配置名称。

### 6.2 六个对象描述符

增加 `FtWrenchTraceObjectDescriptor[6]`，每项至少包含：

- 物理分量；
- Trace 数据类型；
- 对象索引与子索引；
- 从站号；
- API 数据长度与帧内实际字节数；
- 数值编码/有符号性；
- 是否已配置。

这六项初始全部为空。后续用户提供对象索引后再填写并做 Trace 配置读回、帧宽
核对和静止数据验证。首版约定解码结果已经是 N 和 N·m；若实物对象并非工程
单位，必须新增显式比例配置，不能在解码函数中藏常数。

### 6.3 实时帧最小化

力交互运行帧只保留控制和安全必需对象，建议首版为：

- 8 个实际位置；
- 8 个实际速度；
- 8 个状态字；
- 6 个 F/T 通道。

合计 30 个对象。板卡指令位置、指令速度和反馈转矩若只用于观察，应由实测证明
周期预算充足后再加入，不能默认沿用遥控 profile 的全部对象。主机已经保存本次
计算出的参考位置和下发速度，不必为了日志重复从 Trace 读取同义数据。

六个 F/T 值和八轴反馈必须来自同一个完整 Trace 帧，并共享逻辑帧序号。若板卡
对象上限或帧宽不允许 30 个对象，应先停止开发并重新选择信号集合，不能拆成两次
独立读取后伪装成同帧。

### 6.4 复合硬件任务

将当前
`readRuntimeTraceAndMotorVelBatchFastEndpointRemote()` 的通用部分抽为内部复合
任务：

```text
读取最新完整 Trace 批次
→ 验证 profile / 会话 / 帧序号 / FIFO / 新鲜度 / 状态字
→ 解码 8 轴反馈与 6 维力
→ 校验准备好的速度命令尚未过期
→ 一次批量执行 8 轴在线速度命令
```

遥控和力交互分别使用各自的会话令牌及准入 profile，避免一个模式错误接受另一
模式的缓存帧或待提交命令。

## 7. 周期和时间对齐

### 7.1 三种时间必须区分

- `T_bus`：通过 `nmc_get_cycletime()` 读回的 EtherCAT 总线周期；
- `T_trace`：`T_bus × dmc_trace_get_config()` 读回倍数；当前目标配置为
  500 μs，但代码不得用字面量 500 代替读回值；
- `T_c`：六维力动力学、逆运动学、八轴 PID 和命令更新的总体控制周期。

“Trace 与总体控制周期对齐”定义为：

```text
T_c = N · T_trace，N 为正整数
```

二者不要求数值相等。若 `T_trace=500 μs`，则 1、2、5、10、20 ms 控制周期
分别对应 2、4、10、20、40 个 Trace 帧。启动前必须按实际读回值校验整除关系。

### 7.2 时间主线

延续现有末端遥控架构，使用两条职责不同、但能够互相映射的时间线：

- **主机单调时钟**是控制调度主线：决定 Newmark、运动学、八轴 PID 和命令
  下发何时到期；
- **Trace 逻辑序号**是传感器与电机反馈采样主线：用于确认新帧、检测丢帧、
  防止重复消费，并保证六维力和八轴反馈来自同一帧。

`HardwareInterface` 已把板卡 Trace 逻辑序号按读回的采样周期映射到主机单调
时间，并在 `RuntimeTraceSnapshot` 中提供帧序号、帧龄、完整性和映射时间。阶段 B
首版直接消费该快照，不在上层重复猜测帧宽或重新构造时间戳。启动过程为：

1. 切换力交互 Trace profile 后清空 FIFO；
2. 取得第一帧可靠同帧数据，建立
   `(traceAnchorSequence, hostAnchorMonotonicUs)`；
3. 主机控制格点按
   `hostDueTime(k) = hostControlStartUs + k·T_c` 推进；
4. Trace 帧的逻辑采样时刻按下式映射到同一主机单调时间域：

   ```text
   traceTime(seq) = hostAnchorMonotonicUs
                  + (seq-traceAnchorSequence)·T_trace
   ```

5. 每个到期控制周期消费最新可靠的完整 Trace 数据，用逻辑序号检查该帧是否
   更新，并按映射时间对齐六维力、八轴反馈和期望状态；
6. 力样本 `W_k` 在对应模型区间内按 ZOH 保持，不查询或插值尚未取得的未来
   Trace 样本。

Trace 序号不直接决定“何时调用速度 API”，也不替代主机周期调度。主机定时器
只负责唤醒，真正的迟到量由当前单调时间与 `hostDueTime(k)` 比较得到，不能用
“回调次数 × 设置周期”伪造已经流逝的时间。

### 7.3 实际执行周期超出预设周期

每周期同时记录：

- 计划控制格点；
- 实际进入控制线程的主机时间；
- 最新 Trace 序号与帧龄；
- 本周期经过的 Trace 帧数；
- Trace 读取、Newmark、运动学、PID、API 和完整周期耗时。

当实际回调晚到时，先由主机单调时钟计算调度迟到量和漏掉的控制格点数，再用
Trace 序号核对这段时间内反馈是否正常产生。阶段 B 首版采用保守实现：每次只
推进一个固定 `T_c` 的 Newmark 步、只提交一组当前命令，并累计漏周期计数；不会
突发补发过期硬件命令。历史力样本补推进留到测得确有需要后再实现：

- 尚未到达下一主机控制格点：不重复推进 Newmark；
- 正常到期且取得新鲜同帧 Trace：以固定 `T_c` 推进一步并下发一组速度；
- 晚到一个控制格点：可使用短缓存中该历史区间已经取得的 F/T 样本，以固定
  `T_c` 补推进模型；只向硬件提交当前时刻的一组最新速度，不补发已经过期的
  中间命令；
- 连续漏掉多个命令格点、Trace 缓存不足、序号异常或帧龄超限：安全停止。

Newmark 步长始终是冻结的总体控制周期 `T_c`。不能因为某次 Windows/Qt 调度
迟到，就把一个很大的实际回调间隔直接作为单次 Newmark 步长；也不能在恢复后
连续突发下发多组过期速度。短小的同帧 Trace 环形缓存只用于一次受限的软件状态
补推进和诊断，不改变“当前已取得力样本按 ZOH 使用”的因果关系。

### 7.4 消除 500 μs 硬编码的范围

修改时应替换遥控准入代码中类似
`traceSamplePeriodUs == 500` 的字面量判断，改为与本次会话读回并冻结的
`expectedTraceSamplePeriodUs` 比较。这不是要求改变板卡当前 500 μs 设置，而是
避免代码把配置值、实测值和控制周期混为一谈。

## 8. 真实 F/T 的预热与零漂

真实模式从 Trace profile 配置成功后即可持续采样、显示和异步记录，但在零漂
确认前不得送入 Newmark。状态建议为：

```text
未配置 → Trace待验证 → 预热采集中 → 候选稳定
       → 零漂已确认 → 力交互就绪
```

- 自动稳定判定使用最近一段窗口的均值、标准差、均值变化率、峰峰值和有效率；
- 阈值由后续传感器静止实测数据确定，不在当前版本中猜测；
- 人工“确认当前零漂”取稳定窗口均值，不能取按钮瞬间单帧；
- 零漂在运行期间冻结，停止或重新配置 Trace 后重新确认；
- 零漂属于本次运行状态，写入记录文件但不自动覆盖固定安装配置。

## 9. UI 调整方案

现已在 `mainwindow.ui` 中增加独立“六维力交互”页，以及“软件验证（阶段A）”
和“电机空转（阶段B）”子页；固定控件、分组和布局均保留在 `.ui`，C++ 只负责
参数读取、控制编排和状态刷新。后续页面继续按下列职责扩展，不把控件塞入
遥控页：

1. **输入**：模拟/真实选择，六分量常值、脉冲、正弦或公式；
2. **传感器**：六通道 Trace 映射状态、预热状态、稳定窗口、确认/清除零漂；
3. **安装**：`R_ES`、`r_ES_E`、方向约定及“未配置”提示；
4. **动力学与控制**：总体控制周期、质量惯量摘要、Newmark 参数、共享 PID
   参数和限幅；
5. **状态**：当前原始力、去零漂力、质心局部力、全局力、期望末端状态、
   Trace 帧龄、实际周期和保护状态；
6. **操作**：准备、启动、正常停止、急停。

页面还应提供“只计算”和“电机空转”两个明确模式：

- **只计算**：运行模拟力、Newmark、运动学和虚拟反馈，不调用雷赛运动 API；
- **电机空转**：使用真实八轴 Trace 反馈并下发八轴速度，但仍明确标注为虚拟
  CDPR 验证，不显示为实物平台控制已就绪。

安装参数和 Trace 对象可放入单独配置对话框，主页面只显示“未配置/已验证”和
摘要。运行期间冻结所有影响实时链的配置。阶段 A 页面只读显示当前实测
`r_ES` 和 `R_ES`，并明确模拟量按传感器原始值进入同一安装变换链。

## 10. 预计修改的源文件

| 位置 | 计划改动 |
|---|---|
| `pose_control_module/forceinteractiontypes.h` | 已完成公共帧、力旋量和末端状态 |
| `pose_control_module/wrenchsource.*` | 已完成常值、脉冲、正弦和公式模拟输入；真实 Trace 与零漂留待阶段 C |
| `pose_control_module/wrenchtransformer.*` | 已完成 `S → E质心` 的确定性力旋量变换及未配置拒绝逻辑 |
| `pose_control_module/cdprdynamics.*` | 已完成纯惯性 Newmark-β 单步及解析解自检 |
| `pose_control_module/forceinteractionsoftwarevalidator.*` | 已完成阶段 A 后台验证编排和逐步记录接入，不调用雷赛运动 API |
| `pose_control_module/forceinteractionrunrecorder.*` | 六维力交互阶段 A～D 专用的有界异步 CSV 记录器；当前先由阶段 A 使用 |
| `pose_control_module/forceinteractionruntimecontrol.*` | 已完成阶段 B 周期状态机、Newmark、运动学、八轴闭环、保护和记录 |
| `pose_control_module/compensatedcablekinematics.*` | 复用现有补偿逆运动学，必要时只补公共数据适配与测试 |
| `pose_control_module/forwardkinematicssolver.*` | 复用现有正运动学，供低频虚拟位姿和离线一致性分析 |
| `pose_control_module/virtualcdprobserver.*` | 暂不新增；首版复用主窗口现有绳长换算和 `ForwardKinematicsSolver`，后续规模扩大时再抽取 |
| `pose_control_module/controlworker.*` | 已接入会话互斥、主机格点调度、Trace消费、批量速度命令、停机与状态发布 |
| `embedded_module/hardwareinterface.*` | 六维 F/T 对象描述符、新 Trace profile、同帧解码和读回校验 |
| `embedded_module/hardwareinterface_jogfast.cpp` | 抽取遥控/力交互共用的批量速度复合任务 |
| `control_interface_module/mainwindow.*`、`mainwindow.ui` | 已接入阶段 A/B 页面、配置冻结、准备/启动/停止、5 Hz虚拟正运动学和结果显示 |
| 工程文件与测试目录 | 注册新增源文件和数学/时序单元测试 |

命名可在动工前按现有工程风格调整，但职责边界不应改变。

## 11. 运行记录

异步记录至少包括：

- Trace 序号、Trace 逻辑时间、主机接收和控制执行时间；
- 六通道原始值、零漂、去零漂值、`W_E`、`W_G`；
- Newmark 期望位姿、速度、加速度、迭代次数和残差；
- 八轴参考位置/速度、PID 修正、最终速度命令；
- 八轴同帧实际位置/速度和状态字；
- 各阶段耗时、漏格点数、帧龄、停机原因；
- 本次安装变换、Trace 对象定义、动力学和控制参数快照。

本次采用增量方案，不重构现有 UI 事件日志、故障日志、`SessionRecorder` 和
在线速度 CSV。新增的 `ForceInteractionRunRecorder` 只服务六维力交互阶段
A～D，使用固定字段和可用性掩码：阶段 A 尚不存在的八轴指令、Trace 等字段
保留为空语义，后续阶段直接填充，不再更换文件格式。

计算/实时线程只用 `tryLock` 向固定容量环形队列复制一条定长记录，争用或队列
满时立即丢弃并计数；低优先级记录线程每批最多写 128 行 CSV。UI 只显示开始、
结束、文件路径、接受/写入/丢弃数和关键异常，不逐点刷新文本或曲线。阶段 A
的逐步文件保存于 `data/outputmsg/force_interaction_runs/`，每个 Newmark 步记录：

- 模拟传感器原始力旋量、变换后的平台质心力旋量；
- 期望位姿、速度、加速度；
- 八绳长度、相对电机角；
- 正运动学回算位姿、三类往返误差和越界标志；
- Newmark 迭代/残差及本步计算耗时。

阶段 A 的计算循环运行快于真实时间，因此队列容量暂设为 65536 条；任何丢弃
都会写入最终摘要，不能把存在丢弃的文件当作“完整逐步记录”。阶段 B～D 接入
时复用同一记录器，并补齐八轴参考、PID 修正、速度命令、Trace 反馈和硬件 API
耗时字段。公共写盘内核是否抽取，等力交互记录链稳定后再决定。

## 12. 分阶段实施与验收

### 阶段 A：纯软件数学链

**代码任务**

- 建立公共帧类型、`IWrenchSource`、模拟力源、`WrenchTransformer` 和纯惯性
  `CdprDynamics`；
- 模拟力按传感器参考点、传感器坐标系原始值输入，并与未来真实 Trace 共用
  已知的 `R_ES=I` 和 `r_ES_E=[0,0,0.32548]^T m` 安装变换；
- 接入现有 `CompensatedCableKinematics` 和 `ForwardKinematicsSolver`；
- 建立“只计算”入口，全阶段不连接雷赛运动 API。

**测试方法**

- 零力、恒力、恒力矩、正弦力和六维组合输入；
- 检查位姿、速度、加速度连续性和有限性；
- 对相同初始条件、力输入和步长，与 MATLAB/Newmark 逐步对照；
- 对每一个 Newmark 时间步执行“逆运动学 → 绳长 → 正运动学”往返校验，
  不再按固定数量抽检。

**通过条件**

- 零力状态无非预期漂移，恒力/恒力矩方向和量纲正确；
- C++ 与 MATLAB 的差异处于预先记录的数值容差内；
- 全部时间步的运动学往返均收敛，误差和残差满足模板对应阈值；
- 无未配置真实 F/T 参数被默认值悄悄替代。

**当前落地状态**

- 已实现公共数据契约、四类模拟力源、公式解析与奈奎斯特校验；
- 已实现传感器力旋量到平台质心局部系的独立变换模块；实测安装关系已写入
  `R_ES=I`、`r_ES_E=[0,0,0.32548]^T m`。阶段 A 已冻结并启用这部分已知安装
  参数，使模拟量正式循环与核心自检共用同一变换；真实 Trace 通道和方向仍
  保持未配置，不能据此宣称真实 F/T 已可用；
- 已实现纯惯性自由刚体 Newmark-β 单步，内部统一使用 SI，未加入回中刚度和
  人为阻尼；
- 已复用 `CompensatedCableKinematics` 与 `ForwardKinematicsSolver`，验证器按
  当前界面的结构参数、质量惯量和程序起点，对每个 Newmark 步执行逆/正
  运动学往返校验；
- 已在后台线程执行验证并支持取消，验证期间不会使能轴、不会调用雷赛运动
  API，也不会占用现有硬件线程；阶段 A 仅允许 Lite 模板，结果会记录模板、
  初始位姿、安装参数、首次越界/误差超限步骤；摘要保存到
  `data/outputmsg/stage_a_validation/`，每步完整轨迹由六维力交互专用异步记录器
  保存到 `data/outputmsg/force_interaction_runs/`；
- 已通过 Qt 6.8.3/MSVC 2022 Debug 全量构建和无界面启动检查。

当前“代码已落地”不等于阶段 A 已验收：仍需从新页面使用当前模板运行多组
输入，并将 C++ Newmark 结果与 MATLAB 相同初值、相同步长逐步对照后，才能
记录为阶段 A 验收通过。

### 阶段 B：八电机空载最小闭环

**代码任务**

- 新增力交互控制会话，与遥控、PVT 等运动模式互斥；
- 固定已知初始位姿，锁存八轴 Trace 初始位置；
- 接通“模拟低幅六维力 → Newmark → 运动学 → 八轴位置闭环速度命令”；
- 复用硬件线程中的同帧 Trace 读取和八轴批量在线变速入口；
- 接入虚拟绳长、后台正运动学、异步记录和运行后全量一致性分析。

**调试顺序**

1. 零力启动，确认八轴速度命令保持为零；
2. 只施加单一平移方向的小幅力；
3. 只施加单一方向的小幅力矩；
4. 再测试六维组合输入和较长时间运行；
5. 每一步都验证正常停止、急停、Trace 超时和单轴故障引发整组停止。

**通过条件**

- 零力无非预期运动，八轴命令有限且不超过冻结限值；
- 八轴共用参数、独立 PID 状态，共同限幅不破坏相对速度关系；
- Trace 无重复消费，帧龄、丢帧、API 耗时和控制超期均完整记录；
- 虚拟位姿、绳长和八轴跟随误差能够在线低频查看并离线全量复算；
- 运行结果只能标记为“空载虚拟一致性通过”，不能标记为真实平台验收通过。

**当前落地状态**

- 已增加 `.ui` 固定子页“电机空转（阶段B）”，共享阶段 A 的模拟六维力输入；
- 已增加 `ForceInteractionRuntimeControl`，按冻结控制周期执行模拟力、安装变换、
  纯惯性 Newmark、补偿逆运动学、八轴速度前馈和八份独立 PID 状态；
- 八轴速度和加速度采用共同倍率限幅，保留协同方向；启动前与启动瞬间均复核
  八轴配置/使能及运动模式互锁；
- 运行只接受新鲜、完整、时序可靠且 FIFO 已追平的 Trace，新帧超时、周期不整除、
  位姿/电机位置越界、跟随误差或速度 API 失败均触发整组停止；
- 复用现有 Runtime Trace 与八轴批量在线速度入口；每周期数据进入六维力交互
  专用有界异步 CSV，UI 以 5 Hz 计算并显示虚拟实际位姿；
- 已通过 Qt 6.8.3/MSVC 2022 Release 全量构建，尚未完成八台空载电机验收，
  因此状态只能写作“首版代码已完成，待阶段 B 实测”。

### 阶段 C：真实 F/T 与八电机空载联调

**代码任务**

- 用户提供六个 Trace 对象定义后，建立专用 ForceInteraction profile；
- 将六维 F/T 与八轴反馈放在同一完整 Trace 帧中解码；
- 实现预热、稳定候选、自动/人工零漂确认和配置冻结；
- 本阶段先保持只读；只读验收通过后，才开放低增益、低限速的八电机空载
  子试验。

**测试方法与通过条件**

- 静止采集确定噪声、均值漂移、峰峰值和稳定阈值；
- 施加已知方向的力，核对通道顺序、符号、比例和单位；
- 使用已知力臂检查力矩和平移力产生的交叉项；
- Trace 帧宽、序号、有效率和帧龄通过后，才允许真实力输入驱动空载电机；
- 空载子试验重复阶段 B 的虚拟运动学一致性分析，确认换成真实力源后控制周期、
  停机和八轴映射没有退化。

### 阶段 D：接入绞盘、绳索与真实平台

- 复核 `R_ES=I` 的装配方向，完成传感器方向和八轴收放绳方向标定，并复核当前实测
  `r_ES_E=[0,0,0.32548]^T m` 对应传感器六维力矩的实际参考原点；
- 逐轴校验电机角度、传动比、绞盘运动和绳长变化的对应关系；
- 接入相对初始位置的 ±6.5 圈行程、张力下限、松绳和统一停机保护；
- 先低速度、低加速度、小外力、小工作区运行，再逐步扩大范围；
- 后续接入 Nokov 后，同时比较 Newmark 期望位姿、绳长正运动学位姿和动捕
  实测位姿。只有动捕和实物安全结果也通过，才可宣称真实平台闭环有效。

## 13. 当前实现状态与进入条件

截至本文本次修订：

| 项目 | 状态 | 说明 |
|---|---|---|
| 原末端遥控八轴在线速度链 | 已存在 | 作为硬件执行架构参考，修改前先建立回归基线 |
| `CompensatedCableKinematics` | 阶段 A 已复用 | 已接入往返验证；阶段 B 再接实时候选状态提交 |
| `ForwardKinematicsSolver` | 阶段 A 已复用 | 当前用于后台软件验证，后续仍不放入高频命令前置路径 |
| 模拟六维力接口 | 阶段 A 已实现 | 支持常值、脉冲、正弦和六分量公式；按传感器原始六维力进入统一安装变换 |
| 纯惯性 Newmark-β | 阶段 A 已实现 | 已有解析解自检，仍需完成 MATLAB 逐步对照验收 |
| “只计算”页面 | 阶段 A 已实现 | 固定布局位于 `.ui`，仅允许 Lite；支持仅平动调试、详细结果落盘且不调用硬件运动 API |
| 八轴力交互控制会话 | 首版已实现 | 模拟力、Newmark、八轴速度闭环、同帧Trace、异步记录和5 Hz虚拟正运动学；待空载实测 |
| 六维 F/T Trace | 未配置 | 缺少六个对象定义，禁止真实输入启动 |
| 传感器安装变换 | 安装几何已知 | `R_ES=I`，力臂为局部 `+Z 325.48 mm`；方向和通道未定，禁止真实输入启动 |
| 绞盘/绳索实机力交互 | 未开始 | 仅在阶段 A～C 验收后进入 |

阶段按顺序设置验收门。某阶段没有形成可复现的测试记录和明确通过结论时，不得
仅因界面或接口已经存在而进入下一阶段。

## 14. 后续实物阶段仍需用户提供的信息

真实 F/T 接入前需要：

1. 实机复核传感器三轴与平台局部三轴同向，即 `R_ES=I`；
2. 确认实测 `r_ES_E=[0,0,0.32548]^T m` 的端点就是传感器输出力矩所采用的
   测量参考原点；
3. 传感器输出正负方向定义；
4. 六个 Trace 对象的完整描述和分量顺序；
5. 六通道数据编码及单位确认；
6. 静止预热数据，用于确定稳定判定阈值。

这些信息缺失不妨碍阶段 A、B，但必须阻止阶段 C、D 被误判为“已就绪”。
