# CDPR-G302 六维力交互工作空间边界与安全停车设计

## 1. 目标与适用范围

本设计面向最终六维力传感器、八轴驱动、绞盘和绳索全部接入后的实机交互实验，
阶段 A 的纯数学验证和阶段 B 的电机空转只是同一控制链的前置验收步骤。

正常运行区内必须保留“力旋量 → Newmark-β → 末端状态 → 逆运动学 → 八轴速度
闭环”的纯惯性响应，不允许用八轴公共加速度倍率改变等效动力学。一旦安全逻辑
介入，本次微重力交互试验立即判为失败，后续命令只负责安全停车。

## 2. 一套物理边界

ACC 和 G302 各自只有一套全局物理工作空间，数据来自所选机器模板。正运动学
搜索、阶段 A 软件验证、阶段 B 空转、真实传感器空转和最终收放绳实验均引用该
配置，不再建立“阶段 B 边界”或其他平行边界。

物理判据不是只检查动平台质心。对动平台局部坐标系中的全部八个绳索连接点执行
ZYX 姿态变换和平移，要求每个连接点的全局坐标都位于机架包围盒内：

```text
X ∈ [-frameLength/2, frameLength/2]
Y ∈ [-frameWidth /2, frameWidth /2]
Z ∈ [template.workspaceZMin, frameHeight]
```

G302 当前模板的质心数值搜索原始范围为 X/Y=±1400 mm、Z=-100～2670 mm；
物理验收还会检查旋转后的八个连接点。因此平台几何尺寸本身已经形成质心安全余量，不再为正运动学搜索范围额外增加
数值容差。正运动学求解器的质心搜索上下界也从同一机架尺寸生成；求解结果还要
再次经过八连接点物理判定，搜索成功不等于物理安全。

因此，过去仅按质心范围合法的初始位姿现在也可能被拒绝。例如 G302 平台局部点
在 Z 方向约为 ±123.87 mm；质心位于 -56 mm 时，下侧连接点会低于 -100 mm
物理边界。阶段 A、阶段 B 和轨迹准备均应在运动前直接报告此类非法初态，不能
等到电机开始运动后再处理。

姿态角的工程限制属于同一机器模板，但与“平台边缘不得越出机架”的几何判定
分别记录触发原因。恢复完整六自由度实验前必须给出可验证的姿态限制。

## 3. 动态停车距离

不单独建立“总响应延迟”模型。对每个连接点和六个机架边界面，取其朝向该边界
的法向速度 `v_out`，停车触发距离采用：

\[
d_{trigger}=\frac{v_{out}^2}{2a}+d_{margin}
\]

- `a`：用户设定并经空载、加载试验验证的末端平动加速度上限；同一数值也作为安全停车减速度；
- `d_margin`：覆盖离散周期、Trace/驱动响应、跟踪误差和模型误差的保守附加余量；
- 不使用速度模长代替法向速度，也不写入 `v·T_delay` 项。

完整六自由度下，连接点速度由质心线速度和转动共同决定：

\[
v_i=v_c+\omega\times(Rb_i)
\]

这样即使质心未靠近边界，平台转动使某个连接点接近机架时也能正确触发保护。

## 4. 边界状态与判据

从安全区向外只保留三个有明确动作的层次：

```text
正常区 → 动态制动触发区 → 固定急停线 → 物理边界
```

1. 正常区：继续使用当前力样本推进动力学。
2. 动态制动触发区：剩余距离不大于 `d_trigger`，且下一步仍朝外加速、维持危险
   速度，或已经没有足够纯制动距离时，冻结力输入并进入协同受控制动。
3. 固定急停线：距物理边界的固定小距离，用于受控制动失效的最后保护；到达后
   立即停止并锁存故障。
4. 物理越界：最高等级故障，立即停止、禁止自动恢复。

若当前法向加速度明确减小朝外速度且纯制动距离仍充足，可继续本周期力响应；若
剩余距离已经小于 `v_out²/(2a)`，即使当前正在减速也必须接管停车。

所有触发均锁存到本次运行结束。回到安全区不能自动恢复力交互，必须由操作者
检查原因后重新准备。

## 5. 协同受控制动

受控制动在末端空间生成，不逐轴独立截断。以配置的 `a` 沿当前末端速度反向
减速，每周期得到新的末端位姿和速度，再统一经过逆运动学和八轴速度闭环下发。
这样可以保持八绳几何协调关系。

模拟力达到设定时长、用户点击“减速停止”和动态边界触发均进入该状态。真实
F/T 输入没有固定运行时长，只能由用户停止或安全事件结束。速度降至停车阈值后
下发零速并调用正常停止；受控制动期间若出现 Trace 失效、轴故障、绞盘行程越界、
固定急停线触发或命令提交失败，立即升级为急停。

## 6. 分阶段实施与验收

### 第一步：统一配置与纯数学边界

- 建立独立物理边界模块；
- ACC/G302 模板提供机架尺寸、平台连接点和姿态限制；
- 正运动学、阶段 A、阶段 B 共用该模块；
- 使用中心、接近六个面、平台转动、恰好触边和越界算例验证。

### 第二步：阶段 B 动态判定与协同停车

- UI 增加一个同时用于响应上限和停车减速度的末端加速度 `a`，以及附加余量和固定急停线距离；
- 正常力响应检查加速度配置，但不做公共比例缩放；
- 实现运行、制动、停止、故障锁存状态机；
- 以模拟力和八电机空转验证准时停车及八轴协同。

### 第三步：通用安全监控与日志闭环

- SafetyMonitor 接收同一物理边界配置和最新有效末端状态；
- 逐周期异步记录连接点最小余量、限制面、法向速度、停车距离、状态转换、八轴
  指令/Trace、API与整周期耗时；
- UI 只以低频显示当前最小余量和首次锁存原因；
- 提供无需硬件的边界回放以及阶段 B 日志离线复算。

### 第四步：真实 F/T 空转

- 配置六个 Trace 对象、预热稳定判定、自动/手动置零和通道有效性；
- 将真实力源接到与模拟力相同的坐标变换接口；
- 传感器安装关系保持 `r_ES=[0,0,0.32548] m`、`R_ES=I`，通道符号待实测确认；
- 不修改下游动力学、边界和八轴执行层。

### 第五步：最终实机收放绳

- 接入每轴相对初始位置 ±6.5 绞盘圈行程、速度、状态字、张力上下限和断绳保护；
- 低频正运动学/动捕只用于实际位姿监测，不进入高频命令生成路径；
- 依次完成空载、小力、小范围、逐步加载和完整交互验收。

## 7. 记录字段与通过条件

每个控制周期至少记录：力源类型和力旋量、Newmark 期望状态、八连接点全局坐标、
最小物理余量、限制点/限制面、法向速度/加速度、纯停车距离、附加余量、安全状态、
首次触发原因、八轴参考位置/速度、PID修正、最终命令、Trace反馈及耗时。

CSV 必须明确标出“有效力交互区间”和“安全停车区间”。微重力响应评估只使用
制动接管前的数据。通过条件不能只看程序没有报错，还必须证明：无物理越界、
无绞盘越程、Trace连续、记录无丢弃、制动距离满足配置，并能由日志离线复算得到
相同边界结论。

## 8. 当前实现进度（2026-09-05）

已完成：ACC/G302 模板名称与结构参数源统一；八连接点物理边界模块；阶段 A
逐步校验；阶段 B 每周期动态停车判定；运行时长和用户停止进入协同减速；固定
急停线；通用轨迹预检、正运动学结果验收和独立 SafetyMonitor 的物理硬边界复核；
界面参数；异步 CSV 中的状态段、停车原因、八连接点坐标、边界量、八轴指令/
Trace 和耗时记录。

尚需实测验收：用合法初始位姿完成阶段 A 边界算例；八电机空转验证手动/时长/
动态边界三种减速入口；检查停止距离和八轴协同性；确认记录零丢弃。真实 F/T
Trace、绞盘和绳索接入仍按第四、第五步推进，不能因当前代码已构建通过就视为
实机安全已验收。
