# CDPR-G302 六维力交互工作空间边界与安全停车设计

## 1. 目的

六维力交互以纯惯性 Newmark-β 模型模拟微重力环境中的接触响应。正常运行区内，
软件应完整保留外力产生的末端加速度、速度和位姿时间历程，不通过公共比例加速度
缩放改变等效质量。一旦安全逻辑介入，本次试验立即判为失败，后续运动只用于安全
停车，不再作为有效微重力响应数据。

本设计首先用于 Lite 模板和平动调试；恢复姿态响应后，再按相同原则扩展角度、
角速度和角减速度边界。

## 2. 边界定义

沿任一工作空间边界的法向方向，从安全区向外依次定义：

```text
正常区 → 动态停车判定边界 xa → 固定急停触发边界 xe → 最终硬边界 xb
```

- `xb`：最终硬边界，动平台绝对不得越过；其数值必须来自实物安全工作空间，
  不能直接用正运动学求解器的数值搜索上下界代替。
- `xe`：固定急停触发边界，位于 `xb` 内侧。越过后立即停止并失能，作为受控
  减速未能及时停车时的最后软件保护。
- `xa`：根据当前朝向该边界的速度动态计算。到达该边界且无法确认系统正在可靠
  远离危险时，停止接收力输入并进入安全停车。

任何一根绳索或绞盘先达到行程限制时，其约束优先于末端空间边界。

## 3. 动态停车判定边界

对每一个边界面独立计算法向速度。以 X 轴上边界为例：

\[
v_n=\max(v_x,0)
\]

保守停车距离为：

\[
d_{stop}=\frac{v_n^2}{2a_n}+v_nT_{delay}+d_{margin}
\]

其中：

- `a_n`：用户设置并经过实机验证、能够保证实现的末端法向停车减速度；
- `T_delay`：从检测触发到减速指令实际生效的控制、Trace、API和驱动器总延迟；
- `d_margin`：离散控制周期、位置跟踪误差、模型误差和测量误差的安全余量。

动态边界相对固定急停边界计算：

\[
x_a=x_e-d_{stop}
\]

对于下边界，应使用指向下边界的速度分量并反向建立同样的距离关系。Y、Z方向
分别计算，不能用末端速度模长代替某个边界的法向速度。

## 4. 进入停车状态的判据

当当前位置尚未到达 `xa` 时，按正常六维力响应运行。到达或越过 `xa` 后，使用
当前力样本和 Newmark 状态预测下一控制周期的法向速度：

\[
v_{n,k+1}=v_{n,k}+a_{n,cmd}\Delta t
\]

- 若指令确实使平台减速或远离边界，并且预测停车余量持续改善，可暂不触发；
- 若仍朝边界加速、维持危险速度，或预测余量没有改善，立即进入停车状态；
- 即使当前加速度方向为减速，只要剩余距离已小于保守停车距离，也必须停车。

判断必须带少量数值容差和连续帧确认，避免浮点噪声在边界附近反复切换；但固定
急停边界和硬边界不得等待连续帧确认。

## 5. 停车状态机

```text
待机
  ↓
正常力响应
  ├─ 模拟输入运行时间到 → 受控减速 → 正常停止
  ├─ 动态边界触发       → 试验失败并冻结力输入 → 受控减速 → 停止
  ├─ 越过固定急停边界   → 立即停止、轴失能、故障锁存
  └─ 越过最终硬边界     → 最高等级故障、轴失能、禁止重启
```

进入停车状态后必须锁存，不允许因为速度降低或重新回到动态边界内侧而恢复力交互。
重新运行必须由操作者确认故障原因、平台位置、绳索和绞盘状态后重新准备。

## 6. 模拟力与实际力的结束逻辑

### 6.1 模拟力调试

模拟运行达到设定时长后，冻结模拟力输入，按用户设置的停车减速度减速至零。
纯惯性模型在外力消失后本应保持匀速，因此该停车段属于人为安全操作，不属于
微重力物理响应。

### 6.2 实际六维力输入

实际传感器模式没有固定运行终点。正常情况下持续响应外力，直至操作者主动停止、
动态边界触发或其他安全保护触发。动态边界一旦触发，本次力交互试验判定失败。

## 7. 停车轨迹与执行能力

正常力交互阶段不采用八轴公共比例加速度限制。进入安全停车后，允许使用独立的
受控减速轨迹，因为此时目标已从保持物理响应切换为安全停车。

UI中的末端最大响应加速度和安全停车减速度应分开定义：

- 最大响应加速度用于检查模拟力、质量和惯量配置是否合理；
- 安全停车减速度用于停车距离预测和停车轨迹生成。

安全停车减速度必须通过空载和逐步加载试验验证。若逆运动学得到的八轴停车指令
超过轴速、轴位置或驱动能力，直接升级安全等级，不能静默缩放后继续宣称试验有效。

## 8. 姿态边界扩展

恢复六自由度响应后，每个姿态边界使用朝向该边界的角速度分量和可实现角减速度：

\[
\theta_{stop}=\frac{\omega_n^2}{2\alpha_n}
                 +\omega_nT_{delay}+\theta_{margin}
\]

线加速度与角加速度必须分别配置。欧拉角仅适合小姿态边界显示和首轮保护；后续若
姿态范围扩大，应改用旋转误差或四元数定义边界，避免欧拉角奇异性。

## 9. 记录与判定

每次运行至少记录：边界法向、当前位置、法向速度、预测法向加速度、`xa/xe/xb`、
停车距离各组成项、触发原因、停车开始时刻、八轴原始指令/实际指令/Trace反馈、
API耗时和最终停止位置。

数据文件必须标记：

```text
有效力交互区间 | 安全停车区间
```

微重力响应评估只使用安全停车开始前的数据。UI只低频显示当前最小边界余量、当前
状态和首次触发原因，逐周期信息由六维力交互异步记录器写盘。

## 10. 实施前仍需确认的参数

1. Lite实物平台的位置与姿态安全工作空间，而非正运动学数值搜索边界；
2. 固定急停触发边界相对最终硬边界的内缩距离；
3. 平动最大响应加速度和保证可实现的停车减速度；
4. 总响应延迟及位置、速度和模型安全余量；
5. 姿态启用后的角度边界、最大角加速度和安全角减速度；
6. 八根绞盘行程限制与末端工作空间边界的联合判定方式。

