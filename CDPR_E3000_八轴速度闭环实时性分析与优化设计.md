# CDPR E3000 八轴速度闭环实时性分析与优化设计

## 1. 文档目的

本文记录当前八轴速度闭环测试中已经观察到的实时性现象、通过分段计时定位出的原因，以及后续程序优化方案，供代码修改、实机复测和人员交接使用。

本文只讨论以下内容：

- 控制线程中的同步写盘问题；
- Trace 对象数量、读取方式及数据传递开销；
- 控制计算、线程调度、日志和绘图对实时性的影响；
- 优化后的对照试验和验收方法。

现阶段不优化八轴速度指令 API 的调用策略。八轴仍在每个控制周期分别下发速度指令，不增加速度死区，不跳过指令，也不以批量接口替代现有调用。

## 2. 当前控制链路

八轴速度闭环的主要周期链路为：

```text
控制周期到期
  -> 读取并解析 Trace 数据
  -> 按主机时间计算期望轨迹
  -> 读取最新实际位置、实际速度
  -> 八轴位置误差与 PID 计算
  -> 八轴速度指令下发
  -> 安全判断、状态更新与记录
```

理论或规划状态以主机控制时间为基准。Trace 反馈通过启动锚点和帧序号映射到相对运行时刻；标定延迟只用于跟随误差对齐及其保护，不参与主机侧期望轨迹和速度指令的生成。

## 3. 已观察到的实时性现象

### 3.1 最近一次八轴测试统计

目标控制周期为 1 ms，最近一次完整运行得到：

| 指标 | 平均值 | 最大值 |
| --- | ---: | ---: |
| 实际调度间隔 | 4.861 ms | 8.111 ms |
| 完整控制周期 | 4.786 ms | 8.040 ms |
| Trace 读取与处理 | 2.674 ms | 4.437 ms |
| 控制计算 | 0.670 ms | 1.940 ms |
| 八轴速度 API 合计 | 1.436 ms | 2.922 ms |

完整控制周期 P99 约为 5.742 ms。3293 个控制周期全部发生执行超期，其中 3292 个周期发生调度超期，估算累计错过约 12965 个 1 ms 周期。

这说明当前程序不是偶发偏离 1 ms，而是稳定运行在约 5 ms 的实际节拍上。1 ms 只能作为目标配置值，不能视为已经实现的控制周期。

### 3.2 Trace 帧数与耗时的关系

一次轮询通常需要处理 4～5 个积压帧，读取帧数越多，Trace 耗时整体越高：

| 本次处理帧数 | Trace 平均耗时 | 完整周期平均耗时 |
| ---: | ---: | ---: |
| 4 帧 | 约 2.475 ms | 约 4.609 ms |
| 5 帧 | 约 2.712 ms | 约 4.813 ms |
| 6 帧 | 约 2.857 ms | 约 4.975 ms |
| 7 帧 | 约 3.389 ms | 约 6.183 ms |

由于控制周期本身超过 1 ms，卡侧会在下一次主机轮询前继续产生新帧，形成“周期越慢、积压帧越多、Trace 处理又越慢”的反馈关系。

### 3.3 8 ms 尖峰不是单一原因

启动初期存在两类可以稳定复现的尖峰：

- 建立 Trace 启动锚点时，控制线程同步更新一次运行上下文；
- 跟随误差保护开始生效时，控制线程再次同步更新运行上下文。

上述周期中，Trace、计算和八轴 API 之外仍有约 3.2～3.3 ms 未归入常规分项。代码检查确认这部分主要来自 `updateCdprRunContext()` 对 `run_context.json` 的同步序列化、写入和 `QSaveFile::commit()`。

另有少量后期尖峰来自 Trace、计算和 API 同时变慢。例如某周期中 Trace 约 4.437 ms、计算约 1.432 ms、API 约 1.930 ms，三者相加已经接近 7.8 ms。这类尖峰不能仅靠异步写盘消除。

## 4. 写盘架构分析

### 4.1 原工程的高频记录方法

原工程的高频力控记录采用分层处理：

```text
控制线程产生样本
  -> 写入内存环形缓冲区
  -> UI 线程每 100 ms 批量取出
  -> 暂存到运行记录数组
  -> 停止记录后由后台线程导出 CSV
```

其优点是控制线程不在每个高频周期直接打开、写入或刷新文件。

原工程仍存在以下风险：

1. 控制线程写环形缓冲区和 UI 批量取样使用同一把互斥锁。当 UI 堵塞并积累大量样本后，批量搬移可能短暂阻塞控制线程。
2. Qt 全局日志处理器对每条日志立即执行 `flush()`。如果日志由控制线程直接产生，磁盘刷新可能在该线程内发生。
3. 并非所有配置、诊断和运行文件都通过同一后台写盘通道处理，部分同步导出仍可能造成 UI 卡顿或流程等待。

因此，原工程的高频记录思路值得保留，但不能照搬其互斥缓冲和逐条日志刷新方式。

### 4.2 当前工程的高频记录方法

当前工程的 `TelemetryRecorder` 已采用更适合高频采样的方式：

```text
控制线程写无锁环形缓冲区
  -> 独立写盘线程每 100 ms 批量取出
  -> 写入 Trace 二进制文件和周期耗时 CSV
```

控制线程通过原子读写索引投递数据；写盘线程负责文件 I/O。正常运行期间不会因为逐帧 CSV 写入而直接等待磁盘。

当前架构的主要例外是 `run_context.json`：运行中某些状态变化会直接调用 `QSaveFile` 重写整个 JSON 文件并提交，且调用发生在控制线程。这是已经定位到的确定性尖峰来源。

### 4.3 写盘优化原则

- 控制线程只更新内存数据，不执行 JSON 序列化、文件打开、文件提交和磁盘刷新；
- `run_context.json` 更新改为向记录线程投递键值变化，由记录线程合并保存；
- 高频变化不必每次落盘，可在记录线程中按时间合并，运行结束时保证写入最终状态；
- 故障、停止等关键事件可立即投递，但仍由写盘线程处理；
- 控制循环中的日志只记录结构化数值和计数，中文格式化及 UI 显示放到低频线程；
- 禁止控制线程直接调用会触发同步 `flush()` 的日志路径；
- 停止记录时允许等待写盘线程排空队列，因为此时已经退出正常控制周期。

## 5. Trace 对象分析

### 5.1 当前配置

当前固定为每轴四个对象：

| 类型 | 含义 | 八轴速度闭环中的用途 |
| --- | --- | --- |
| type 03 | 板卡指令速度 | 对照主机 PID 命令与板卡实际采用的速度指令 |
| type 04 | 实际速度 | PID 微分项、实际速度监控和安全判断 |
| type 05 | 板卡指令位置 | 驱动执行层诊断、位置延迟标定辅助量 |
| type 06 | 实际位置 | 位置闭环、跟随误差保护、绳长和正运动学 |

八轴共配置 32 个对象。

原工程直接下发位置指令，其 Trace 对象选择与当前速度指令闭环不同，不能根据原工程的位置模式直接决定当前对象集合。两者只能在“采样数据先缓存、再异步处理”的记录架构上类比。

### 5.2 常规八轴速度闭环建议

常规运行保留：

```text
type 03 + type 04 + type 06
8 轴 x 3 对象 = 24 对象
```

删除常规运行中的 type 05，原因是速度模式下的卡内指令位置不是主机规划位置，不能替代“规划位置－实际位置”这一控制策略跟随误差。

type 03 虽然不是生成控制指令的必需反馈，但能够判断主机 PID 命令是否被板卡及时采用，具有重要故障诊断价值，第一轮优化中保留。

### 5.3 Trace 配置档位

建议增加内部配置档位，而不是让所有功能永久共用一套 32 对象配置：

| 使用场景 | 建议对象 |
| --- | --- |
| 八轴速度闭环 | type 03、04、06 |
| 单轴速度闭环完整诊断 | type 03、04、05、06 |
| Trace 位置延迟标定 | 被标定轴的 type 05、06，必要时增加 type 03、04 |
| PVT 执行诊断 | type 05、06 |
| 最小位置/速度反馈 | type 04、06 |

Trace 只允许在测试启动前停止、复位并重新配置，不在运动过程中动态切换对象。

### 5.4 解析器同步修改要求

当前解析器默认每帧必定存在“轴数 x 4”个对象，并按四个固定数组偏移取值。不能只删除 type 05 配置，否则会造成帧对象数量不足或字段错位。

后续必须同时完成：

- 为每个配置对象保存明确的语义和轴号；
- 按对象映射解析，而不是按固定四段偏移解析；
- 在反馈结构中增加字段有效标志；
- 未配置字段不得用零冒充有效值；
- 数据记录元数据中保存本次 Trace 对象清单；
- 延迟标定、绘图、PVT和八轴闭环分别检查自己需要的字段是否有效。

将对象数从 32 减至 24 后，卡侧帧长度、主机传输量和解析量都会下降，但不能预设耗时严格按比例下降，必须通过对照试验验证。

## 6. Trace 读取与数据传递优化

### 6.1 控制与记录使用不同数据需求

控制计算只需要最新一帧有效反馈；完整时间历程记录需要保留本次读出的所有帧。因此读取后应分流：

```text
一次批量读取卡侧 Trace
  -> 最新有效帧更新控制快照
  -> 全部原始帧送入无锁记录队列
```

不得为了控制计算而逐帧重复更新同一轴状态，也不得为了记录再次调用 Trace API。

### 6.2 降低周期内分配与复制

- Trace 批量容器在初始化时预分配并循环复用；
- 避免每周期创建临时 `QVector`、`QString` 和映射容器；
- 原始帧尽量一次解析，控制快照和记录帧共享该解析结果；
- UI绘图只接收低频批次，不接收逐帧信号；
- 控制线程不生成包含大量 `.arg()` 的诊断文本，异常时先保存结构化故障快照；
- 对 Trace 耗时继续细分为“库函数调用、原始数据复制、对象解析、记录投递”。

### 6.3 是否拆分 Trace 线程

只有在完成对象精简、批量读取和内存优化后，Trace API 固有耗时仍无法满足目标周期时，才考虑独立采集线程。

在未确认 LTDMC 动态库和板卡接口线程安全前，不允许 Trace 线程与速度命令线程无约束地并发调用雷赛 API。更稳妥的扩展方式是建立唯一硬件 I/O 线程，由其按确定顺序执行 Trace 读取和八轴命令，控制计算线程通过快照与命令邮箱交换数据。

## 7. 控制计算、调度与 UI 优化

### 7.1 控制计算

- 八轴状态使用固定长度数组，避免周期内动态分配；
- PID、轨迹计算、逆运动学和安全检查复用工作区；
- 将“参考轨迹/动力学、逆运动学、八轴 PID、安全检查”分别计时；
- 仅在参数或运行阶段变化时更新配置快照；
- 正运动学后处理和虚拟一致性分析不得进入实时控制周期。

### 7.2 调度方式

- 使用单调时钟和绝对截止时刻，避免按上一周期完成时刻累计漂移；
- 控制线程保持较高优先级，禁止承载 UI、绘图、文件导出和离线分析；
- 发生超期时记录错过周期数，不通过连续补跑多个过期控制周期制造更严重拥塞；
- Trace 新鲜度根据帧序号和采样周期判断，不根据 UI 收到信号的时间判断。

### 7.3 绘图和日志

- 保持“高频存点、低频批量绘制”；
- UI 刷新建议不高于 20 Hz；
- 显示数据采用抽稀或分桶极值，不把全部高频点重复送入图表；
- 日志采用限频摘要，周期内重复状态只累计次数；
- 故障发生时保存一次完整结构化快照，再由 UI 生成可读日志。

## 8. 不纳入本轮的优化项

本轮不改变八轴速度指令 API 的语义和调用频率，具体包括：

- 不因速度变化较小而跳过某一轴的指令；
- 不增加速度命令死区；
- 不将八轴共用为一条批量命令；
- 不改变 `dmc_vmove` 与在线变速函数的现有调用关系；
- 不通过降低下发频率来换取表面上的周期耗时下降。

八轴 API 耗时仍继续记录，作为完整周期预算的一部分，但不作为本轮代码优化目标。

## 9. 建议实施阶段

### 阶段 A：消除确定性写盘尖峰

1. 将 `run_context.json` 的运行中更新移出控制线程；
2. 增加内存上下文和异步更新消息；
3. 记录线程合并写入，结束时强制保存最终状态；
4. 检查控制线程内所有文件、日志刷新和阻塞式跨线程调用；
5. 复测启动锚点和保护启用附近的周期耗时。

### 阶段 B：Trace 从 32 对象精简为 24 对象

1. 建立对象语义映射和配置档位；
2. 修改解析器和字段有效性；
3. 八轴速度闭环使用 type 03、04、06；
4. 延迟标定、PVT和单轴诊断使用各自配置；
5. 确认跟随误差、绘图、日志和离线分析没有读取无效字段。

### 阶段 C：减少 Trace 处理和控制计算开销

1. 批量读取后仅用最新帧更新控制快照；
2. 全部帧直接送往记录队列；
3. 复用内存，移除周期内非必要字符串和容器；
4. 细分 Trace 和计算耗时；
5. 根据结果决定是否需要唯一硬件 I/O 线程。

### 阶段 D：调度、绘图和日志整理

1. 使用绝对时刻调度；
2. UI固定低频批量刷新；
3. 日志限频和故障快照化；
4. 检查停止、急停和记录排空过程不会回流阻塞正常控制。

### 9.1 当前实施状态（2026-08-11）

本轮已经完成以下代码修改：

- `run_context.json` 的运行中更新只投递到记录线程，由记录线程按100 ms节拍合并序列化和提交；初始化仍在运动开始前同步完成，结束时强制排空最终状态；
- 八轴速度闭环 Trace 从每轴 type 03/04/05/06 精简为 type 03/04/06，共24个对象；离线PVT使用 type 05/06，共16个对象；单轴诊断和延迟标定保留完整 type 03/04/05/06；
- 一次硬件线程调用同时返回最新轴反馈、完整Trace帧批次、读取诊断和状态文字，代替控制线程内多次阻塞式跨线程查询；
- 控制快照只由最新Trace帧更新，完整批次仍原样进入异步记录队列；
- Trace原始读取缓冲区循环复用，不再为每次SDK读取重新分配内存；
- 轴状态机和轴错误码改为10 ms刷新一次，轨迹位置和速度仍按每次Trace读取更新；
- 八轴速度闭环不再执行依赖type 05的卡内指令位置延迟队列计算，也不再执行只供单轴页面显示的速度诊断遍历；
- 二进制 `TraceTelemetryFrame` 布局和记录格式版本保持不变，已有同版本日志仍可由虚拟运动学一致性分析器读取；
- 八轴速度指令的逐轴、逐周期调用策略没有改变。

已完成Debug配置的全量清理重编译。上述改动仍需通过实机同参数复测确认实际耗时改善，不能仅凭编译通过判定实时性目标已达成。

## 10. 对照试验与验收

### 10.1 对照矩阵

每次实机复测保持相同轨迹、相同 PID 参数和相同轴映射，依次比较：

1. 自动记录关闭与开启；
2. 同步运行上下文写入与异步写入；
3. 32 对象与 24 对象 Trace；
4. 1 ms、5 ms、10 ms 配置周期；
5. 单轴与八轴运行。

每组至少记录：平均值、P95、P99、最大值、周期超期次数、估算错过周期数、Trace 丢帧数、记录队列丢弃数和每次读取帧数分布。

### 10.2 功能验收

- 八轴运动方向、轨迹终点和现有安全逻辑不发生变化；
- 跟随误差仍为延迟对齐后的“规划位置－Trace 实际位置”；
- Trace 序号连续，记录帧和周期耗时样本无丢弃；
- 运行上下文包含完整启动锚点、保护启用状态和结束原因；
- 单轴延迟标定、PVT记录和八轴虚拟一致性分析仍可读取各自所需字段；
- 程序停止后文件可以完整关闭并被离线分析工具读取。

### 10.3 实时性阶段目标

第一阶段不承诺实现 1 ms 八轴周期。建议先以 5 ms 作为工程验证基准：

- 消除与运行上下文写盘对应的确定性 8 ms 尖峰；
- 完整周期平均值明显低于 5 ms；
- 完整周期 P99 低于 5 ms；
- 记录开启前后周期统计无显著恶化；
- Trace对象从32减至24后，读取耗时有可重复的下降。

完成上述目标后，再根据 Trace API 的实测固定耗时评估更短周期是否具有实现条件。

## 11. 结论

当前八轴速度闭环实时性不足由多项耗时叠加造成：Trace 是最大的持续开销，同步更新运行上下文是已确认的确定性尖峰来源，控制计算、内存搬移、日志与线程调度共同放大了尾部延迟。

后续不通过减少八轴速度指令调用来规避问题，而是优先完成写盘隔离、Trace 对象精简、批量数据分流、内存复用和调度整理。每项修改都必须通过同一套周期分段统计进行前后对照，避免以主观观察替代实时性结论。
