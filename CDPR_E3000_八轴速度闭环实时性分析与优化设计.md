# CDPR E3000 八轴速度闭环实时性分析与优化设计

## 1. 目的与结论

本文记录八轴速度闭环的实时链路、已完成的优化和2026年8月12日实机对照结果，作为后续CDPR控制周期选择与继续优化的依据。

当前结论如下：

- 异步运行上下文写盘已经消除原先约3 ms的确定性额外尖峰；
- Trace由32个对象精简为24个后，读取与处理耗时明显下降；
- 5 ms控制周期已经基本稳定，可作为当前八轴联调和后续力交互控制的工程基准；
- 1 ms配置下实际周期约为2.43 ms，现有串行Trace读取和八轴速度API调用无法满足1 ms；
- 本轮优化没有降低八轴轨迹跟踪和虚拟CDPR运动学一致性。

## 2. 控制链路与时间基准

八轴速度闭环每个控制周期依次执行：

```text
读取并解析八轴Trace
→ 按主机运行时间查询期望轨迹
→ 计算八轴位置误差和独立PID
→ 逐轴下发速度命令
→ 安全判断与异步数据记录
```

规划位置和规划速度使用主机运行时间。Trace反馈通过启动锚点、帧序号和1 ms采样周期映射到运行时刻。各轴标定延迟只用于计算“规划位置(t-τ)－Trace实际位置(t)”及其超限保护，不改变期望轨迹和速度命令的主机时间基准。

八轴仍共用一套PID参数，但每轴保存独立PID状态。本轮没有改变逐轴、逐周期调用速度API的策略，没有增加速度死区或跳过小变化指令。

## 3. 优化前的问题

优化前，1 ms测试实际稳定运行在约5 ms节拍：

- Trace一次处理约4～5个积压帧，平均耗时2.674 ms；
- 八轴速度API平均耗时1.436 ms；
- 控制计算平均耗时0.670 ms；
- 启动锚点和跟随保护启用时同步重写`run_context.json`，产生约3.2～3.8 ms未归类耗时；
- 以上耗时叠加后，完整周期平均4.786 ms、最大8.040 ms。

控制周期越长，下一次读取时积压的Trace帧越多，读取又进一步变慢。同步JSON写盘还会在特定状态变化处形成明显尖峰。

## 4. 已完成的优化

### 4.1 写盘隔离

Trace原始帧和周期耗时仍由控制线程写入无锁环形缓冲区，独立记录线程每100 ms批量落盘。`run_context.json`的运行中更新也改为只投递键值变化，由记录线程合并序列化和提交；停止记录时再强制保存最终状态。

因此，正常控制周期不再执行JSON序列化、文件打开、`QSaveFile::commit()`或磁盘刷新。

### 4.2 Trace按用途配置

| 功能 | Trace对象 |
| --- | --- |
| 八轴速度闭环 | type 03指令速度、type 04实际速度、type 06实际位置，共24个对象 |
| 离线PVT | type 05指令位置、type 06实际位置，共16个对象 |
| 单轴完整诊断与延迟标定 | type 03、04、05、06 |

速度闭环不再采集没有控制意义的卡内指令位置type 05。跟随误差继续使用主机规划位置与type 06实际位置计算。

### 4.3 热路径整理

- 一次硬件线程调用返回最新反馈、完整Trace帧批次、诊断和状态，减少阻塞式线程往返；
- 控制计算只用最新帧，完整帧批次直接送往记录队列；
- Trace原始读取缓冲区循环复用；
- 八轴速度闭环不再遍历仅供单轴页面使用的诊断数据；
- 轴状态机和轴错误码按10 ms刷新，位置和速度仍按Trace帧更新；
- 二进制记录帧结构保持兼容，离线一致性分析仍可直接读取。

## 5. 对照试验

### 5.1 统一参数

- 末端相对位移：Δx=0.2 m、Δy=0.1 m、Δz=1.0 m；姿态不变；
- 五次多项式轨迹时间：15 s；
- 速度前馈：启用，增益1.0；
- PID：启用，但Kp=Ki=Kd=0，作为纯速度前馈基线；
- 轴速度上限：720 °/s；
- 轴加速度上限：2000 °/s²；
- 在线变速时间：1 ms；
- 最大跟随误差：2°；
- 板卡当量：500.622 pulse/unit，1 unit=1°；
- Trace采样周期：1 ms。

测试分别采用5 ms和1 ms控制周期。两组均运行完整15 s轨迹，并执行1 s终点收敛。

### 5.2 实时性结果

| 指标 | 优化前5 ms | 优化后5 ms | 优化前1 ms | 优化后1 ms |
| --- | ---: | ---: | ---: | ---: |
| 实际周期均值 | 5.029 ms | 5.000 ms | 4.861 ms | 2.431 ms |
| 实际周期最大值 | 8.580 ms | 6.014 ms | 8.111 ms | 5.528 ms |
| 完整周期均值 | 4.838 ms | 2.883 ms | 4.786 ms | 2.368 ms |
| 完整周期P99 | 6.144 ms | 4.382 ms | 5.743 ms | 3.876 ms |
| 完整周期最大值 | 8.507 ms | 5.449 ms | 8.040 ms | 5.412 ms |
| Trace平均耗时 | 2.706 ms | 1.420 ms | 2.674 ms | 0.923 ms |
| 控制计算平均耗时 | 0.681 ms | 0.030 ms | 0.670 ms | 0.030 ms |
| 八轴API平均耗时 | 1.445 ms | 1.428 ms | 1.436 ms | 1.412 ms |
| 执行超时 | 856/3184 | 3/3202 | 3293/3293 | 6574/6584 |
| 调度超时 | 5 | 0 | 3292 | 6573 |
| 估算漏周期 | 5 | 0 | 12965 | 8998 |

5 ms下完整周期P99低于周期预算，3202个周期仅3次轻微执行超时，没有调度超时和漏周期。1 ms下虽然性能提高约一倍，但Trace与八轴API的平均耗时之和已经约为2.335 ms，1 ms在当前调用链上没有实现条件。

## 6. 尖峰与写盘结论

优化前，扣除Trace、计算和八轴API后，未归类耗时最大为：

- 5 ms测试：3.826 ms；
- 1 ms测试：3.309 ms。

它们与运行上下文同步提交对应，是完整周期达到约8 ms的重要原因。

优化后，未归类耗时最大仅在启动首周期达到0.242～0.284 ms；进入正常运行后最大约0.025～0.028 ms，也没有发现每100 ms随记录线程落盘出现的周期性尖峰。由此确认同步写盘尖峰已经消除。

当前仍存在少量Trace和八轴API同时升高形成的组合尖峰：

- 5 ms最大周期5.449 ms，其中Trace 3.226 ms、八轴API 2.167 ms、其他约0.006 ms；
- 1 ms最大周期5.412 ms，其中Trace 2.548 ms、八轴API 2.788 ms、其他约0.006 ms。

这些尖峰来自雷赛Trace和逐轴速度API调用，不是磁盘写入，也不是主机控制计算。

## 7. 运动效果验证

两组测试均满足：

- 15000/15000帧可完成数值分析；
- Trace序号断裂为0；
- 没有跟随误差、Trace或轴故障；
- 八轴运行正常完成。

| 虚拟CDPR一致性指标 | 5 ms | 1 ms |
| --- | ---: | ---: |
| 末端平移RMS误差 | 0.0755 mm | 0.0641 mm |
| 最大平移误差 | 0.1082 mm | 0.1128 mm |
| 最大姿态误差 | 0.0123° | 0.0172° |
| 最大绳长闭合残差 | 37.67 μm | 27.14 μm |

按拟合后的全局延迟对齐后，5 ms测试各轴角度误差RMS为0.021～0.044°、最大0.086°；1 ms测试RMS为0.018～0.036°、最大0.084°。优化没有造成轨迹跟踪退化。

## 8. 当前工程决策与后续工作

1. 后续八轴联调和CDPR力交互控制首先采用5 ms周期，Newmark、逆运动学、八轴PID和命令更新使用同一5 ms步长。
2. 用相同参数重复运行至少3次，确认5 ms下P99、执行超时和漏周期具有重复性。
3. 保留现有分段计时，继续观察Trace和单轴API的长尾，而不是只看平均值。
4. 1 ms仅作为压力测试。若未来必须实现1 ms，需要重新设计硬件I/O链路或更换支持确定周期命令交换的接口；继续削减主机计算和写盘已不足以达到目标。
5. 在没有新的实测依据前，不通过速度死区、跳过某轴命令或降低下发频率掩盖周期超时。

本阶段实时性优化已达到预定目标：写盘尖峰消除、Trace负担降低、5 ms周期基本稳定，且八轴虚拟运动学一致性保持良好。
